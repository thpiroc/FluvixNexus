# Fluvix Nexus 設計ドキュメント

> ステータス: STEP 1（基盤構築）完了 / STEP 2（Dockable Workspace 基盤）完了 / STEP 3（各パネルの本機能）完了 / STEP 4（日常使用の土台）完了 / STEP 5（LSP）完了 / **STEP 6（DAP）実装中（Session 6-3）**
> 最終更新: 2026-09-10

このドキュメントは**製品としての設計方針**を扱う。実装の構造と開発手順は以下を参照。

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — 現在の実装構造、IPC の作法、セキュリティの状態
- [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) — コマンド、品質基盤の方針、動作確認の手順、exe 化の残作業

## 1. 目的

軽量・シンプルでありながら、実際のアプリ開発に日常的に使える Windows 向けコードエディタを作る。

VS Code や Cursor のように何でも詰め込むのではなく、**必要な機能を高品質に絞り込む**方向性を取る。まずは開発者本人が日常的に使えるレベルの品質を目指し、最終的には `.exe` 化して他のユーザーにも無料で配布できるデスクトップアプリにする。

核となるコンセプトは以下の一言に集約される。

> 必要な開発機能だけを高品質に持ち、Files・Terminal・GitHub・Workspace を自由に組み替えて、自分専用の開発環境を作れる軽量コードエディタ。

---

## 2. 基本構成（技術スタック）

- **Electron + React + TypeScript** を第一候補とする。
- プロセス構成は以下を明確に分離する。
  - **Main Process**：アプリのライフサイクル管理、ウィンドウ管理、OS 連携処理の実体
  - **Renderer**：UI 描画（React）。OS に直接触れる処理は行わない
  - **Preload**：Renderer に安全な API のみを橋渡しする層
  - **IPC**：Main と Renderer 間の通信経路
- Terminal・Git 操作・ファイル操作など OS に触れる処理は Renderer から直接行わず、Main Process 経由の安全な構成にする。
- Node.js / Python / .NET SDK 本体はアプリに同梱せず、PC にインストール済みの環境を検出して利用する（軽量さを優先）。

---

## 3. パネル構成

基本パネルは4つ。すべて独立した Dockable Panel として扱う。

| パネル    | 役割                                          |
| --------- | --------------------------------------------- |
| Workspace | コード編集                                    |
| Files     | ファイル管理                                  |
| Terminal  | PowerShell、Node、Claude Code などの CLI 実行 |
| GitHub    | Commit / Push / Pull などの Git 操作          |

### Dockable UI

- 上下左右への移動、リサイズ、タブ化、独立ウィンドウ化に対応する。
- パネルサイズに応じて UI が自動的に形を変える（レスポンシブ・パネル）。
- パネル識別のため、非常に薄い識別色を使う（Session 4-4 で Dark / Light の両方に用意した）。
  - Files：薄い青
  - Terminal：薄い緑
  - GitHub：薄いオレンジ or 紫
  - Workspace：ほぼ無彩色
- 自由に配置を変えても、どのパネルかが直感的に分かることを目指す。

### パネルの表示切り替え

- 使わないパネルは閉じられ、必要になったら View メニューからいつでも戻せる。
- 閉じることはパネルの定義を消すことではない。閉じたパネルも一覧には残り続ける。
- 戻したときは「本来の居場所」＝適用中のプリセットでの位置に近い場所へ戻る。既に組んだ配置は崩さない。

### レイアウトプリセット

- 自分好みの配置を保存・復元できるようにする。
- 例：Coding / Claude Code / Debug などのプリセット、および自作レイアウトの保存。
- 現在は Default のみ実装。プリセットは表に追加するだけで増える構造にしてある。
- レイアウトロック機能も候補として検討する。

### 前回の配置で起動する

- 組み替えた配置（分割・タブ・大きさ・閉じたパネル）は自動で保存し、次回起動時にそのまま復元する。保存操作は要求しない。
- 保存先はアプリの設定データ（`%APPDATA%` 配下）で、プロジェクトフォルダには書かない。配置は「その人の道具の形」であって、プロジェクトの持ち物ではないため。
- 保存データが壊れていても起動できなくならないこと。読めなければ Default の配置で立ち上げる。
- 「レイアウトを初期化」は画面だけでなく保存内容も初期化する（次回起動で古い配置が戻らない）。

### Files パネルの表示切り替え

- 縦長時：一般的なファイルツリー表示。
- 横長時：Unity の Project ウィンドウ + Mac Finder のカラム表示のように、フォルダ階層を横方向に展開する表示。
- **どちらも同じファイルを見ている。** 表示方式が変わるのは並べ方だけで、開いているフォルダ・読み込み済みの中身・操作（作成 / 改名 / 削除 / 移動 / コピー / ドラッグ&ドロップ / 検索）は共通。切り替えても読み直しは起きない。
- **形に合わせるが、選んだら勝手に変えない。** まだ選んでいない間はパネルの形に従う（横長ならカラム、縦長ならツリー）。ツールバーで選んだ後は、リサイズしても置き場所を変えても選んだ方のままにする。境界の付近でパネルを掴んで動かしても表示方式が往復しないよう、切り替わる幅と戻る幅には遊びを持たせる。
- 選び直しはいつでもでき、選択中のボタンをもう一度押せば「形に合わせる」状態へ戻る。
- **選んだ見え方はアプリを閉じても残る**（表示方式・カラムの幅）。カラムの幅は境目を掴んで変えられ、全部の列が同じ幅になる。

### GitHub パネル

- Git 初心者でもターミナルを使わず操作できることを目標にする。
- 初回のみ「GitHub に公開」ボタンから、Git 初期化 → 初回 Commit → GitHub リポジトリ作成 → remote 設定 → Push までを案内・自動化する。
- 2回目以降は `Commit & Push` / `Commit` / `Push` / `Pull` の操作ボタンを表示する。
- 初心者向けに「①変更確認 → ②コミットメッセージ → ③Commit & Push」の順番が分かるガイド UI を用意する（※詳細な UI 実装は後日）。
- 単体ウィンドウとして切り離して表示可能にし、Unity や DTM ソフトなど別の制作環境の横に並べて使えるようにする。

### Terminal パネル

- 自由ドッキングに対応。
- 複数タブを持ち、PowerShell / Claude Code / Node / その他 CLI を切り替えて使える。
- Claude Code を長時間使う場合、Terminal を右側に大きく配置するような使い方も想定する。
- 文字の大きさとさかのぼれる行数は Terminal の設定（タブ列の ⚙）から変えられ、アプリを開き直しても保たれる。

### Workspace（開いているプロジェクトフォルダ）

4つのパネルはいずれも「今開いているフォルダ」を共通の対象として動く。Files はそのフォルダを辿り、Terminal はそこを作業ディレクトリとして起動し、Git はそこをリポジトリとして扱い、Editor はその中のファイルを開く。したがって**フォルダを開くことがアプリの入口**になる。

- フォルダを開く / 閉じるができる。開く手段は OS 標準のフォルダ選択ダイアログ。
- 次回起動時は前回のフォルダで開く。保存操作は要求しない（レイアウトと同じ考え方）。
- 保存先はアプリの設定データ（`%APPDATA%` 配下）。**プロジェクトフォルダの中には何も書かない。**
- 前回のフォルダが消えている / 繋がっていない場合は、落ちずに未選択の状態で起動し、理由を伝える。
- 未選択のときは Editor に Welcome を出す。「まず何をすればよいか」が分かる状態にしておく。
- 「最近開いたフォルダ」は将来対応（開いた記録は id / 名前 / 開いた時刻を持つ形にしてある）。

#### 用語について

「Workspace」はこのドキュメントの中で3つの意味に使われている。実装では次のように呼び分ける。

| ここでの呼び方                 | 実装での名前     | 実体                         |
| ------------------------------ | ---------------- | ---------------------------- |
| Workspace（§3 の表のパネル）   | Editor パネル    | コード編集の画面             |
| 画面全体の器（Dockable UI）    | Workspace Shell  | パネルを並べる枠と、その配置 |
| 開いているプロジェクトフォルダ | Workspace Folder | このセクションが扱うもの     |

---

## 4. コードエディタ（Workspace）

**Monaco Editor + LSP** を有力候補とする。

最低限、以下の機能を実装し、「VS Code ほど多機能ではなくても、1日普通に開発してストレスが出ない品質」を目標とする。

- シンタックスハイライト
- 行番号
- コード補完
- エラー・警告表示
- オートインデント
- 複数タブ
- 検索・置換
- プロジェクト全体検索
- Go to Definition
- Rename Symbol
- フォーマット
- コメント切替
- 基本ショートカット

「1日普通に開発してストレスが出ない」ためには、上の一覧に無いものも要る。**アプリの外でファイルが変わる**（ビルドツール・Git の checkout・別のエディタ）ことへの備えと、**未保存の内容を取り違えて失わない**ことがそれで、Session 3-5 で入れた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12）。機能の数には出ないが、無いと「開いたまま作業を続けられない」。

### 対応言語（v1）

v1 から正式対応する言語は以下の3つとする。

- JavaScript / TypeScript
- Python
- C#

Node.js、Python、.NET SDK 本体はアプリに同梱せず、PC にインストールされている環境を検出して LSP 経由で利用する。

---

## 5. デバッグ機能

v1 から対応する。**DAP（Debug Adapter Protocol）** の利用を前提に検討する。

最低限実装する機能：

- Breakpoint
- Continue
- Pause
- Step Over
- Step Into
- Step Out
- Stop
- Variables
- Call Stack
- Debug Console
- エラー位置へのジャンプ

**何を実行するかは、`.vscode/launch.json` ではなく Fluvix Nexus 独自の Debug Profile が持つ**（Session 6-0 の決定）。Workspace の中に置かれたファイルが「何が起動するか」を決める形にはしない ── フォルダを開いただけで実行対象が決まるのは、LSP で「Workspace の中の実行ファイルは探さない」と決めたのと同じ線に反する（§12・[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §19.2）。

Profile は Main が `userData` 配下に Workspace 単位で持ち、Renderer が触れるのは**アプリの言葉で書かれた安全な欄だけ**になる。Debug Adapter の実行ファイル・引数・作業ディレクトリ・絶対パス・環境の組み立ては Main の側にあり、Renderer には欄が無い。決定の全体は §13、構造は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20。

---

## 6. Claude Code 連携

- 独自 AI を新規開発するのではなく、**Claude Code を操作しやすくする UI** として設計する。
- **第一段階**：Terminal パネルから Claude Code を CLI としてそのまま利用する。
- **将来段階**：専用パネルから自然言語で指示を書き、以下の流れを直接つなげる。

  ```
  Fluvix Nexus（専用パネル） → Claude Code → 現在開いているプロジェクトを編集
  ```

- Claude Code などの外部 AI からの指示は、本ソフト上の Terminal または Workspace 上で実行される構成とし、AI エージェント自体の実装は行わない（あくまで実行環境・操作 UI を提供する立場）。

---

## 7. 配布・更新まわり（方針レベル）

以下の3点は、現時点では詳細実装ではなく**方針**として整理する。詳細は STEP 1 以降で詰める。

### 7.1 Windows 向け exe 化

- 最終的に Windows の `.exe` として配布する。
- Installer / アプリアイコン / バージョン管理 / GitHub Releases 対応を見据えた構成にしておく。
- **electron-builder** を前提とする。STEP 1 では導入せず、それを妨げない構成（`productName` の設定、`electron` の devDependencies 化、`out/` への成果物分離、ユーザーデータを `userData` 配下へ統一）までを済ませている。残作業は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §6 を参照。

### 7.2 自動更新（方針）

- **electron-updater** を前提候補とする。
- 配布形式・署名方式などの詳細は将来対応として整理し、現時点では確定しない。

### 7.3 更新通知 UI（方針）

- 自動更新で取得した更新内容を、既存の Dockable UI に馴染む形で通知する。
- **専用パネル方式**または**モーダル方式**のどちらか（あるいは両方の使い分け）を想定するが、具体的な UI 設計は後日詰める。

### 7.4 無料配布（方針）

- ユーザーには無料で提供する方針。
- 配布は **GitHub Releases** を基本とする。
- Windows SmartScreen 対策やコード署名（証明書の取得・運用）は、**将来対応**として切り分ける（v1 の必須要件にはしない）。

---

## 8. 将来的な Mac 対応を妨げない設計方針

- 開発対象自体は当面 Windows 向けだが、以下の点でクロスプラットフォーム化の余地を残す。
  - Electron を採用することで OS 依存部分を Main Process に閉じ込め、Renderer / UI ロジックは OS 非依存に保つ。
  - OS 固有処理（ファイルパス、シェル起動コマンド、通知 API など）は抽象化レイヤーを介して呼び出し、Windows 固有実装を差し替え可能な形にしておく。
  - パネルレイアウト・エディタ・LSP/DAP 連携などのコア機能は OS に依存しない設計とする。
  - 自動更新・署名まわりは OS ごとに実装が異なることを前提とし、将来的に macOS 対応を追加する際に electron-updater 側の macOS 対応（notarization 等）を後から組み込める構成にしておく。
- v1 では Mac 対応の実装は行わない。あくまで「後から対応を追加しやすい構造にしておく」ことが目的。

---

## 9. 未確定・後日詰める事項

- 更新通知 UI の具体的なビジュアル・配置
- コード署名 / Windows SmartScreen 対策の具体的な進め方
- テレメトリ・クラッシュレポートの要否
- 初心者向けガイド UI の詳細（Git ガイド以外にも展開するか）
- レイアウトロック機能の要否と仕様
- Mac 対応を実際に開始するタイミング
- 対応する文字コードの範囲（UTF-8 以外を開けるようにするか。境界は整理済み ── [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.5）
- Workspace の外のファイルを**開けるようにするか**（Session 4-2 で書き出しはできるようになったが、タブが追従するのは Workspace の中だけ ── [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.9.3）

---

## 10. 実装ステップと現状

### STEP 1（完了）— 基盤構築

Electron + React + TypeScript のベース構築と、Main / Renderer / Preload / IPC の分離。

| セッション | 内容                                     |
| ---------- | ---------------------------------------- |
| 1-1        | プロジェクト初期セットアップ、3層の分離  |
| 1-2        | IPC 基盤（登録基盤・エラーの正規化）     |
| 1-3        | IPC 契約レイヤー（shared）と結果型       |
| 1-4        | セキュリティ設定と起動確認               |
| 1-5        | 全体整理・品質基盤・アプリシェルの仕上げ |

STEP 1 の成果物は「機能を載せるための土台」であり、パネル UI・エディタ・Terminal などの機能は含まない。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。

### Session 1-4 からの持ち越し項目の判断

| 項目                           | 判断                    | 理由                                                                                                                                  |
| ------------------------------ | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| ウィンドウ状態の保存 / 復元    | STEP 1 で対応           | 常時起動して使う道具の体感品質に直結する。レイアウトプリセットが再利用する永続化層をここで用意できる                                  |
| 単一インスタンス制御           | STEP 1 で対応           | 保存ファイルの同時書き込みを防ぐ。Terminal / LSP / DAP がプロセスを持つ前に前提を固定する必要がある                                   |
| 権限要求の既定拒否             | STEP 1 で対応           | セキュリティの既定値であり、後から足すと「いつから安全か」が曖昧になる                                                                |
| アプリメニュー整理             | STEP 1 で対応（最小限） | 配布ビルドから開発者向け機能を外す。本格的な操作面はアプリ内 UI に置く方針のため、メニュー自体は育てない                              |
| ログ基盤                       | STEP 1 で対応（最小限） | 出力先を一本化しておかないと、Terminal / LSP / DAP が増えた時点で追えなくなる                                                         |
| CSP の運用整理                 | STEP 1 で対応           | ポリシーの明文化と、dev / 配布ビルドでの適用範囲の違いの確認                                                                          |
| Format / Test 基盤             | STEP 1 で対応           | コードが増える前に入れるほど安い。Prettier + Vitest                                                                                   |
| Lint（ESLint）                 | 見送り                  | TypeScript 7 に `typescript-eslint` が未対応で、`.ts` を解析できない。対応後に再検討（[docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §3） |
| electron-builder による exe 化 | 準備のみ                | 実際のパッケージングはアイコン・appId・署名方針など決めるべきことが多く、独立した STEP として扱う（§7.1）                             |

### STEP 2（完了）— Dockable Workspace 基盤

Dockable UI の土台。**パネルの中身（Files / Editor / Terminal / Git の本機能）は STEP 3 の担当**で、STEP 2 の対象は「どのパネルでも同じように置ける器」を作りきることだった。

| セッション | 内容                                                   |
| ---------- | ------------------------------------------------------ |
| 2-1        | Workspace Shell（レイアウトの器・Panel Registry）※完了 |
| 2-2        | Dock / Split のデータモデルと基本操作 ※完了            |
| 2-3        | パネルのドラッグ&ドロップ ※完了                        |
| 2-4        | Split 境界のリサイズ ※完了                             |
| 2-5        | パネルの表示管理・レイアウトプリセットの基盤 ※完了     |
| 2-6        | レイアウトの保存 / 復元（永続化）※完了                 |
| 2-7        | STEP 2 全体の統合テスト・品質整理 ※完了                |

Session 2-1 で画面の骨格を、Session 2-2 で Dock / Split をレイアウトデータの操作として実装し、Session 2-3 でそれをタブのドラッグ&ドロップにつないだ。Session 2-4 で領域の境界をドラッグして大きさを変えられるようにし、Session 2-5 でパネルを閉じる / 戻す操作とレイアウトプリセットの器を入れた。パネルを掴んで別の領域へ移す・上下左右へ落として分割する・境界を掴んで好みの大きさにする・要らないパネルを閉じて必要なときに戻す、という §3 の「自由な組み替え」がマウスだけで行えるようになっている。Session 2-6 でその結果を保存し、次回起動時に同じ配置で立ち上がるようにした。

Session 2-7 では新しい機能を足さず、**Dock → Drag → Resize → Close → Restore → Save → Restart → Restore** という実際の利用フローを1本の流れとして検証し、保存データが壊れている場合の振る舞いを一通り確かめたうえで、レイアウト状態を保持する層の置き場所を整理した（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.2）。構造とデータモデルは同 §7、確認の内訳は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §4。

配置は操作のたびに自動保存される（明示的な保存操作は無い）。保存先は `%APPDATA%/Fluvix Nexus/workspace-layout.json` で、Renderer はファイルに触れず IPC 経由で Main に預ける。保存データにはバージョン番号を持たせ、読めない・古すぎる・壊れている場合は Default の配置で起動する（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.8）。

閉じたパネルは定義としては残り、View メニューからいつでも戻せる。戻す位置は適用中のプリセットを設計図にして決めるため、Default 以外のプリセットを足せば「そのプリセットでの定位置」へ戻るようになる（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.7）。

領域の最小サイズは値をコンポーネントに書かず `layout/constraints.ts` 1箇所で決めている。Editor のように「置けるだけでは足りず、実際に操作できる広さが要る」パネルはそこで個別に底上げする（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.6）。

STEP 2 の時点で足りていなかった **Main → Renderer のイベント経路**は、Session 3-3 で既存の IPC 契約と対になる形で追加した（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §3.3）。Terminal の出力・LSP / DAP の通知も同じ経路に載る。

### STEP 3（進行中）— 各パネルの本機能

STEP 2 の Workspace 基盤に載せる形で、パネルの中身を実装していく。

| セッション | 内容                                                                       |
| ---------- | -------------------------------------------------------------------------- |
| 3-1        | Workspace / プロジェクトフォルダの基盤 ※完了                               |
| 3-2        | Files のファイルツリー基盤 ※完了                                           |
| 3-3        | Files 基本操作・Editor 連携基盤 ※完了                                      |
| 3-4        | Monaco Editor 本実装 ※完了                                                 |
| 3-5        | Editor 実用機能・外部変更監視 ※完了                                        |
| 3-5.1      | Files 既知不具合の修正 ※完了                                               |
| 3-5.2      | Delete のエラー分類 ※完了                                                  |
| 3-6-1      | Files のファイル移動 ※完了                                                 |
| 3-6-2      | Files のコピー / ペースト ※完了                                            |
| 3-6-3      | Files のドラッグ&ドロップ ※完了                                            |
| 3-6-4      | プロジェクト全体検索① 走査 + ファイル名 ※完了                              |
| 3-6-5      | プロジェクト全体検索② 全文検索 ※完了                                       |
| 3-6-6      | Files のファイル種類別アイコン ※完了                                       |
| 3-6-7      | Files の横長時のカラム表示 ※完了                                           |
| 3-6-8      | Files の仕上げ（見え方の保存・カラムの幅・自動スクロール）※完了            |
| 3-7-1      | Terminal の基盤（PTY 1本・xterm.js での描画）※完了                         |
| 3-7-2      | Terminal の複数タブとシェルの選択 ※完了                                    |
| 3-7-3      | Terminal の表示・リサイズ基盤（cwd の扱い・文字の大きさ）※完了             |
| 3-7-4      | 実行中のプロセスを終わらせる前の確認 ※完了                                 |
| 3-7-5      | Terminal の表示設定と永続化（文字の大きさ・行数）※完了                     |
| 3-8-1      | Git の実行基盤とリポジトリ検出 ※完了                                       |
| 3-8-2      | Git パネルの変更ファイル一覧 ※完了                                         |
| 3-8-3      | Git の Stage / Unstage ※完了                                               |
| 3-8-4      | Git の Commit ※完了                                                        |
| 3-8-5      | Git の Push / Pull / Commit & Push ※完了                                   |
| 3-8-6      | Git のブランチ（一覧・切り替え・作成）※完了                                |
| 3-8-7      | Git 基本機能の統合確認と仕上げ ※完了                                       |
| 3-8-8      | Git の `.git` 監視と自動追従 ※完了                                         |
| 3-8-9      | Git の Diff 表示と破棄 ※完了                                               |
| 3-8-10     | `git init` と「GitHub に公開」※完了                                        |
| 3-8-11     | Git のコミット履歴 ※完了                                                   |
| 3-8-12     | Git のコミット詳細と差分 ※完了                                             |
| 3-8-13     | Git の履歴からブランチを作る ※完了                                         |
| 3-8-14     | Git のブランチの削除 / rename ※完了                                        |
| 3-8-15     | Git の退避（stash）※完了                                                   |
| 3-8-16     | Git の remote 管理（一覧・追加・削除）※完了                                |
| 3-8-17     | Git の remote の URL 変更 / rename ※完了                                   |
| 3-8-18     | Git の競合の解決（解決済みにする）※完了                                    |
| 3-8-19     | Git の remote-tracking branch から手元にブランチを作る ※完了               |
| 3-8-20     | Git のマージの開始 / 中止 ※完了                                            |
| 3-8-21     | Git の競合の ours / theirs の差分 ※完了                                    |
| 3-8-22A    | Git の仕上げ（Fetch・途中の操作の検出と禁止・マージの既定メッセージ）※完了 |
| 3-8-22B    | STEP 3 Git 全体の production app 統合確認 ※完了                            |

Session 3-1 では、パネルの中身に入る前に**全パネルが共通の対象にするもの**を用意した。フォルダを開く・閉じる・前回のフォルダで起動する、という §3「Workspace」の振る舞いがこれにあたる。Files / Editor / Terminal / Git は、どのフォルダを対象に動くかをここから読む（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §8）。

開いているフォルダの正本は Main 側にある。ファイルを読む・シェルを起動する・Git を叩くといった処理はすべて Main で起きるため、そこが「今どのフォルダか」を持っていないと、Renderer から届いたパスを信じて OS を触ることになる。Renderer が持つのは表示用の写しで、切り替えは必ず IPC を通る。開く対象のパスを Renderer から渡せないことが、この API の限定の実体になっている（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §8.4）。

保存先は `%APPDATA%/Fluvix Nexus/workspace-folder.json` で、レイアウトとは別ファイル・別 API にしてある（用途ごとに API を切る方針。[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §5）。保存されたフォルダが消えていても落ちず、未選択の状態で起動して理由を Welcome に出す。

Session 3-2 では、その Workspace を基点に **Files パネルの中身**を実装した。フォルダを開くとルートが出て、フォルダを展開すると1階層ずつ中身が読まれる、という §3 の Files の土台にあたる（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §9）。

Renderer は fs にも絶対パスにも触れない。Renderer が言えるのは「今の Workspace の中の、この相対位置」までで、root を指定する手段はどこにも無い。`..`・絶対パス・symlink による Workspace 外への脱出は Main が2段階（パス文字列 → realpath による実体）で拒否する（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §9.3）。汎用のファイル API を公開しない方針（§5）は、root を渡せる API が1つあればそこで迂回されるため、Files 側でも同じ線を引いている。

**開いた瞬間に全体を読まない。** 読むのは常にフォルダ1階層で、展開したときに初めてそのフォルダを読む。`node_modules` を持つプロジェクトでも起動が重くならないのはこのため（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §9.4）。フォルダが読めない場合（消えた・権限が無い）もアプリは落とさず、Files パネルの中の1行として扱って、その場で再試行できるようにしてある。

Session 3-3 では、その Files を**閲覧できるツリーから、開発作業に使える入口**へ進めた。ファイルを選ぶと Editor に開き、Files から作る・名前を変える・消せるようになっている（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10）。

**Workspace の外に手が届かないという性質は、書き換える側にも同じだけ効かせている。** 読む側だけを守っても、作成 / 改名 / 削除から外へ出られれば意味が無い。作成・改名・削除はどれも「あるフォルダの中で起きる」操作なので、確かめるのは**親フォルダ**で、触るのはその実体（realpath）の直下だけに限る（§10.2）。名前に区切り文字を含められないことが、その「直下だけ」の担保になっている。

**削除は OS のごみ箱へ送るだけ**で、完全削除の経路は公開していない。UI にも確認を入れているが、UI は迂回されうる以上、戻せない削除が Renderer からの要求で起きる状態そのものを作らない。ごみ箱へ送れない場所（ネットワークドライブなど）では失敗として返し、完全削除に切り替えることはしない。

Editor 側は**タブの状態を独立したモデルとして**用意した（§10.3）。同じファイルを2枚開かないこと、リネームされてもタブが同じタブであり続けること、フォルダごと消えたら閉じることまでを、この層が決める。Monaco Editor 本体は後続で、差し替わるのは中身を描く1ファイルだけになる。

Session 3-4 では、その1ファイルを実際に **Monaco Editor** へ差し替えた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §11）。ここで §4「コードエディタ」のうち、シンタックスハイライト・行番号・オートインデント・複数タブ・検索と置換・コメント切替・基本ショートカット・そして**編集と保存**が動くようになっている。残るコード補完・エラー表示・Go to Definition・Rename・フォーマットは、STEP 5 で LSP と一緒に入った（§12）。

**Monaco のために Files や Workspace を作り直していない。** Files でファイルを選ぶ → 中身を読む → タブが持つ → Editor が出す、という Session 3-3 の経路はそのままで、最後の「出す」だけが変わった。保存はその経路に同じ作法で1本足したもので、Renderer が言えるのは相変わらず「Workspace の中の、この相対位置」までに留まる。

**STEP 1 で決めた CSP を1文字も緩めていない。** Monaco は既定では Worker の起動用スクリプトを `blob:` として組み立てるため、そのまま入れると `worker-src 'self'` では動かない。ここは CSP を広げるのではなく、Monaco 側にその経路を通らせない形（`MonacoEnvironment.getWorker`）を選び、**アプリにバンドルされた Worker だけ**を返すようにした（§11.2）。contextIsolation・sandbox・Renderer への Node 非公開も含め、前提はすべて Session 3-3 のまま。

編集の中身を持つのは **Monaco の Model** で、タブとは別の層にある（§11.5）。タブの同一性は発番した id、中身の同一性は Workspace 内の位置、と基準が違うため。エディタは1つだけ作り、タブを切り替えたら Model を差し替える。この形にすると編集内容と Undo 履歴が Model 側に付くので、**タブを切り替えても、Editor パネルを閉じて開き直しても、編集が消えない**。

保存では**無条件の上書きをしない**（§11.6）。読んだ時点の mtime とサイズを覚えておき、保存の直前に食い違っていれば書かずに返す。Reload / Compare を並べた Conflict UI は後続だが、「気づけないまま上書きする」構造にしてしまうと後から選択肢を差し込む場所が無くなるため、検出の入口だけを先に置いた。

Auto Save は **必ず OFF で始まる**（§11.7）。自動保存は利用者が明示的に選ぶものにする ── 既定で入れると、開いて眺めていたつもりの操作がディスクに残る。設定の形は将来 Settings から `onFocusChange` / `onWindowChange` を足せるようにしてあり、Session 3-4 で動くのは `off` と `afterDelay` の2つ。

Session 3-5 では、その Editor を**実際の開発で1日使える**ところまで進めた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12）。§4 の「1日普通に開発してストレスが出ない品質」に向けて足りていなかったのは機能の数ではなく、**アプリの外で起きることへの備え**だった。

ビルドツールがファイルを書き換える。Git で checkout する。別のエディタで直す。どれも開発中に普通に起きることで、それに気づけないエディタは「開いたまま作業を続けられない」。そこで Workspace の中を Main 側で見張り、変化を Session 3-3 から使っている `files:changed` に載せた（§12.1）。**Renderer に filesystem の監視 API は渡していない** ── 監視するのは Main で、届くのは相変わらず「Workspace の中の、この相対位置」だけ。

**未保存でなければ黙って取り込み、未保存なら止まる**（§12.3）。失うものが無いなら選択肢を出す意味が無く、失うものがあるなら**アプリが片方を選んではいけない**。どちらが要るかを知っているのは利用者だけで、しかも間違えると取り返せない。止まったときに出すのが Reload / Compare / 上書き の3つで、Compare は Monaco の Diff Editor をそのまま使っている。**押す前に何を失うかが分かる**ようにしてあるのが要点で、そうしないと Compare を挟む意味が薄れる。

**未保存の内容を失う操作は4つあり、確認は1本にまとめた**（§12.6）。タブを閉じる・Workspace を閉じる・Workspace を切り替える・アプリを終了する。それぞれの入口に確認を書くと、入口が増えるたびに保護が抜ける。「失われるものを持っている側（Editor）」と「失わせる操作をする側」を分け、間を1つの器でつないである。実際、Session 3-7-4 で Terminal の「実行中のプロセスがある」が加わったときに増えたのは、申告する側と文面の分岐だけになる。

**終了時も同じ経路を通る**（§12.7）。「閉じるか」を決めるのは Main、「未保存があるか」を知っているのは Renderer という分担を崩さず、閉じる操作を Main が握ったまま判断だけを尋ねる形にした。ウィンドウの × も Alt+F4 も `app.quit()` も同じ 'close' を通るため、終了処理と保存確認が別々に走って競合することがない。ただし **OS のシャットダウンや強制終了では保証できない** ── 原理的にできないことなので、そこは Auto Save を選べるようにすることで補う。

Auto Save は4方式すべてが動くようになり、設定はアプリ再起動後も保たれる（§12.4）。保存先は `%APPDATA%/Fluvix Nexus/editor-settings.json` で、レイアウト・Workspace と同じくプロジェクトフォルダには何も書かない（Session 4-3A で `settings.json` の `editor` section へ移した。§12.4）。Settings 画面（§9）はまだ無いが、**設定の値そのものは既にアプリの設定として保存されている**ため、画面が入っても保存形式は変わらない。

文字コードは **UTF-8 前提であることを明確にした上で、BOM の有無だけは保つ**（§12.5）。Windows のツールが付ける BOM を落としたまま保存すると、開いて保存しただけで差分が出る。Shift_JIS / UTF-16 へ広げるときに増えるのは「バイト列 ↔ 文字列」の2箇所だけで、Editor の経路は変わらない形にしてある。変換表を持つ大きなライブラリは、実際に必要になるまで入れない。

作成・改名・削除の後に**ツリー全体を読み直さない**（§10.5）。読み直すのは変わった親フォルダ1つだけで、Session 3-2 の Lazy Load はそのまま保たれる。その伝達に使っているのが、STEP 1 から持ち越していた **Main → Renderer のイベント経路**（§3.3）。操作した側（Files）と、知る必要がある側（Files のツリーと Editor のタブ）が違うため、応答ではなくイベントで配る形にした。Session 3-5 のファイル変更監視は**この経路にそのまま載った**（受け手は1つも変えていない）。Terminal の出力・Git の状態変化・LSP / DAP も同じ経路に載る。

**STEP 2 の時点では意図的に手を付けていない項目**は以下のとおり（Files は Session 3-2 / 3-3、Editor は Session 3-3 / 3-4 で着手済み）。

| 項目                                             | 補足                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Files 本機能                                     | ツリー・作成 / 削除 / リネーム / 移動 / コピー・ドラッグ&ドロップ・外部変更の追従・ファイル名検索・全文検索・種類別アイコン・**横長時のカラム表示**は実装済み                                                                                                                                                                                                                                                                                                                  |
| Monaco Editor 本機能                             | **Session 3-4 で実装済み**（バンドル済み Worker で `worker-src 'self'` を維持）                                                                                                                                                                                                                                                                                                                                                                                                |
| ファイルの編集・保存                             | **Session 3-4 で実装済み**（`files:write-file`・未保存の印・Auto Save）                                                                                                                                                                                                                                                                                                                                                                                                        |
| 外部変更の監視                                   | **Session 3-5 で実装済み**（Main で監視し、`files:changed` に載せる）                                                                                                                                                                                                                                                                                                                                                                                                          |
| 外部変更との競合（Reload / Compare / Overwrite） | **Session 3-5 で実装済み**（Compare は Monaco の Diff Editor）                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 未保存の保護（タブ / Workspace / 終了）          | **Session 3-5 で実装済み**（確認は1本にまとめてある）。実行中のターミナルも同じ器で守る（Session 3-7-4）                                                                                                                                                                                                                                                                                                                                                                       |
| Settings（設定の永続化）                         | **Session 3-5 で実装済み**（`settings:*`）。Session 4-3A で `settings.json` 1つ・section 分けへ集約（§12.4）。設定を並べる**画面**は §9 として後続                                                                                                                                                                                                                                                                                                                             |
| Terminal 本機能                                  | **Session 3-7-1 / 3-7-2 / 3-7-3 / 3-7-4 / 3-7-5 で実装済み**（node-pty は Main 側・出力は Session 3-3 のイベント経路・複数タブとシェルの選択は 3-7-2・大きさと文字の扱いは 3-7-3・終わらせる前の確認は 3-7-4・見え方の設定と永続化は 3-7-5）                                                                                                                                                                                                                                   |
| Git / GitHub 本機能                              | **Session 3-8-1 〜 3-8-22A で実装済み・3-8-22B で production app 確認済み**（検出・変更一覧・Stage / Unstage・Commit・Push / Pull / Fetch・ブランチ・`.git` の監視・差分と破棄・`git init` と「GitHub に公開」・コミット履歴・コミットの詳細と差分・履歴からのブランチ作成・ブランチの削除 / rename・退避・remote の管理と URL 変更 / rename・競合の解決・remote の枝から手元にブランチを作る・マージの開始 / 中止・競合の ours / theirs の差分・途中の Git 操作の検出と禁止） |
| ブランチ操作 UI                                  | **Session 3-8-6 / 3-8-13 / 3-8-14 / 3-8-19 / 3-8-20 で実装済み**（バーのブランチ名から開く面で一覧・切り替え・作成・削除・rename・マージ、履歴の行から始点を指す欄、remote の枝から作る畳んだ段。ローカルと remote-tracking は**別の一覧**。マージ中はパネルの帯から中止できる）                                                                                                                                                                                               |
| LSP                                              | §4。JavaScript / TypeScript・Python・C#。SDK は同梱せず PC の環境を検出する                                                                                                                                                                                                                                                                                                                                                                                                    |
| DAP                                              | §5 / §13。**STEP 6**。Breakpoint / Step / Variables / Call Stack。Session 6-0 で設計確定、6-1 で DAP wire / adapter process / catalog foundation、6-2 で Debug Session lifecycle、6-3 で Breakpoint を実装                                                                                                                                                                                                                                                                     |
| パネルの独立ウィンドウ化                         | §3。Main 側で別ウィンドウを開き、Shell のルートを分岐する                                                                                                                                                                                                                                                                                                                                                                                                                      |
| タブのドラッグによる並べ替え                     | レイアウト操作としては実装済みで、UI の入口だけが無い（ARCHITECTURE.md §7.9）                                                                                                                                                                                                                                                                                                                                                                                                  |
| レイアウトプリセットの追加・自作レイアウトの保存 | Coding / Debug などの表への追加と、名前を付けた保存（§3）                                                                                                                                                                                                                                                                                                                                                                                                                      |

STEP 2 の基盤に対しては、これらのために先回りした実装を入れない。パネルは Registry に登録された中身を描くだけで、配置・大きさ・可視状態は Workspace 側が持つ、という分担を保つ。

Session 3-5.1 では、Session 3-5 の作業中に見つかっていた **Files の不具合3件**だけを直した（新しい機能は足していない）。3件とも「規則そのものは正しいのに、当てる相手を間違えていた」という同じ形をしている。

**利用者が指した名前と、検証のための文字列を混ぜない。** 相対位置を丸ごと trim していたため、`notes.txt ` を指した要求が `notes.txt` を指す要求に化けていた。trim は判定にだけ使い、fs へ渡す名前は生のまま通す（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.2）。

**「これから作れる名前か」と「既に在るものを指せる名前か」は別の問い。** 新規作成向けの規則を削除にも当てていたため、他の OS で作られた `aux.ts` が**ツリーには並ぶのに消せない**状態になっていた。守るものが何も無いのに手が出せないものが残るのは、名前の規則が守ろうとしていたものとは無関係な不便でしかない。境界の検証（Workspace の外へ出ないこと・symlink の扱い・触る場所を親の realpath 直下に限ること）は緩めていない。

**「確定した」と「送信している」も別。** リネームの同名衝突で失敗した後、入力欄が確定済みとして固まり、打ち直すことも取り消すこともできなくなっていた。二重送信を抑えるのは応答を待っている区間だけで、失敗したら必ず打ち直せる状態へ戻す（§10.4）。

修正の過程で、**Windows のシェル削除 API が末尾の空白 / ドットを落とす**ことも分かった（`notes.txt ` を渡すと隣の `notes.txt` がごみ箱へ入る）。指したものと違うものが消える方が、消せないことより悪いので、宛先を伝えられない名前は渡さずに断る。改名は `fs.rename` を使うので字義どおり動くため、**改名してからなら消せる**（§10.2）。

Session 3-6-1 では **Files のファイル移動**を足した（`files:move`）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.7。

改名（`files:rename`）に相乗りさせず別のチャンネルにしたのは、**移動が2つの場所を指す操作**だからにほかならない。改名は「同じフォルダの中で名前を変える」に閉じており（新しい名前に区切り文字を含められない）、境界を確かめる相手は1つしかない。そこへ移動を混ぜると、片方だけを通した経路から Workspace の外へ手が届く。分けたことで、Session 3-2 / 3-3 で決めた検証の手順（相対位置の検証 → 親の realpath → 境界 → その直下だけを触る）を**元と先の両方に**そのまま通せている。

**移動にしか無い失敗が1つだけある。** 移動先が動かすもの自身か、その中（`src` を `src/lib` へ）。パスの形も名前も正しいので、fs に任せると OS の「引数が変」で返り、利用者に何を直せばよいかを伝えられない。判断できる側で判断し、理由を添えて断る。

変化の伝え方は変えていない。移動は改名と同じ `renamed` として `files:changed` に載り、**受け手（Files のツリー・Editor のタブ）は1行も変えていない** ── 「位置が変わった」から読み直す相手を導く形にしてあったため、改名では1つのフォルダに、移動では2つのフォルダに、同じ規則のまま落ちる。Session 3-3 のイベント経路が、また1つ機能を受け止めた形になる。

UI は「動かすものを決める → 行き先のフォルダを右クリックする」の2手。行き先を選ぶダイアログを別に作っていないのは、**行き先を選ぶのに一番向いた道具が目の前のツリーそのもの**であるため。

Session 3-6-2 では **Files のコピー / ペースト**を足した（`files:copy`）。対象は単一のファイル / フォルダで、詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.8。

境界の確かめ方は移動と**まったく同じものを使い回している** ── 元とコピー先の両方に同じ手順を通す。移動でその形にしておいたので、コピーのために境界の検証は1行も増えていない。

**同名でも断らず、上書きもしない。** `example.txt` → `example copy.txt` → `example copy 2.txt` と名前を変えて作る。作成・改名・移動では「同じ名前があります」と断っているのに、コピーだけ訊き直さずに済ませているのは、複製が「同じものをもう1つ」という操作で**名前そのものに意味が無い**ため。名前の規則は Main と Renderer の両方が同じ答えを見る必要があるので、他の名前の規則と同じく shared に置いた。

**リンク（symlink / ジャンクション）は辿らない。** 辿れば Workspace の外にある実体の中身を中へ持ち込めてしまい、「外は読まない」という線がここで抜ける。フォルダの中にあるリンクはとばして残りを複製し、**とばした件数を画面に出す**（複製したつもりで中身が欠けている方が、断られるより悪い）。リンクそのものを複製しようとした場合は断る ── 削除・移動がリンクに対して通る（動かすのは入口だけ）のと矛盾しない。違うのは、コピーだけが中身を持ってくる操作である点。

フォルダの再帰コピーは1回の操作では終わらないため、途中で失敗しうる。そのとき**作りかけを消しに行かない** ── 消す手段（完全削除）を持たない方針を、アプリ自身が作ったものに対しても曲げない。残ったものはツリーに現れ、「途中で止まって作りかけが残っている」ことを文言で伝える。

変化の伝え方は今回も変えていない。コピーは `created` 1件として `files:changed` に載り、フォルダの中身までは配らない（展開されていなければ読まれておらず、展開されていれば既存の読み込み経路が拾う）。**コピー専用の更新経路を1つも作っていない。**

UI は移動と同じ2手（右クリック →「コピー」→ 貼り付け先を右クリック →「ここに貼り付け」）。OS のクリップボードは使わない。控えは Renderer の中だけに持ち、`{ mode: 'copy' | 'cut', entry }` という形にしてある ── `cut` は器だけ用意して**まだ UI へ出していない。** cut は結局のところ移動であり、移動には既に入口がある。同じ操作への入口を2つ並べる前に、どちらを主にするかを Session 3-6-3 のドラッグ&ドロップと合わせて決めた（下記）。

Session 3-6-3 では **Files パネルの中のドラッグ&ドロップ**を足した。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.9。

**新しいファイル操作は1つも作っていない。** 足したのは、Session 3-6-1 の移動と Session 3-6-2 のコピーに対する**2つめの入口**だけ ── 行を掴んでフォルダへ落とせば移動、Ctrl を押しながらならコピーになる（Windows のエクスプローラと同じ約束）。落とせるかの判定も、右クリックの2手が使っている関数をそのまま呼んでいる。同じ規則をドラッグ側に書き直すと、右クリックからは断られる操作がドラッグからは通る、という食い違いが生まれる。

**禁止の最終的な正本は Main のまま。** 自分自身・自分の子孫・同じフォルダへの移動は画面に出ている位置だけで判断できるのでその場で断るが、ジャンクションを挟んだ行き先が実は Workspace の外だった、という形は Renderer からは見抜けない（realpath を持たない）。そこは Main が断る。実際、ツリー上はフォルダに見えるジャンクションへのドロップは、案内が出たうえで Main に拒否される ── Renderer の判定は「できない操作を見せない」ためのものであって、許可の根拠ではない。

**案内のために要素を1つも重ねていない。** 落とせるフォルダ行と、Workspace root にあたるツリーの余白に色を付けるだけで、上に別の要素を敷かない。敷くとそれがカーソルの下に入り、どの行を指しているかを判定できなくなる（カーソルに付く小さな表示だけは重なるので、ポインタイベントを受けないようにしてある）。落とせない場所では、落とせるように見える表示を出さない。

**cut は UI へ出さないことに決めた。** 移動の主な入口はドラッグ&ドロップになり、右クリックの「移動…」は行き先が画面に出ていないときの道として残る。ここへ「切り取り → 貼り付け」を足すと、同じ操作への入口が3つになり、3つめは既にある2手と手数も結果も同じものになる。器（`mode: 'cut'`）は残す ── エクスプローラとの受け渡しを入れるときに要る形で、それは Main 側の別の経路の話になる。

Session 3-6-4 では **プロジェクト全体検索の基盤とファイル名検索**を足した（`files:search`）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.10。

§4 の一覧にある「プロジェクト全体検索」のうち、**名前で探す**ところまで。ファイルの中身に対する全文検索は Session 3-6-5 で、この走査・上限・取り消しの上に載せた（下記）。

**走査は Main が持つ。** Renderer から列挙（`files:read-directory`）を繰り返して自前で再帰する形にしなかったのは、そうすると Lazy Load のために1階層ずつにした経路が、検索という別の目的で全階層を舐める経路に化けるため。Renderer が言えるのは検索語までで、返るのも相対位置を持つ `FileEntry` だけになる（絶対パスは相変わらず持たない）。

**Workspace の外へは出ない。** 起点は realpath まで解決した root ひとつで、そこから先は readdir が返した名前を継ぎ足して降りるだけ ── 外から相対位置を受け取らないため、検証すべき入力そのものが無い形にしてある。リンク（symlink / ジャンクション）の中へは潜らない（コピーが辿らないのと同じ線）。リンクそのものは、そこに在るものとして1件出す。

**無制限に列挙しない。** 件数（500）・深さ（12 段）・走査数（50,000 件）・時間（4 秒）の4つで打ち切る。件数だけでは足りない ── 一致が0件でも数十万件を舐めうるため、走査数と時間は「見つからないときのため」の上限にあたる。打ち切りは失敗ではなく「ここまでしか見ていない」という事実として、理由ごとに違う文言で伝える。`.git` / `node_modules` を外す規則は**外部変更の監視と共有した**（2箇所に書くと、`.git` の中の変更は届かないのに検索結果には出る、という食い違いが生まれる）。

**走るのは常に1本。** 新しい検索を始めると古い検索は Main 側で必ず止まり、利用者からも止められる。Workspace の切り替え / Close でも進行中のものを捨てる。取り消しは失敗ではなく、**そこまでに見つけたものと一緒に返る**。

**パネルは増やしていない。** 検索は Files パネルの中のもう1つの見せ方で、ツールバーの虫めがねで切り替え、左上の `<` でツリーへ戻る。切り替えでどちらの状態も捨てないため、ツリーの展開状態も検索結果も行き来で失われない。結果を押すと**ツリーの行を押したときと同じ経路**で Editor が開き（検索専用の「開く」を作っていない）、その場所はツリー側でも祖先が開いて選択される。

検索中・0 件・取り消し・打ち切り・失敗は必ず言い分ける。利用者の次の一手（待つ / 語を変える / 絞り込む / もう一度試す）がそれぞれ違うため、1つの「結果がありません」にまとめない。

Session 3-6-5 では **ファイル本文の全文検索**を足した（`files:search-content`）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.11。

これで §4 の一覧にある「プロジェクト全体検索」が、名前と中身の両方で行えるようになった。**Session 3-6-4 の経路は1つも変えていない** ── 全文検索は別のチャンネルとして足し、共有したのは値と純粋な関数（除外の規則・深さと走査数の上限・検索語の規則）と、走っている検索の管理だけになる。

**中身を探すのは Main。** Renderer へ渡るのは相対位置・行・桁と、一致の周りを切り出したテキストだけで、ファイルの中身そのものは運ばれない（絶対パスも相変わらず持たない）。読むのはテキストだけで、バイナリ・大きすぎるファイル・`.git` / `node_modules`・**Workspace の外を指すリンク**は読まずにとばす。とくにリンクを読まないのは、読めば外にある実体の中身が結果に載るため。

**「Editor で開けるものを探す」に揃えた。** 1ファイルの大きさの上限を Editor と同じ 2 MB にしてあるので、見つかったものは必ず開けるし、開けるものは必ず探される。別の値にすると、そのどちらかが必ず食い違う。

**上限は6つ**（一致の総数 1,000 / 1ファイル 50 件 / 読むファイル 2,000 件 / 1ファイル 2 MB / 時間 6 秒 / 深さ・走査数は 3-6-4 と共有）。すべて失敗ではなく「ここまでしか見ていない」として、理由ごとに違う文言で伝える。1ファイルの中で打ち切った場合は、そのファイルの行に `50 件以上` と出る。

**走るのは合わせて常に1本。** 名前の検索と全文検索は同じ管理を通るため、モードを切り替えて検索し直した瞬間に前の検索が止まる。Workspace の切り替え / Close で捨てるのも同じ。

**入口は増やしていない。** Files パネルの検索モードの中が「ファイル名 | 全文」の切り替えになっただけで、パネルも View メニューの項目も増えない。行き来してもどちらの結果も消えない。結果は **フォルダ → ファイル → 一致** の3段で並び、`3:7 const example = 1` のように行・桁と周辺のテキストが読める。

**押すと Editor が該当位置へ飛ぶ。** 開く経路はツリー・名前検索と同じで、**既に開いているファイルならタブは増えず**、そのタブが手前に出て位置だけが動く。一致した範囲はそのまま選択されるので、飛んだ先で探し直さずに済む。

置換・正規表現・検索フィルター・Git 連携検索・AI 検索は**今回は実装していない**（構造としては足せる形にしてある）。

Session 3-6-6 では **Files のファイル種類別アイコン**を足した。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.12。

**機能は1つも増えていない。** 変わったのは一覧の見え方だけで、IPC も契約もデータ構造も1つも動いていない ── 種類は名前から Renderer 側で分かるものなので、`FileEntry` にアイコンの欄を足さなかった。足せば「今ディスクにあるものの写し」だったはずの型が表示の都合を持ち始め、アイコンを1つ増やすたびに IPC の応答が変わる。

**判定は1箇所、絵はその隣。** 名前から種類を決めるのは `files/fileIcon.ts`（React にも DOM にも依存しない・テスト対象）で、SVG は `FileTreeIcons.tsx`、色は CSS が持つ。ツリー・ファイル名検索・全文検索の3つが同じ関数を呼ぶため、**同じファイルがツリーと検索結果で違う絵になることが構造的に起きない**。全文検索の結果は `FileEntry` を持たず名前しか無いので、入口は名前1つに揃えてある。

**表は2つに分け、名前の表が先に効く。** 拡張子で決まるもの（`.ts` / `.tsx` / `.js` / `.jsx` / JSON / HTML / CSS / Markdown / Python / C# / 画像 / テキスト）と、名前そのものに意味があるもの（`package.json` / `.gitignore` / `Dockerfile` / `README.md`）を混ぜると、`README.md` を「md の例外」として拡張子の表に書くことになる。`package.json` が JSON ではなくパッケージの定義として出るのは、この順番の結果にほかならない。種類を増やすときに足す場所も、どちらの表かで決まる。

**外部アセットを1つも増やしていない。** アイコンフォントも画像も使わず、その場で描く SVG のままで、CSP は1文字も緩めていない（Monaco のときと同じ線。§4）。大きさは種類によらず 14px に固定してあるので、行の高さもインデントも展開の三角の位置も変わらず、Rename / 移動 / コピー / ドラッグ&ドロップ / 検索の見た目と当たり判定はそのまま。

**色は補助であって、唯一の手掛かりにしない。** 種類はまず絵の形が示していて、色が主になるのは形が同じ組（`.tsx` / `.jsx`）だけ。テキストと種類が分からないファイルには色を付けない ── 全部に色が付くと色そのものが情報を持たなくなり、一覧が色の並びになる。フォルダは閉じている / 開いているで形が変わるが、フォルダ名ごとの専用アイコン（`src`・`node_modules`）は今回持たない。

Session 3-6-7 では **Files の横長時のカラム表示**を足した（§3「Files パネルの表示切り替え」）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.13。

**Files のデータは1つのまま。** カラム表示はツリーと**同じ `FileEntry` / `relativePath` / 読み込み済みの中身**を見る、もう1つの描き方でしかない。Main 側の API も、Files の状態も、1つも増やしていない ── 増えたのは「一番右のカラムが見せているフォルダ」1つだけで、列の並びはそこから導く（左のフォルダを選び直すと右の列が消えるのは、切り詰める処理ではなく導出の結果）。

**Lazy Load もそのまま。** カラムを開くことは*そのフォルダを展開すること*として表してあるので、読み込みは Session 3-2 からの1本の経路（展開されたのに中身を知らないフォルダを読む）に乗る。Workspace 全体の先読みは起きず、**ツリーとカラムで読み込み済みの中身が共有される**（切り替えても読み直さない）。

**「勝手に変わる」と「置いた場所に合う」を両立させる。** パネルの形は利用者がいつでも変えられるため、**形から決まる提案**と**利用者が選んだ方**を別の値として持つ。まだ選んでいない間は横長でカラム・縦長でツリーになり、一度選んだら以降リサイズでも置き場所を変えても変わらない。しきい値には遊びを持たせてあり、境界の上でパネルを掴んで動かしても表示方式が往復しない。

**選んだことはパネルより長く生きる。** パネルを Dock / Split で動かすと React の木の中で作り直されるため、選択をパネルの中に持つと*運んだだけで消える*。選択だけを Shell の外側に置いてある（パネルの形の観測はパネルの中のまま ── 場所が変われば測り直すのが正しい）。

**操作の入口は増やしていない。** Rename / Delete / 移動 / コピー / ドラッグ&ドロップ / ファイルを開く / 検索は、ツリーとまったく同じ関数を通る。カラムの余白へのドロップだけは行き先が変わり、**そのカラムのフォルダ**になる（一律 root にすると、指した場所と違うところへ動く）。

Session 3-6-8 では、Files に**新しい機能を1つも足さず**、Session 3-6-1 〜 3-6-7 で作ったものを使い続けたときに引っかかる3つを埋めた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.14）。この回で Files パネルはひと区切りになる。

**選んだ見え方が、次に開いたときにも残る。** 表示方式（ツリー / カラム）とカラムの幅がアプリを閉じると忘れられていた ── パネルの配置は Session 2-6 から保たれているのに、その中の見え方だけが戻る状態だった。保存先は `%APPDATA%/Fluvix Nexus/files-settings.json` で、**Editor の設定に相乗りさせず別のファイル・別のチャンネル**にしてある（用途ごとに API を切る方針。§5）── この形は Session 4-3A で `settings.json` の `files` section にまとめ直した（§12.4）。「パネルの形に任せる」を選び直したことも保存する ── 保存しないことでそれを表すと、一度カラムを選んだ人は二度と自動に戻れなくなる。

**カラムの幅を掴んで変えられる。** 変わるのは全部の列で、列ごとには持たない。これは好みの話ではなく、**列が導出だから**にほかならない（列は「今どこを開いているか」から毎回導かれ、選び直すたびに顔ぶれが変わる）。列ごとの幅を持たせても、その幅が「どの列のものだったか」を保てる場所がどこにも無い。中身に合わせて自動で伸ばさないのは Session 3-6-7 のままで、幅を変えるのは利用者だけ。

**ドラッグを縁で止めると、中身が流れる。** Session 3-6-3 の時点では、行き先が画面の外にあるといったん離して掴み直すしかなかった。ドラッグは「離すまでが1つの操作」なので、途中で離す必要があること自体が操作の形と食い違っている。**掴む・判定する・落とす経路は1つも変えていない** ── 足したのは「カーソルが縁にある間、その下の器を少しずつ動かす」ことだけで、落とせるかの判断も、落とした後に走る `files:move` / `files:copy` も Session 3-6-1 / 3-6-3 のまま。器の外では動かさない（そこはそもそも落とせない場所で、流すと隣のパネルへ運んだだけでツリーが動く）。

Session 3-7-1 では **Terminal パネルの基盤**を足した（§3「Terminal パネル」）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §13。

これで §6「Claude Code 連携」の**第一段階**（Terminal パネルから Claude Code を CLI としてそのまま利用する）が成立し、開いているフォルダを対象に**コマンドを実行できる**ようになった。Files で見て、Editor で直して、Terminal で走らせる、が1つのウィンドウの中で閉じる。

**Main が長命な子プロセスを持つ、最初の機能になる。** Files も Editor も、要求のたびに始まって応答で終わる処理だった。ターミナルは一度立てたら利用者が終わらせるまで生き続け、その間ずっと出力を出し、アプリが終わるときには確実に片付ける必要がある。この形は LSP（サーバ1本）と DAP（デバッグ対象）がそのまま踏襲するため、**ペイロードがただのテキストである Terminal で先に固めた。**

**境界を「プロセスの中身」ではなく「起動の入口」へ移した。** Files は「Workspace の外へ出られないこと」を、相対位置しか受け取らないことで担保していた。Terminal に同じ手は使えない ── 起動したシェルの中で `cd ..` を止めることに意味は無いし、止めれば道具として成立しない。**Terminal は定義上 Workspace の外へ出られる。**

そこで守る場所を1つ内側へずらしてある。`terminal:create` の要求に入っているのは**大きさだけ**で、実行ファイルの欄も作業ディレクトリの欄も無い。何を起動するかは Main が持つ表が、どこで起動するかは Main が持つ現在の Workspace が決める。`workspace-folder:open` に開く対象のパスを渡せないのと同じ形で、Renderer が言えるのは「ターミナルを1つ」までに留まる。入口さえ指定できなければ、「Renderer からの要求で任意の場所の任意の実行ファイルが動く」という形は作られない。

**PATH で解決しない。** `powershell.exe` とだけ書くと、解決に使われるのは起動時の PATH と**作業ディレクトリ**になる。作業ディレクトリは利用者が開いた Workspace で、その中に `powershell.exe` が置かれていることは十分にありうる（clone してきたリポジトリの中身は、この時点ではただのファイルでしかない）。「フォルダを開いただけ」が「そのフォルダの中の実行ファイルが動く」になっては困るので、`%SystemRoot%` から絶対パスを組み立てる。

**開いたターミナルは、素の PowerShell を開いたときと同じように振る舞う。** 何もしなければ、Electron が自分のために立てた環境変数（`ELECTRON_RUN_AS_NODE`・`NODE_OPTIONS`）をそのまま受け継ぐ。とくに前者を引き継ぐと、**そのターミナルから起動した Electron アプリが素の Node として立ち上がって落ちる** ── このアプリで Electron アプリを開発する利用者が最初に踏む類のもので、しかも落ちるのは相手のアプリなので原因に気づきにくい。落とすのはこの2つだけで、足すのは端末であることの申告（`TERM`）だけにしてある。

**出力は束ねるが、間引かない。** ファイル変更の通知（`files:changed`）は畳んでよく、多すぎれば捨ててよかった ── 受け手は結局そのフォルダを読み直すので、正しい状態に追いつける。ターミナルの出力にその性質は無い。1回きりで、順番に意味があり、落とした分を後から取り戻す手段が無い。そこで上限は「捨てる基準」ではなく「**待たずに送る基準**」として置いた。Session 3-3 のイベント経路が、**取りこぼしの許されない最初の用途**を受け止めた形になる。

**セッションと画面は、パネルより長く生きる。** パネルは閉じられるし、Dock で動かせば React から見て作り直される。シェルのプロセスは Main が持っているので自然に守られるが、**画面（xterm とその DOM）は Renderer 側にある** ── パネルの中に持つと、別の場所へ運んだだけでスクロールバックが白紙に戻る。これは Editor の Model がぶつかったのとまったく同じ問題なので、置き場所も同じ（Workspace Shell の外側）にした。実際、パネルを動かしても閉じて開き直しても、シェルは動き続け、それまでの出力も残る。

**CSP は1文字も緩めていない。** xterm は既定の描画で Worker も blob も使わないため、Monaco のときのような迂回すら要らなかった。CSS もアプリにバンドルしている。xterm 本体は Monaco と同じく遅延読み込みで、Terminal パネルを開くまで読み込まれない。

**アプリの終了時には必ず片付ける。** ウィンドウが閉じても消えるのは Renderer だけで、プロセスは Main の持ち物のまま残る。片付けないと、ターミナルで立てた dev server がアプリの終了後もポートを掴み続ける。Session 3-7-1 の時点では**黙って終わらせていた** ── 実行中のプロセスがあることを尋ねるのは Session 3-7-4 で足してある（下記）。

Session 3-7-1 の時点では **Workspace を切り替えたときにも終わらせていた**（Editor が切り替えでタブを捨てるのに揃えていた）。これは Session 3-7-3 で改めてある（下記）。

見え方の設定とその永続化は**この時点では持っていない**（Session 3-7-5 で足した ── 下記）。複数タブとシェルの選択は Session 3-7-2 で足した（下記）。

Session 3-7-2 では、その Terminal を**複数のタブと、開くシェルの選択**へ広げた（§3「Terminal パネル」の残り）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §13.2 / §13.6.1。

これで §6「Claude Code 連携」の第一段階が実際に使える形になる ── **PowerShell を1本開いたまま、隣のタブで Claude Code を動かせる。** Session 3-7-1 の時点では1本しか開けず、Claude Code を使うにはそのターミナルを明け渡すしかなかった。

**選ばせる入口を作ることと、対象を渡せるようにすることは別の話にした。** Renderer から届くようになったのは「Main が持つ表の**どの行か**」を指す値1つだけで、実行ファイルのパスも引数も作業ディレクトリも、要求に入る欄が無いのは Session 3-7-1 のままになる。知らない値は Main が断るため、「Renderer からの要求で任意の実行ファイルが動く」という形は依然として作られない。`workspace-folder:open` に開く対象のパスを渡せないのと同じ線を、ここでも引いている。

**既定のシェルの名前を Renderer が知らない。** 行の id は `powershell` ではなく `default`（＝その OS の既定のシェル）で、それが Windows で PowerShell に、Unix 系で `$SHELL` に解決されるのは Main の表の都合になる。タブに出る名前も Main が返したものを描いているだけで、Renderer には「Windows なら PowerShell」という知識が1文字も無い（§8 の「OS 固有の分岐は Main に閉じる」）。

**PATH を「辿る」ことと、PATH に「任せる」ことは違う。** Node も Claude Code も System32 には居ないので、居場所を知っているのは PATH だけになる。それでも実行ファイル名だけを渡すことはしない ── Session 3-7-1 で PowerShell に対して避けたのとまったく同じ理由で、**作業ディレクトリの中身が起動されうる**ためにほかならない。PATH はこちらで辿り、相対の項目（`.` など）は飛ばし、実体を確かめてから絶対パスを渡す。見つからない行は選択肢に出さない ── Node も Claude Code も入っていない PC はふつうにあり、**表にあることと起動できることは別**になる。

**タブが増えても、器は1つのまま。** タブごとに画面の置き場所を並べて隠す形にはしていない（隠れている器は大きさが 0 になり、出すたびに測り直しが要る）。画面の持ち主は Session 3-7-1 の時点で「要素を器から器へ付け替える」形になっていたので、タブの切り替えはその経路をそのまま使う。実際、タブを行き来してもそれぞれのスクロールバックは残り、パネルを閉じて開き直しても2本とも動き続ける。

**タブを閉じることは、そのシェルを終わらせること。** パネルを閉じた / 動かしただけでは終わらない（セッションの寿命はパネルの表示より長い）のと、はっきり分けてある。Session 3-7-2 の時点では**黙って終わらせていた** ── 実行中のプロセスがあることを尋ねるのは、アプリの終了と同じく Session 3-7-4 で足してある（下記）。

Session 3-7-3 では、その Terminal を**置き場所と文字の大きさが変わっても崩れない**ところまで詰めた。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §13.4 / §13.8。

範囲は4つ ── パネルの大きさへの追従、それに合わせた ConPTY の桁数・行数、Workspace を切り替えたときの作業ディレクトリの扱い、文字の大きさとスクロールバック。

**動いているターミナルを、フォルダを見に行っただけで終わらせない。** ここが Session 3-7-3 で**振る舞いを変えた唯一の点**にあたる。シェルの作業ディレクトリが起動時に決まり後から動かせないのは以前と同じで、変えたのはそこから導く結論になる。

| 対象                     | Workspace を切り替えた後                    |
| ------------------------ | ------------------------------------------- |
| 既に動いているターミナル | **そのまま。** 作業ディレクトリも変わらない |
| その後に開くターミナル   | 新しい Workspace で起動する                 |

Session 3-7-1 では Editor がタブを捨てるのに揃えていたが、**Editor のタブと違い、ここで捨てるのは動いている OS のプロセスにほかならない。** ビルドの途中経過も、立てたままの dev server も、Claude Code との対話も、フォルダを1つ見に行った巻き添えで消えてよいものではないし、捨てたものは開き直しても戻らない。「開いていないフォルダを指したターミナルが残る」ことは受け入れ、**残っている事実を隠さない**方を選んである ── そのタブには名前の前に印が付き、`title` に「別のフォルダで起動」と出る。どのフォルダかまでは出せない（Renderer は OS のパスを1つも持っていない）。

**大きさは、器が測って xterm が決める。** 桁数と行数はピクセルではないので、器の px から桁数を出せるのは実際の文字幅を知っている xterm だけになる。パネルの側から「大きさが変わった」を伝える経路は作らず、器1か所だけを観測する（Monaco の `automaticLayout`・Files の測り方と同じ形）── パネルの置き場所は自由に変わるため、伝える側を作ると置き方が増えるたびに伝え忘れが生まれる。Dock でも Split でも境界のドラッグでもウィンドウのリサイズでも、同じ1か所で拾える。

**測り直しは次のフレームまで待ち、桁数が変わらないなら Main へ送らない。** 測り直しの中で xterm は自分の DOM を組み替えるため、観測している最中にそれを呼ぶと、境界をドラッグしている間ずっとブラウザから警告が出続ける。1フレームに1回へ束ねれば足りる（描画はそれ以上細かくならない）。文字数に直して前回と同じなら、そこで止める ── 器は1px 変わるたびに鳴るが、桁数が変わらないことがほとんどで、そのたびに ConPTY を作り直させる理由が無い。

**文字の大きさは変えられる（Ctrl + `+` / `-` / `0`）。全部のタブで同じ。** タブごとに持つと、切り替えるたびに文字の大きさが変わることになる。器のピクセルは変わらないまま入る桁数だけが変わるので、ここだけは器の観測では拾えず、変えたときに測り直して ConPTY へ伝える経路を別に持つ。

**端末の打鍵を横取りするのは、この3つだけ。** 端末は打鍵をそのまま渡すのが約束なので、横取りする組み合わせは少ないほどよい。それでも取っているのは、Session 3-7-3 の時点で文字の大きさを変える入口が他に無かったため（設定 UI は Session 3-7-5 で足した ── それでも打鍵は残してある。端末を触っている手を止めずに変えられることに意味がある）。何を取るかの判断は純粋な関数に切り出してあり、`Ctrl+C` や `Ctrl+D` が通ることをテストで固定してある ── **横取りの範囲が黙って広がらない**ようにするため。

Session 3-7-3 の時点では**保存していなかった**（開き直せば 13px に戻る）。値の持ち主を1箇所に集めてあったので、Session 3-7-5 で増えたのは「初期値をどこから読むか」だけになった（下記）。

Session 3-7-4 では、**動いているものを黙って殺さない**ようにした。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.6 / §13.9。

Session 3-7-1 から残っていた最後の「黙って」がこれにあたる。タブの × もアプリの終了も、シェルとその子プロセスをその場で終わらせる ── ビルドの途中でも、dev server が立っていても、Claude Code と話している最中でも、何も言わずに消えていた。Session 3-7-3 で「フォルダを見に行っただけでは終わらせない」を決めた時点で、**残る危険は2つだけ**になっていた（タブを閉じる・アプリを終了する）。ここを塞ぐ。

**確認の器は増やさない。** 未保存の変更を失う操作に確認を挟む仕組みは Session 3-5 で1本にまとめてある。実行中のプロセスも「続けると戻せないものが消える」という同じ性質を持ち、利用者にとっては同じ場面の同じ判断（終了しようとして、止められる場所が要る）なので、**申告を1つ足す**形にした。実際に増えたのは Terminal 側の申告と、文面を種別で分ける判断だけになる。

| 何が失われるか         | 文面                                  | 選べる道                                 |
| ---------------------- | ------------------------------------- | ---------------------------------------- |
| 未保存の変更だけ       | 「保存されていない変更があります」    | すべて保存 / 保存しない / キャンセル     |
| 実行中のターミナルだけ | 「実行中のターミナルがあります」      | 終了する / キャンセル                    |
| 両方                   | 「保存されていない変更と、実行中の…」 | すべて保存 / 保存せずに終了 / キャンセル |

**保存にあたるものが無い相手に「保存」を出さない。** 走っているプロセスは保存できない。押せるのに何も救えない選択肢は、「保存したのに失われた」と受け取られる ── ディスクから消えたファイルに保存を並べない（Session 3-5）のと同じ判断になる。

**何を実行中と見なすかは、推測しない。** 「そのシェルが子プロセスを持っているか」を OS に聞く。プロンプトで待っているだけのシェルは子を持たず、`npm run build` を走らせている間はその実行ファイルが子に居る。画面に流れた文字から「まだ終わっていないか」を当てにいく道もあるが、それは**プロンプトの見た目を推測すること**にほかならない（利用者は自由にプロンプトを変えられる）。外れたときに起きるのは「動いているものを黙って殺す」か「何も無いのに毎回尋ねる」のどちらかで、どちらも確認を挟む目的から外れる。

**分からないときは尋ねる側に倒す。** OS へ聞けなかった場合（PowerShell が見つからない・応答が返らない）は、動いているセッションを全部「実行中かもしれないもの」として扱う。確認が1回余分に出るのと、動いているビルドが黙って死ぬのとでは、前者の方がはるかに軽い。

**Workspace の切り替えでは尋ねない。** 尋ねる相手は「実際に失われる操作」だけで、Session 3-7-3 以降、切り替えでも Workspace を閉じてもターミナルは終わらない。失われない操作で確認を出すと、次に本当に失われるときの確認まで読み飛ばされることになる。

**代償は、閉じるまでに 0.4 秒ほど待つこと。** OS へ聞く往復ぶんで、実行していないタブを閉じるときにも通る。その間そのタブの × は押せない見た目にしてある（押しても何も起きない時間を、押せるように見せない）。

Session 3-7-5 では、Terminal の**見え方を選べるようにし、それを覚えるように**した。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.4 / §13.4。

範囲は2つだけ ── 文字の大きさ（8〜32px）と、さかのぼれる行数（500〜50000 行）。どちらも Terminal パネルのタブ列の ⚙ から変えられ、`%APPDATA%/Fluvix Nexus/terminal-settings.json` に残る（Session 4-3A で `settings.json` の `terminal` section へ移した。§12.4）。

**アプリ全体の Settings 画面は作っていない。** ここに置いたのは Terminal の設定だけで、入口も Terminal の中にある。全体の画面を先に用意しなかったのは、今そこへ並べられるものが2項目しか無いためにほかならない ── 項目が2つの設定画面は「まだ何も無い場所」を作るだけで、その後に足す設定の置き場所まで先に決めてしまうことになる。**値の持ち主は UI ではなく Terminal の側**なので、全体の設定画面ができたときにこの2項目はそのまま移せる。

**保存の形は Editor / Files から一字も変えていない。** 別ファイル・別チャンネル・別の検証で、既にあるチャンネルへ相乗りさせない（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §5）。3つめの利用者として同じ形をなぞったことで、「これがこのアプリの設定の形」が固まった格好になる。読み込みが終わるまで保存を許さないこと（＝起動のたびに既定へ戻らないこと）も同じ。

**壊れた設定ファイルは、端末を開けない理由にならない。** 読めない JSON も、範囲の外の値も、既定（13px / 5000 行）へ落として端末はそのまま開く。**丸める関数は1つ**で、設定 UI に打ち込まれた値も、ディスクから読んだ値も、ディスクへ書く値も同じところを通る ── 通り道が分かれると「UI では止まるのに、ファイルを直接書けば通る」が生まれる。

**さかのぼれる行数に上限を置いているのは、それがメモリそのものだから。** 出力の量に比例して Renderer のメモリが伸び、タブごとに1本ぶん持つ（最大 8本）。50000 行という上限はその最悪値から決めたもので、「選べたのに重くなった」を作らないための頭打ちにあたる。

**フォントの種類と行の高さは並べていない。** 等幅でないフォントを選べるようにすると、桁数で大きさを決めている以上、画面と ConPTY の折り返しが食い違う。「＋」で開くシェルの既定（WSL / Git Bash を含む）も今回は入れていない（下記）。

Session 3-8-1 では、Git パネルの**土台**を置いた。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14。

§3 の GitHub パネルが答えるべき問いは「①変更確認 → ②コミットメッセージ → ③Commit & Push」だが、その手前に**もっと先に決まっていなければならない問い**がある ── 今開いているフォルダで、そもそも Git 操作を始められるのか。Session 3-8-1 の範囲はそこだけになる。

**Renderer から git のコマンドも引数も作業ディレクトリも渡せない。** これがこのセッションで引いた線そのものにあたる。`git` は `-c core.pager=<任意のコマンド>` や `-c alias.x=!<任意のコマンド>` で任意の実行ファイルを起動できるため、「git の引数を渡せる API」は実質「任意のコマンドを実行できる API」になる。だから Terminal（表の行だけを渡せる）よりさらに絞り、**要求そのものを空**にした ── Renderer が言えるのは「今の Workspace について調べて」までで、渡せる欄が1つも無い。

**git 本体も PATH に任せず、こちらで辿って絶対パスで起動する。** Windows の CreateProcess は作業ディレクトリを先に見るため、`git` とだけ書くと**開いた Workspace の中の `git.exe` が動きうる**。Terminal で Node / Claude Code に対して引いたのと同じ線で、規則そのものを1箇所へ寄せてある（PATH で見つからない Git for Windows のために、既定のインストール先も当たる）。

**Workspace root がリポジトリ root でなければ、Git 操作は行わない。** リポジトリの一部だけを開いた状態で Commit を許すと、Files に出ていないファイルまで対象になる ── 利用者から見て「何を送ったのか分からない」状態を作ることになる。サブフォルダ側のリポジトリを勝手に選ぶこともしない。案内に出すのはリポジトリの**名前**だけで、その絶対パスは Renderer へ渡していない（渡せば、それを指して開かせる API が欲しくなる）。

**使えない理由は、すべて「次の一手」とセットで出す。** Git が入っていない・まだリポジトリではない・所有者を信頼できない（Windows で最も多い）・作業ツリーが無い。どれもアプリが落ちる理由にはせず、Git パネルの中の表示として扱う。**git の生の英文はそのまま出さない** ── 分類するのは Main で、Renderer へ渡るのは分類だけになる（詳細はアプリのログに残る）。

**`git init` はここでは実行しない。** 初期化は §3 の「GitHub に公開」の一部（初期化 → 初回 Commit → リポジトリ作成 → remote → Push）で、途中まで自動でやって止まると、利用者が自分で片付けられない中途半端なリポジトリが残る。今回は検出と案内までに留めてある。

Session 3-8-2 では、その上に**①変更確認**を載せた。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.8 / §14.9。

Git パネルが答える3つの問い（①変更確認 → ②コミットメッセージ → ③Commit & Push）の最初の1つで、範囲は「**状態を見る**」ことだけになる。Stage も Commit も押せない。

**一覧は4つに分けて出す** ── 競合 / ステージ済みの変更 / 変更 / 未追跡のファイル。上から下へ「利用者が次に触るもの」の順で、そのまま Commit に近い順になる。**衝突を staged / unstaged と同じ場所に並べない**のは、解決するまで Stage も Unstage も意味を持たないため ── 成立しない操作を勧める場所を作らないことにあたる。

**同じファイルが2つのグループに並ぶことがある。** index 側と作業ツリー側の両方が変わっている状態（`git add` した後にもう一度書き換えた）で、利用者から見てそれは別々に扱えるものになる。1行に畳むと、片方だけを戻す操作（Session 3-8-3 の Unstage）が行の上で表せなくなる。

**ボタンは1つも増やしていない。** この段階で押せるのは再取得と、行を押してファイルを開くことだけになる。押しても何も起きないボタンや、半分だけ効く操作を先に並べると、利用者はそれを「壊れている」と受け取る ── 未初期化のフォルダに `git init` のボタンを出さなかったのと同じ判断（設計判断 1）。

**ファイルを開く経路を新しく作らない。** 行から開くのは Files のツリー・カラム・検索結果とまったく同じ経路で、Git 専用の入口は無い。同じ形にできるのは、Workspace root ＝ リポジトリ root のときしか一覧を出さないため ── git が返す位置が、そのまま Files の相対位置になる。

**開けないものは押せる形にしない。** 削除されたファイルと、中身がすべて未追跡のフォルダ（`node_modules/` のような塊は1件にまとまる）は、「今は押せない」のではなく押す先が無い。消えたものの中身は、Session 3-8-9 で差分の側から見られるようになった（行は押せないままだが、差分のボタンは出す）。

**名前を解く処理を持たない。** git は既定では「変わった名前」を引用符で包んでエスケープして返すため、そのままでは日本語のファイル名が必ず化ける。区切りを NUL にすることで生のバイト列がそのまま返り、**解く処理そのものが要らなくなる**（空白・引用符・改行を含む名前も同じ理由で通る）。

**保存すれば一覧も変わる。** Session 3-8-1 では調べる契機が3つ（パネルを出した・Workspace の切り替え・手動更新）だったところに、作業ツリーのファイルの変化を足した。手で更新しない限り古いままの一覧は、「変更したのに出てこない」という形で嘘をつく。この時点では `.git` の中を見ていないため、`git add` や `git checkout` を端末で行った結果は届かなかった ── **手動の更新を残したのはそのため**で、その残り半分は Session 3-8-8 で埋めている（下記）。

**upstream との進み具合（↑ / ↓）も同時に出す。** Push / Pull（Session 3-8-5）が押せるようになる前に、送るものがあるのか・受け取るものがあるのかは見えていた方がよい。追跡先が無いときは欄そのものを出さない ── 「0 / 0」で表すと、同期済みと**比べる相手が無い**が同じ表示に潰れる。

Session 3-8-3 では、その一覧に**手を伸ばせる**ようにした（Stage / Unstage）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.10 / §14.11。

Git 機能で、Fluvix Nexus からの操作がリポジトリを**書き換える**のはこれが最初になる。したがってこのセッションで守るべきものは、足す機能より先に「3-8-1 で引いた線を1mm も動かさないこと」にあった。

**渡せるようになったのは、値が1つだけ。** Stage / Unstage の要求に載るのは Workspace root からの相対位置1つ、またはグループの区別（変更 / 未追跡）だけになる。git のコマンド名も引数も作業ディレクトリも実行ファイルも、**要求に欄そのものが無い**のは 3-8-1 のときと変わらない。チャンネルも汎用の実行 API を作らず、`git:stage` / `git:unstage` と操作ごとに1本ずつ切ってある。

**その1つの値も、疑って弾くだけでは足りない。** git に渡すのは「パス」ではなく **pathspec**（対象を選ぶ式）で、`a*.txt` は glob として他のファイルまで巻き込み、`:(exclude)x` は path ですらない。絶対パス・`..`・`.git` の中・空文字は弾いたうえで、**弾けないもの（`*` を含む実在のファイル名）は意味の側を止める** ── `--` の後ろに置き、`--literal-pathspecs` で glob と魔法の解釈そのものを切る。弾く方だけで済ませると、一覧には出ているのに永久に Stage できないファイルが生まれる。

**「すべて Stage」で何が載るかは、Renderer が持っている一覧では決めない。** グループの「すべて」を path の配列で渡す形にすると、それは「任意の複数 path を渡せる欄」にほかならない。渡すのはグループの区別だけにして、中身は Main がその場で読み直した状態から決める ── 押すまでの間に消えたファイルや、競合に変わったファイルを巻き込まないためでもある。**`git add -u`（作業ツリー全体）は使っていない**（衝突しているファイルまで「解決済み」として載ってしまう）。

**初回 commit 前でも Unstage できる。** HEAD がまだ何も指していないリポジトリでは「HEAD の中身へ戻す」が成り立たないため、git の版によっては断られる。**いちばん人が触る場面（初回 commit の直前）で、結果を git の版に委ねない** ── HEAD があるかどうかを先に確かめ、無ければ index から取り除く経路へ切り替える。戻り先は「未追跡」になり、それは利用者の期待とも合う。

**rename を Unstage すると、元の位置の削除も一緒に戻る。** index の上では rename は「元の削除」と「新しい位置の追加」の2つで、新しい位置だけを戻すと**削除だけが Stage に残る**。画面では1行だったものを戻したのに別の1行が残る、という結末を作らない。

**Stage / Unstage は作業ツリーに触らない。** 書きかけの中身が消える操作（discard / reset --hard）とは、チャンネルからして別にしてある ── 同じ口に「どちらか」を引数で持たせると、いつか片方の意味でもう片方が動く。

**走る git は常に1本。** ボタンを押せなくするだけでは足りない（自動の読み直しや別の行の操作は止まらない）ため、順番待ちは Main 側に置いた。弾かずに並ばせるのは、押したのに何も起きないを作らないため。操作とその後の状態の読み直しまでが1つの枠に入るので、**画面に出る一覧は必ず「その操作の結果」**になる。

**失敗しても、パネルは壊れず一覧も新しくなる。** 対象が消えていた・index が他の git に握られていた・リポジトリでなくなった ── どれも案内の画面へ差し替えず、一覧の上に1行だけ添える。応答には**成功でも失敗でも操作後の状態**が入っているため、失敗の後に古い一覧が残ることが無い。生の英文を出さない方針も 3-8-1 のまま。

Session 3-8-4 では、その一覧の下に**②コミットメッセージと③Commit** を置いた。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.12。

§3 の GitHub パネルが答える3つの問いのうち、これで①と②、そして③の前半までが揃ったことになる。

**何を Commit するかを、要求に載せていない。** Stage / Unstage では「どの1件か」を渡せるようにしたが、Commit の要求に載るのは**利用者が書いた文章だけ**になる。対象は index の中身そのもので、それを決めるのは直前までの Stage / Unstage にほかならない ── ファイルを指せる欄を作ると、画面に「ステージ済み」として出ているものと、実際に Commit されるものが別々に決まりうる。この形がそのまま「staged A / unstaged B / untracked C のうち **A だけが Commit される**」の担保になっている。

**利用者が書いた文章は、コマンドラインに1文字も載らない。** ここで初めて git へ渡るのが「位置」ではなく「文章」になった。引数に載せると、長さの上限・改行・先頭の `-`・プロセス一覧への写り込みという**4つの配慮が同時に要る**ことになり、そのどれか1つを忘れた日に破れる。標準入力から読ませれば、その4つは問い自体として消える ── 3-8-3 で pathspec に対して「値が値でしかない状態を作る」としたのと同じ形にあたる。

**足していない口の方が多い。** amend・sign・author / date の変更・`--allow-empty`・`--no-verify` は、どれも欄そのものを作っていない。履歴に永久に残るものを Renderer から書き換えられる形にしないためで、hook を迂回する口を作らないのは、**リポジトリが置いた決まりごとをアプリが黙って外す**ことになるため。その代わり、hook が走る分だけ待ち時間の上限を別に持たせてある（lint やテストを丸ごと動かす hook で必ず時間切れになる、を作らない）。

**名乗り（user.name / user.email）はアプリが決めない。** 未設定なら Commit は失敗として案内するだけで、こちらから設定することはしない ── 名乗りはリポジトリの履歴に永久に残るもので、アプリが推測した値を黙って刻むと利用者は後から直せない。動かす**前**に確かめているのは、後から stderr で当てにいくと hook の出力と混ざるためと、通らないと分かっている Commit のために hook を走らせないため。

**書いた文章は、失敗しても消さない。** 成功したときだけ入力欄を空にする。名乗りが未設定・hook が止めた・競合が残っている ── どれも文章とは無関係な理由で、直したうえで**同じ文章のまま**押し直せる必要がある。失敗のときに消すのは、文章を失わせたうえで「もう一度書いてください」と言うことにほかならない。

**押せない理由を、薄いボタンだけで伝えない。** Commit が押せない条件は3つある（ステージ済みが無い・メッセージが空・他の Git 操作が動いている）ため、押せる / 押せないを決める場所に**理由の一言も一緒に**持たせてある。ただし空欄には何も出さない ── 何も書いていない欄の下に「入力してください」と出しても、増えるのは文字だけになる。

**Commit だけは、他の Git 操作が動いている間も押せない。** 行の `＋` / `−` は押した対象だけを止めるが、Commit の中身は「ステージ済み」の**全体**で、走っている Stage / Unstage はまさにその全体を書き換えている最中にあたる。走る git が常に1本であることは 3-8-3 のまま Main 側が担保している。

Session 3-8-5 では、その下に**③の残り（Commit & Push / Push / Pull）**を置いた。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.13。

これで §3 の GitHub パネルが答える3つの問い（①変更確認 → ②コミットメッセージ → ③Commit & Push）が、ひととおり揃ったことになる。

**ネットワークへ出る操作でこそ、渡せる欄を作らない。** Push / Pull の要求は **`void`**（渡す値が1つも無い）に戻した。remote 名もブランチ名も refspec も欄そのものが無く、Renderer が言えるのは「今のブランチを送って」「取り込んで」だけになる ── 名前で指せる形にすると、**画面に出ているブランチとは別のものへ送れる欄**にほかならず、押した人から見て何が起きたのか分からなくなる。Main の側でも名前は組み立てず、`@{upstream}` / `push.default` という**git 自身に解かせる記法**だけを使う。だから `-` で始まるブランチ名も、空白や日本語を含む名前も、引数として解釈される経路そのものが無い。

**`git pull` は使わない。** Pull は `fetch` → `merge --ff-only` の2つに分けてある。`pull` は `pull.rebase` の設定で merge にも rebase にもなり、**同じボタンが PC ごとに違う履歴を作る** ── いちばん説明しにくい振る舞いにあたる。分けておけば「届かなかったのか、届いたが取り込めなかったのか」も分かり、その2つは次の一手がまったく違う。

**早送りできないときは、取り込まずに断る。** merge commit を作るか rebase するかは履歴の形を決める判断で、リポジトリの流儀によって答えが違う。アプリが黙って選ぶと、利用者が意図していない形の履歴が残り、後から直すのは難しい。断ったときに失われるものは無い（git は上書きせずに断るため、作業ツリーの書きかけもそのまま残る）。

**認証は、待たせずに・持たない。** アプリが呼ぶ git には端末が付いていないため、認証を尋ねられると待ち続ける。それを止める仕掛けを2つ掛けてある ── 端末から尋ねさせない（`GIT_TERMINAL_PROMPT=0`）だけでは足りず、Windows の既定の credential helper は**自前のウィンドウ**を出すため、helper 側の対話も止める（`credential.interactive=false`）。結果として Push / Pull は「覚えていればそのまま通る」か「即座に認証が必要として返る」のどちらかにしかならず、アプリの裏で出たウィンドウを見つけられないままパネルが固まることが無い。**Fluvix Nexus 自身は認証情報を持たない**（設計判断 7）── 尋ねればどこかへ保管することになり、OS と Git が既に持っている仕組みと二重になる。案内するのは「Terminal パネルで一度 push / pull して覚えさせる」で、その後はアプリからも通る。

**Commit & Push は1本の口にした。** Renderer から Commit と Push を続けて呼ぶ形にすると、**その2回の間に別の操作が挟まりうる** ── 挟まると「Commit したものを送った」と言えなくなる。1本にすれば、Commit・Push・状態の読み直しがまるごと1つの順番待ちの枠に入る。

**Commit は通ったが Push が通らなかった、を失敗に丸めない。** 認証・ネットワーク・remote 側の拒否は、どれも普通に起こる。失敗として出すと利用者は「Commit も失敗した」と読み、**同じ内容をもう一度 Commit する** ── 履歴に同じ commit が2つ積まれることになり、このパネルでいちばん起こしてはいけない結末にあたる。そこで「途中まで通った」という結末を別に持たせ、「Commit は完了しましたが、Push できませんでした」と**済んでいることを先に**伝える。**Commit を取り消して失敗に揃えることもしない** ── 頼まれていない取り消しであり、しかも戻す操作そのものが失敗しうる。

**通らないと分かっている Push のために、commit を積まない。** Commit & Push では、ブランチの上に居るか・remote があるか（＝Commit しても変わらないもの）を**Commit の前に**確かめる。逆に「送るものがあるか」は見ない ── Commit すれば必ず1件増える。押す前に分かることをネットワークへ出る前に分けておくのは Commit のときと同じ判断だが、こちらの方が効く（相手のサーバー次第で数十秒待たされた末に、手元の理由で失敗するのを避けられる）。

**断られ方の2つを取り違えない。** 「remote に新しい変更がある」（次の一手は Pull）と「remote 側が受け取りを断った」（保護ブランチ・サーバー側の hook）は、git の書き方が似ているのに次の一手が正反対にあたる ── 後者に「先に Pull してください」と案内すると、押しても何も変わらず利用者は同じところを回り続ける。**Pull を Push の左に置いてある**のも同じ理由で、「先に Pull」と出たときに目が右から左へ戻らずに済む。

**強制 Push の口は作っていない。** `--force` / `--force-with-lease` は他人の commit を消しうる操作で、押し間違えたときに戻せない ── 破棄や amend を置いていないのと同じ判断になる。

Session 3-8-6 で足したのは、この流れの**上**でも下でもない ── **どのブランチの上でそれを行うか**（一覧・切り替え・作成）になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.14。

**新しい場所を作らず、既にあるものを押せるようにした。** それまで文字だけだったバーのブランチ名を押すと、そこに一覧と「新しいブランチ名」の欄が開く。「今どこに居るか」と「どこへ行けるか」は同じ問いの表と裏で、押す前と押した後で目が動かずに済む。**一覧と作成は1つの面**にしてある ── 行き先が既にあるかどうかは、一覧を見て初めて決まるため。

**切り替えてよいかは、アプリではなく Git が決める。** 「未保存の変更があります。切り替えますか？」は出さない。`--force` も `--merge` も渡さないので、**失われるものがあるときは git が断る**（そのとき書きかけも今居る場所も1文字も動かない）。アプリが重ねて尋ねると、通るはずの切り替え（切り替え先が触らないファイルの書きかけは、そのまま残る）まで止めることになり、しかも尋ねた後で git に断られうる。失われるものがある操作に確認を挟む（§12.6）のは**こちらが消す側に回るとき**で、ここはそうではない。

**扱うのはローカルブランチだけ。** remote-tracking branch（`origin/x`）は一覧に載せず、名前を渡しても手元に作らない（`--no-guess`）── 既定の git は「無い名前」を渡されると remote から手元のブランチを作って追跡先まで設定するが、それは**一覧を見ただけでは分からない副作用**にあたる。remote の枝から始める形は Session 3-8-19 で**別の一覧**として足した（下記）── 副作用を混ぜるのではなく、押したら何が起きるかが行ごとに変わらない場所を別に作る、という答えになる。

**「切り替える」と「作る」を1つの口にしない。** 名前を打ち間違えたときに、切り替えたつもりで新しいブランチが増える形を作らない。逆に作成の側は `switch --create` の**1回**で作って切り替える ── 2回に分けると「作られたのに切り替わっていない」状態が残り、その後の Commit が意図しないブランチに積まれる。

**今のブランチも選べる（押しても何も起きない）。** 印は付けるが、押せなくはしない ── 押せなくすると、「今どこに居るか」を確かめるために開いた面で、いちばん見たい行だけが薄くなる。削除と rename は置いていない（削除は戻せない操作で、確認の形と一緒に設計する）。

Session 3-8-7 では、Git に**新しい機能を1つも足さず**、Session 3-8-1 〜 3-8-6 で置いたものが1本の流れとしてつながっているかを確かめた ── ①今の状態を見る → ②Stage / Unstage → ③Commit → ④Push / Pull → ⑤どのブランチの上でそれを行うか。確認の内訳は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §4。

**確かめる相手は、画面ではなく実際のリポジトリにした。** 画面の中だけで突き合わせると、それは画面どうしの整合でしかない。commit が積まれたか・remote に届いたか・作業ツリーが入れ替わったかは、すべてアプリとは別の経路（実 git と実ファイル）で読んで比べている。remote はネットワークの向こうではなく、同じ PC に置いた bare リポジトリで足りる。

**この回で直したのは、コードではなくドキュメントの食い違いだった。** 3つのドキュメントのヘッダが 3-6-8 / 3-7-5 のまま止まっている・Session 表に 3-8-5 / 3-8-6 の行が無い・確認の記録が 3-8-4 で止まっている（Push / Pull を実物の git に固定しているテストも書かれていない）・ARCHITECTURE.md §14 の番号が飛んでいる（14.10 と 14.14 が無い）・Push / Pull を Session 3-8-4 と書いている箇所がある、の5つになる。**実装は動いているのに記録だけが古い**状態は、次のセッションで「どこまで済んでいるか」を読み違える形で効いてくる。

Session 3-8-8 で足したのは、①〜⑤のどれでもない ── **手を触れずに追いつくこと**になる。

**きっかけは外部の端末ではなく、このアプリの中にある。** 3-8-7 までの Git は「呼ばれたときだけ調べる」で通していたが、Terminal パネルで `git add` と打った利用者にとって、隣の Git パネルが古いままなのは「同じアプリなのに伝わっていない」という形で見える ── 更新ボタンを押して初めて揃うのは、道具として説明の付かない振る舞いにあたる。

**見張るのは `.git` で、ファイルの監視とは別の経路にした。** 作業ツリーの監視（`files:changed`）は `.git` を**除外することで**成り立っている ── そこへ混ぜると、Files のツリーと Editor が `.git` の中の変化を受け取り始める。Git 用に watcher を1本足し、`git:changed` という別のイベントで配っている（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.15）。

**配るのは「調べ直して」という合図1つで、何が変わったかは載せない。** `.git` の中の位置は Renderer にとって意味を持たず、意味に翻訳できるのは git 自身だけになる。受け取った側がすることは常に1つ（今の状態を1回問い合わせる）で、**ブランチ名と変更ファイルの一覧はその1回の答えに揃って載る** ── ブランチを切り替えた直後に「名前だけ新しくて一覧は前のブランチのまま」が起きないのは、この形そのものによる。

**拾う名前は数え上げてある。** `.git` の中は1回の commit でも数百件が書き換わる（大半は `objects/`）。「要らないものを外す」形だと、知らない名前が増えるたびに漏れ、その1つが git の連射に化ける。拾うのは `HEAD` / `index` / `refs/` など**状態そのもの**に限り、書き込み途中のロックファイルは必ず捨てる。そのうえで束ねる時間・遅れの上限・配る間隔の3つで挟み、`git checkout` のように書き込みが数秒続く操作でも「配られない」と「配りすぎる」の両方を避けている。

**`git init` にも追いつく。** リポジトリではないフォルダを開いている間は、Workspace root を浅く見張って `.git` が現れるのを待つ ── 案内を出したまま止まらず、初期化した瞬間に Git パネルが使える状態へ変わる。

**手動の更新ボタンは残してある。** 監視は失敗しうる（権限・ネットワークドライブ・再帰監視が使えない OS）し、失敗しても Git パネルは立っているべきものにあたる。自動で追いつかない環境で、押す先が1つも無い形にはしない。

Session 3-8-9 で足したのは、①〜⑤の**手前でも先でもなく、その中身**になる ── ①の一覧に並んでいる1行を**見る**（差分）と、その1行だけを**やめる**（破棄）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.16。

**一覧は「何が変わったか」までしか答えていなかった。** `M README.md` と出ていても、それが1文字の直しなのか全面の書き換えなのかは、Editor で開いて記憶と突き合わせるしかない ── Stage する前に確かめたいのはまさにそこで、確かめる手立てが無いまま「すべて Stage」を押させるのは、道具として順序が逆になる。

**渡すのは patch ではなく、中身2つ。** `git diff` の出力をそのまま渡す形にはしない。あれは git の記法（ヘッダ・モード・スコア・index 行）がそのまま載るうえ、受け取った側で**もう一度解析が要る** ── Monaco の Diff Editor が受け取るのは2つの中身であって patch ではないため、patch を渡すと Renderer 側に「patch を当てて元の中身を復元する」という2つ目の実装が生まれる。左に出すものと右に出すものを Main が用意すれば、どこが変わったかは Monaco が一貫して決める。

**何と何を比べるかは、押した行が決まったときに決まる。** 一覧は同じファイルを2つのグループに並べることがある（`git add` した後にもう一度書き換えた状態。3-8-2）── ステージ済みの行は HEAD と index を、変更の行は index と作業ツリーを比べている。**この2つは別の答えになる**ので、渡すのは位置1つではなく「位置とグループ」になり、左右に何を出しているかも言葉として画面に出す（「変更前 / 変更後」で済ませない ── Git の「前」は3通りある）。

**差分は Editor のタブにしない。** タブは「編集して保存するもの」の置き場所で、読み取り専用のものが同じ列に混ざると、閉じるときの確認（3-5）にも未保存の印にも「このタブには当てはまらない」という例外が1つずつ生える。Auto Save の相手も増える。Git パネルの一覧の上に重ねて、Esc か `×` で閉じれば元の一覧がそのまま在る形にしてある。

**破棄は、Git 機能で初めて「利用者の書いたものが消える」操作になる。** Stage / Unstage は index を動かすだけ、Commit は履歴に足すだけ、Push / Pull / 切り替えは**失われるなら git が断る**。だからここで守ったのは、足す機能より先に「押した行1つより広いものを消さない」になった。

- **`git clean` を使わない。** 未追跡のファイルは OS のごみ箱へ送る（Files パネルの削除とまったく同じ経路）── `clean` はごみ箱を経由せず、まだ一度も Git に入っていないファイルは消したら**どこにも写しが無い**。Files が完全削除の経路を持たないのに、Git パネルの破棄だけが取り返しのつかない消し方をするのは筋が通らない
- **`reset --hard` も使わない。** 指した1件ではなく作業ツリー全体を戻す
- **ステージ済みからは破棄させない。** 「ステージ済みの変更を破棄」は実際には2つの操作（index を戻す ＋ 作業ツリーを戻す）で、押した人から見て失われるものが1回で2段になる。先に Unstage してもらえば、その2つが別々の1回として画面に出る
- **一括の破棄を置かない。** Stage の押し間違いは Unstage で戻せるが、こちらは戻せる先が無い。渡せるのは常に1件だけになる
- **未追跡のフォルダ1件は断る。** 一覧では `node_modules/` のように1行にまとまる ── 通すと、押した人から見て1行だったものが数万件の削除になる

**確認の文言は、グループで割れる。** 未追跡は「ごみ箱に移動します／ごみ箱から元に戻せます」、変更は「ステージ済みの内容に戻します／この変更は元に戻せません」。同じ文言で済ませると**片方に必ず嘘をつく** ── 未追跡で「戻せません」と出すのは要らない怖さを作り、変更で「戻せます」と出すのは取り返しのつかない誤りにあたる。

**未保存の Editor タブがあるファイルは破棄させない。** 「保存してから破棄」は書きかけをディスクへ書いてから消すだけで何も救われず、黙って破棄すると Editor 側は未保存のまま残り、次の保存で破棄したはずの中身が書き戻る。止めたうえで次の一手（保存する / タブを閉じる）を出し、その場合は**押せるボタンそのものを出さない** ── 待っても押せるようにはならないものに、disabled のボタンを置かない。

Session 3-8-10 で足したのは、①〜⑤の**手前**にあった2つ ── **まだリポジトリではないフォルダをリポジトリにすること**（`git init`）と、**そのリポジトリを GitHub へ出すこと**になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.17。

§3 は「初回のみ『GitHub に公開』ボタンから、Git 初期化 → 初回 Commit → GitHub リポジトリ作成 → remote 設定 → Push までを案内・自動化する」と書いていた。実装した答えは、その5つを**一続きの1本にしない**の側になる。

**`git init` は、それだけで終わる操作にした。** 初回 Commit も `.gitignore` も remote も作らず、押した後に GitHub への公開を促しもしない。3-8-1 で初期化を見送った理由（途中まで自動でやって止まると、利用者が自分で片付けられない中途半端なリポジトリが残る）は、**一続きにしなければそもそも起こらない** ── リポジトリにした時点で Commit / Branch / Diff / Stage / 破棄はすべて使えるようになり、公開しない人にとってはそこが終点にあたる。Git は GitHub のためだけのものではない。

**初回 Commit と `.gitignore` はアプリが作らない。** 最初の commit に何を含めるかは利用者の判断で、`.gitignore` を書く前に全部入りの commit が履歴の1つめとして永久に残るのは、後から直しにくい（`user.name` をアプリが決めないのと同じ判断）。何を無視するかも言語・ツール・人で違い、アプリが置いた「とりあえずの1つ」はそのまま正本のように残る。**初期ブランチ名も決めない** ── 素の `git init` に任せるので、`init.defaultBranch` を設定した人の意図がアプリの中でだけ変わることが無い。

**初期化には、Workspace 名を出す確認を1枚だけ挟む。** 失われるものは無い（確認を挟む基準からは外れる）が、Git パネルは自由に配置できるため**パネルの中に Workspace の名前が出ているとは限らない** ── 思っていたのと別のフォルダがリポジトリになるのが、この操作で起こりうる唯一の取り違えになる。尋ねるのは「このフォルダでよいか」1つだけで、その先に何をすべきかは書かない。

**公開は、残りの3つ（repository を作る → remote を設定する → 初回 Push）だけを1本にした。** その3つは途中で止まると利用者が直しにくい（GitHub 側に repository があるのに手元は何も知らない状態が残る）ためで、Commit & Push を1本にしたのと同じ判断になる。

**途中経過をアプリが覚えない。** 押されるたびに Git の実状態（remote があるか・commit があるか）を読み直し、済んでいるところを飛ばす ── だから「repository は作られたが Push が通らなかった」状態から、同じボタンをもう一度押せば Push だけが走る。覚える形にすると、覚えたものと実際が食い違ったとき（端末で `git remote add` した・GitHub 側で repository を消した）に、**食い違ったまま次が走る**。

**既にあるものは作り直さない。** remote が1つでもあれば repository を作らず（`origin` を上書きしない）、同じ名前が GitHub にあれば別名で作り直しもしない ── どちらも「押した人が指していないもの」を相手にすることになる。`--force` にあたる口はどこにも無い。

**GitHub 側に README も .gitignore も LICENSE も作らせない。** 1つ作らせるだけで手元が持たない commit が remote に載り、その直後の初回 Push が必ず断られる ── 公開の1回目でいちばん起こしてはいけない結末にあたる。

**中身の無いリポジトリは公開しない。** commit が1つも無ければ、外に repository を作る前に断って「先に Commit を」と案内する。detached HEAD でも公開しない。押す前に分かることを外へ出る前に分けるのは Push と同じ形だが、こちらは断ったときに**GitHub 側へ誰も使わない空の repository が残る**ため、より効く。

**公開の初回 Push だけ、認証のウィンドウが出ることを許す。** 3-8-5 で認証の対話を止めたのは、利用者が頼んでいない場面でアプリの裏にウィンドウが出て固まるのを避けるためだった ── 公開を押した直後は**認証を求められることを予期できる唯一の場面**で、そこで黙らせると「アプリの中で完結する」と言った手順の最後の1歩だけが端末へ出ることになる。外すのはこの1回だけで、以降の Push / Pull は従来どおり止めたまま動く。

**repository を作る相手は、差し替えられる境界にした。** 公開の3つのうち外の世界に触れるのは最初の1つだけで、そこだけを切り離してある。今の実装は GitHub CLI（`gh`）で、**認証情報をアプリが持たない**という 3-8-5 の判断がそのまま効いている ── 自前で OAuth を持つと token の保管（暗号化・失効・複数アカウント）がアプリ1つ分増え、しかも利用者の PC には同じものを持った道具が既にあることが多い。代償は「gh が入っていなければ公開できない」ことで、**アプリが勝手に入れることはしない**（案内と `winget install --id GitHub.cli` の1行を出すだけ）。境界にしてあることで、公開の流れそのものはネットワークにも GitHub にも触れずに確かめられる。

**公開範囲の既定は非公開（private）。** 押し間違いが「世界中から見える」になる側を、既定にも選択肢の1つめにもしない ── 後から公開へ変えるのは GitHub 側でいつでもできるが、一度出たものは無かったことにできない。

**公開の入口は、remote がまだ無いときだけ出る。** 済めばその場所ごと消え、上の Push / Pull がその先を担う ── 押す場所が2つ並ばないので、「どちらを押せばよいか」を利用者が判断する必要が無い。畳んであるのも同じ理由で、公開する気の無い人の画面に名前の欄が居座らない。

Session 3-8-11 で足したのは、①〜⑤のどれとも**相手が違う**もの ── **既に記録された commit の連なりを読むこと**（履歴）になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.19。

3-8-10 までの Git 機能が相手にしていたのは、どれも**今の作業ツリーと index**（＝これから記録するもの）だった。履歴はそこから初めて外れ、**過去**の側を出す。

**この面から変えられるものが1つも無い。** 動かす git は `log` の1本だけで、revert も cherry-pick も reset も amend も置いていない ── したがって「押せるか / なぜ押せないか」という 3-8-3 以降ずっと出てきた考え方が、この機能にだけ1つも現れない。

**渡せる欄をまた1つも作らない。** rev も件数も並べ替えも絞り込みも欄そのものが無く、返るのは常に「今の HEAD からさかのぼった 100 件」になる。別のブランチの履歴を見る手立ては既にある ── 上のバーで切り替えれば、履歴もそのブランチのものになる。

**100 件で切り、切ったことを言う。** 履歴は上限を置かなければ際限が無い唯一の一覧で（Linux のリポジトリなら 100 万件を超える）、黙って切ると利用者は「それより前が消えた」と読む。

**開いている間だけ追いつく。** 履歴を出したまま端末で `git commit` することがあり、そのとき出たままの一覧は「さっき積んだ commit が無い」という形で嘘をつく ── かといって閉じている間まで追い続けると、誰も見ていない一覧のために `git log` を動かし続けることになる。

Session 3-8-12 で足したのは、その履歴の**中身**になる ── 並んだ1行を開いて、**その commit で何が変わったか**を見る（変更ファイルの一覧と、その1件ずつの差分）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.20。

3-8-9 が「変更ファイルの一覧の1行の中身」を出したのと、同じ関係が履歴の側でも1段深くなった形にあたる。新しい場所を作ったのではなく、既にある一覧に奥行きが付いた。

**指せるのは、履歴に出した行だけ。** ここで初めて rev がアプリの境界を渡るが、渡るのは行が持っていた短い hash で、通る形は16進 4〜40 桁に限ってある ── `HEAD~5` も `main@{1}` も書けず、そもそも打ち込む欄がどこにも無い。「rev を渡せる欄」ではなく「画面に出ている行を指すための欄」になる。

**マージ commit は対象外にした。** 親が2つ以上あると、**どちらの親と比べるか**が決まらない ── git 自身も既定では答えを出さず、出させるには「1つめの親と比べる」か「全部の親と比べた合成」をこちらが選ぶことになる。どちらを選んでも、出た一覧は「この commit で変わったもの」とは違う意味を持つ。したがってマージの行は**初めから押せない形**で置き、押せない理由は一覧につき1つだけ下に出す（行ごとに出すと、同じ1文が 30 回並ぶ）。書き方は「非対応」ではなく**「決まらない」**にしてある ── 手を抜いたのではないことが伝わらないと、利用者は「そのうち直る」と読む。

**変更ファイルは 500 件で切る。** 履歴の 100 件より明らかに多い必要があり（普段の commit は数件から数十件で、100 で切ると「大きめの1回」が頻繁に切られる）、一方で 500 件を超える commit を縦に読む人は居ない。切れたことは履歴と同じく黙らない。

**面は3枚にしない。** 詳細は履歴の**中の1件**なので、履歴の面の中身を入れ替える形にした ── 重なりが3枚になると、Esc が何を閉じるのかを利用者が数えることになる。戻る道（`←`）は閉じる（`×`）と反対の端に置いてある（同じ側に並べると、1段戻るつもりで面ごと閉じる）。差分だけはその上にもう1枚重なり、そこでは **Esc が開いた順を1つずつほどく** ── 差分 → 変更ファイル → 履歴 → 閉じる。

**過去の中身を書き戻す口は作っていない。** 開けるのは差分（読み取り専用）までで、commit の中からファイルを開くことも、その中身を作業ツリーへ戻すこともできない ── 3-8-9 で「差分の中からの編集」を置かなかったのと同じ線になる。

Session 3-8-13 で足したのは、その履歴の面から**初めて git を書き込みで動かすこと**になる ── 並んだ1行を始点にして、そこからブランチを作る。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.21。

**新しい判断ではなく、保留の解除にあたる。** 3-8-6（ブランチの作成）の時点で「別の commit から作る」は外してあり、その理由は**選ぶための画面（履歴）と一緒でなければ意味を持たない**だった ── 残していた項目の中で、理由が「画面が無いから」だったのはこれ1つだけになる。その画面は 3-8-11 と 3-8-12 で出来上がっていた。

**足すのは、失われるものが無い1つだけ。** 履歴の面から動かせる書き込みは、これ以外に置いていない ── revert も cherry-pick も reset も amend も、どれも「戻せるか」という別の問いを連れてくる。ブランチを作ることは ref を1つ増やすだけで、押し間違えても消えるものが無い。

**渡せる欄は、3-8-12 で開いたものから1つも増えていない。** 始点として渡るのは履歴の行が持っていた短い hash で、通る形も確かめる関数も commit の詳細とまったく同じになる ── 打ち込む欄はどこにも無く、「rev を渡せる欄」ではなく「画面に出ている行を指すための欄」であることは変わらない。

**マージ commit も始点にできる。** 3-8-12 が差分を断ったのは「どちらの親と比べるか」が決まらないためで、始点にはその問いが無い ── 比べるのではなく、その1点から始めるだけになる。したがってマージの行では、変更ファイルは開けないのにブランチは作れる。「マージだから何もできない」ではないことが、その形で伝わる。

**作れば必ずそこへ移る。** 「作るだけ」は置いていない ── 作ったのに移っていないブランチは、次の Commit を前の枝へ積ませる。移る先が始点の中身なので、書きかけがそれで消えるなら git が断る（そのときブランチも作られない）。切り替えてよいかを決めるのは git 自身、という 3-8-6 からの線はここでも動かしていない。

**面は増やさない。** 名前を打つ欄は、押した行の**すぐ下**に開く ── 始点がそのまま真上に出ていることが「どこから作るのか」の答えになる。`Esc` は 欄 → 変更ファイル → 履歴 → 閉じる の順にほどける。

Session 3-8-14 で足したのは、3-8-6 でブランチの面を作ったときに**置かなかった2つ** ── 削除と rename になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.22。

**ここで初めて `git switch` ではなく `git branch` が動く。** 3-8-13 までのブランチ操作（切り替え・作成）は作業ツリーをまるごと書き換えるものだったが、削除と rename が書き換えるのは ref 1つで、押した人の書きかけには何も起こらない ── 待ち時間の上限も、失敗の読み方の表も、そこで分かれる。

**強制削除（`-D`）は持たない。** `git branch -d` が断るのは「そのブランチにしか無いコミットがある」ときで、`-D` はそれをどこからも辿れなくする。押し間違いの代償が釣り合わないため欄そのものを作らず、断りの文でも `-D` を案内しない ── 持っていないものを勧めると探させることになるので、行き先は Terminal パネルになる。強制 Push を置かなかったのと同じ線にあたる。

**今チェックアウトしているブランチは消せない。** これは押す前に分かる（今どのブランチに居るかは読んだ状態が持っている）ので、その行の ✕ は押せない状態にして理由を添える。行そのもの（切り替え）は今のブランチでも押せるままで、そこは 3-8-6 から変えていない ── 「押しても何も起きない」と「押しても絶対に通らない」を同じ扱いにしない。

**マージ済みかどうかは、押す前には分からない。** 基準を持つのは Git で、一覧にも載せていない（載せると一覧を開くたびに Git へ聞くことが増える）── 数少ない「押してみないと分からない結末」になるため、確認の文にも「削除を中止します」と書いてある。

**確認を挟むのは削除だけで、Git ではこれが2つめになる**（1つめは変更の破棄）。ただし**重さは同じではない。** `-d` が通るのはマージ済みのときだけなので、成功した削除でコミットが失われることは無い ── 消えるのは枝の名前とその記録で、確認の文でも「コミットが失われます」とは書かない。盛ると、本当に失われる場面（破棄）の警告まで軽く読まれる。

**rename では何も失われないので、確認は挟まない。** 変わるのは名前だけで、コミットも作業ツリーも動かない。今そこに居るブランチも改名でき、その場合は Git が HEAD を追随させ、未コミットの変更もそのまま残る。

**大文字小文字だけを変える改名は通す。** Windows では `feature` と `Feature` の実体が同じファイルになるため Git は「既にある」と断るが、そのとき指されているのは**改名しようとしているブランチ自身**で、別のブランチではない ── その1点だけは、相手が自分自身であることを確かめたうえで通す。行き先に本当に別のブランチが居る場合は断り、名前を奪うことはしない。

**remote 側は動かない。** rename しても追跡先は改名前の remote 側の名前を指したまま残るので、その後の Push は改名前の名前へ向かう。付け替える欄は作っていない（remote を指せる欄を作らない、という線のまま）。remote branch そのものの削除・改名も対象外で、前者は他人に影響してサーバー側で戻せない。

**面は増やさない。** 削除の確認も名前の入力欄も、押した行の**すぐ下**に開く（一度に1つだけ）── 別の面を重ねると、確認している最中に一覧が閉じてしまう。`Esc` は 行の下 → 面 の順にほどける。削除と rename では**面を閉じない** ── 溜まった枝を続けて片付けられるようにし、一覧はその場で取り直す。

Session 3-8-15 で足したのは、**アプリが既に案内していたのに、行き先が無かった1つ** ── 退避（stash）になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.23。

**「Commit するか退避してから」と書いていたのは 3-8-5 からになる。** 書きかけがあると Pull もブランチの切り替えも Git が断り、そのとき出る文には最初からそう書いてあった ── 前半（Commit）は 3-8-4 で用意できていたのに、後半（退避）だけが端末に丸投げのままだった。3-8-15 で埋めたのはそこになる。

**3-8-6 の「自動 stash は渡さない」は動かしていない。** あれが断ったのは*Git の判断をアプリが黙って上書きすること*で、利用者が明示的に押す退避はその逆側にあたる。切り替えの前に勝手に避けることは今も無い。

**退避に名前を付ける欄は作らない。** 名乗りは Git が付ける（`WIP on main: …`）── `git init` の初期ブランチ名を素の Git に任せたのと同じ線で、アプリが決めなくてよいものは決めない。

**未追跡のファイルは避けない。** `-u` を渡さないため、退避されるのは Git が既に知っているものだけになる ── 未追跡まで巻き込むと、1行に見えて中身が数万件になりうるものを作業ツリーから消すことになる（3-8-9 で未追跡のフォルダの破棄を断ったのと同じ事情）。したがって未追跡しか無いときは押せず、その理由もそう書く。

**「戻す」の口は1つだけ。** 残したまま戻す（`apply`）は置いていない ── 押した後に一覧へ残るかどうかがボタン次第で変わると、どちらを押したかを覚えていないと今の状態が分からなくなる。

**戻すと競合することがある。** そのとき中身は**作業ツリーへ書き込まれたうえで**競合し、Git は退避を捨てずに残す ── これを「失敗」と出すと、利用者は押し直して二度目は「上書きされる」と断られるだけになる。したがって出すのは「戻りましたが競合しました（退避は一覧に残しています）」で、Commit & Push が途中で止まったときと同じ形になる。競合そのものは、3-8-2 から在る「競合」グループがそのまま受ける ── 新しい面は作っていない。

**捨てるときだけ確認を挟み、Git ではこれが3つめになる**（破棄・ブランチの削除に次ぐ）。**3つの中でいちばん重いのがこれ**にあたる ── 破棄が消すのは書いたばかりの変更、削除が消すのはマージ済みの枝の名前でコミットは残るが、捨てた退避の中身はどのブランチからも辿れなくなる。それでも「完全に失われます」とは書かない（盛らない）── 書くのは「アプリからは取り消せません」までになる。

**番号は信じない。** `stash@{1}` は名前ではなく**上から数えた位置**で、端末で1つ避けられれば全部が1つずつ後ろへずれる ── 一覧を出してから押すまでの間にずれると、画面で選んだのとは違う退避が消える。したがって押した瞬間に、その位置に居るのが画面に出ていたものかを Git へ確かめ直し、違えば動かさずに断る。番号そのものは画面に出さない（読む人が「その退避の名前」として受け取ってしまう）。

**面は増やさない。** 一覧と「作業ツリーを退避」は1つの面に置き、捨てる確認は押した行の**すぐ下**に開く ── 履歴と同じ場所・同じ閉じ方で、`Esc` は 行の下 → 面 の順にほどける。退避したものの中身を見る画面は置いていない。

Session 3-8-16 で足したのは、**3-8-10 が開けたままにしていた穴** ── remote の管理になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.24。

**「公開」の隣に「接続」が無かった。** §3 は「初回のみ『GitHub に公開』ボタン」と書いており、3-8-10 はそのとおりに作った ── だが公開は**新しく作る**側だけで、既にどこかに在るリポジトリへ繋ぎたい人（clone せずに始めた人・会社のサーバーへ送る人）には行き先が無く、`no-remote` の文言も「Terminal パネルで `git remote add` を」と外へ案内していた。3-8-16 で埋めたのはそこになる。

**3-8-5 からの「remote を指せる欄を作らない」は、1箇所だけ緩めた。** 足す / 消すは名前で指せるようにし、**送る / 受けるは今も指せない**（要求は `void` のまま）── 増えたのは登録簿を編集する口で、送り先を選ぶ口ではない。この2つを分けている限り、「画面に出ているブランチとは別のところへ送れる欄」は生まれない。

**URL は、このアプリで唯一「Git に任意のプログラムを起動させうる値」になる。** `git remote add x "ext::sh -c whoami"` は今の Git がそのまま受け取り、追加した時点では何も起きないまま、以降の Push / Pull でその文字列がシェルとして走る（実物で確かめた）。つまり素通しの欄にすると、**時間差で発火する任意コマンド実行の欄**になる。

**だから、危ないものを弾くのではなく通す形を決め打ちにした。** 受け付けるのは `https://…` / `ssh://…` / `user@host:path` の3つだけで、それ以外は**危なくないものも含めて**通さない（`git://`・`http://`・ローカルのパス）── 例外を1つ作った時点で、次の例外を断る根拠が「なんとなく危なそう」になる。断りの文には使える3つを書いてあるので、次に打つものは断り文の中に居る。

**認証情報を含む URL も断る。** `https://<トークン>@github.com/…` は実在する使い方だが、通すと**アプリが利用者のトークンを `.git/config` へ平文で書く**ことになる ── 「アプリは認証情報を持たない」は「アプリのどこにも溜めない」であって、「別の場所へ書くのはよい」ではない。認証は今までどおり Git の credential helper と gh に任せる。

**画面に出るのは URL ではなく、表示用のラベルにした。** `https://ghp_xxx@github.com/octocat/Hello-World.git` は `github.com/octocat/Hello-World` として並ぶ ── スキームも認証情報もポートも `.git` も落ちているので、**そこから URL は組み立て直せない。** リポジトリの絶対パスを渡さないのと同じ線で、渡せば Renderer がそれを指して何かを頼みたくなる。ラベルを作るのは Main だけで、Renderer 側には作り方そのものを置いていない。

**それでも、読めない remote を一覧から隠さない。** 端末から追加された `git://` もローカルのパスも `ext::…` も、行としては並ぶ（場所は出さず「ローカルのパス」「不明な形式」とだけ言う）── 隠すと、**見えない remote が Push の送り先になっている**状態を作ることになる。

**URL を変える欄は作らなかった。** `git remote add` は名前が既にあれば失敗し、それがここで欲しい振る舞いになる ── 黙って上書きすると、送り先が入れ替わったことに誰も気づかないまま Push が別のところへ飛ぶ。同じ名前が来たら「既にあります」と断る。

**「公開」と「接続」は別の口のままにした。** どちらも最後は `git remote add` を動かすが、公開の側は名前が固定（`origin`）で URL は GitHub が返したものになる ── 1本にまとめると、公開の口から任意の URL を渡せることになり、3-8-10 の保証がそこで消える。

**消えるのは3つで、そのうち1つが確認の理由になる。** `git remote remove` は設定と手元の remote-tracking ref に加えて、**その remote を追っていたブランチの追跡先**まで消す（実物で確かめた）── コミットは1つも失われないが、押した後に `↑2 ↓1` が消えて Push が「初回の Push」に戻るのは、押した人が予想していないことにあたる。Git で確認を挟む4つめで、**4つの中ではいちばん軽い** ── だから文にも「コミットは失われません。同じ URL で登録し直せます」まで書く。盛ると、本当に戻せない場面（退避を捨てる）の警告まで軽く読まれる。

**追っているブランチが在っても止めない。** 3-8-14 でチェックアウト中のブランチの削除を止めたのは Git が必ず断るものだったからで、こちらは Git が通す ── 止めると「消したいのに消せない remote」が生まれる。伝えるのは確認の文で行う。

**入口は上のバーに置き、remote の有無に関わらず開ける。** 無いときだけ出す形にすると、押す場所がリポジトリの状態で動く ── 1つも無い人にとっても「ここが接続する場所」であることは変わらない。それとは別に、Push / Pull の下（公開のボタンの隣）へ「既存のリポジトリに接続する」の1行を置いてある ── remote が無い人がいちばん長く見ているのはその位置で、そこに選択肢が1つしか無いと公開が唯一の道に見える。ボタンではなく控えめな1行にしてあるのは、2つを同じ大きさで並べるとどちらを押すかを先に決めさせることになるため。

**面は増やさない。** 一覧と追加の欄は1つの面に置き、削除の確認は押した行の**すぐ下**に開く ── 履歴・退避と同じ場所・同じ閉じ方で、`Esc` は 行の下 → 面 の順にほどける。

Session 3-8-17 で足したのは、**3-8-16 が登録簿に置かなかった「書き換える」2つ** ── remote の URL の変更と rename になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.25。

**残していた理由を実物に当てたら、片方は当たり、もう片方は逆だった。** 3-8-16 は2つとも「別の設計が要る」として置かなかったが、確かめた結果は分かれた ── `set-url` は書いたとおり危なく（しかも書いたより悪く）、`rename` は**消して足し直すより安全**だった。**残す理由を書いた時点の推測は、次のセッションで確かめ直すものになる。**

**`set-url` が書き換えるのは、設定の1行だけになる。** URL を丸ごと別のサーバーへ向けても、手元の追跡情報（`refs/remotes/origin/…`）も追跡先も1つも動かない ── つまりバーの `↑1` はそのまま残り、それは**もう別の相手と比べた数**にあたる。3-8-16 が「誰も気づかない」と書いたのは控えめで、実際には**画面が積極的に嘘をつく。**

**だから断つのは「上書き」ではなく「黙って」の方にした。** 危なかったのは上書きできること自体ではなく、それが知らないうちに起きることだった ── 押す前に「今どこを指していて、これからどこを指すか」を並べれば、その理由はそこで解ける。Git で確認を挟む5つめで、**5つの中で失われるものが1つも無い唯一の確認**になる。だから文言も「何が消えるか」ではなく**何が残るか**を言う（前の送り先の追跡情報が残ること）── それがこの操作でいちばん読まれにくいことにあたる。

**確認に出る2つは、どちらも新しく渡ってきた値ではない。** 左は一覧の行が既に出しているラベル、右は利用者が今その欄に打った文字列になる ── 3-8-16 の「URL は画面へ渡さない」は1文字も動かしていない。**その代わり、URL を変える欄は空から始まる**（今の URL がそもそも画面に無いため）。打ち直すのは全体になるが、送り先を変える人は新しい URL を手元に持っている。

**同じ URL を打っても止めない。** 画面は今の URL を持っていないので、押す前に「変わりません」と言う手立てがそもそも無い ── 確かめるために Git を1本増やしても、得られるのは文言だけで、変わらない値で上書きしても壊れるものは1つも無い。**言えることと言えないことは、比べる相手が手元にあるかで決まる**（rename の側では「同じ名前」を押す前に言える ── 名前は行が持っているため）。

**rename には確認を置かない。** `git remote rename` は追跡情報も追跡先も既定の送り先も全部追随させ、**失われるものが1つも無い** ── rename の後も `↑2 ↓1` は消えず、Push はそのまま通る。3-8-14 でブランチの rename に確認を置かなかったのと同じで、確認が要るのは削除の側になる。**消して足し直す2段でアプリが代用することもしない** ── 後半で失敗すると、remote が1つも無いリポジトリが残る。

**大文字小文字だけの改名だけは、押せなくした。** ここは 3-8-14 と**逆の答え**になる ── ブランチでは Git に逃げ道（`--force`）があり、相手が自分自身だと確かめたうえで通していた。`git remote rename` にそれが無く、Windows で渡すと Git は**途中まで書き換えたまま止まる**（設定だけが新しい名前になり、追跡情報も追跡先も古い名前を指したまま残る）── つまり「押しても通らない」ではなく**押すと壊れる。** 同じ問いに逆の答えを出しているのは、Git が用意している逃げ道の有無がそこで分かれるためになる。理由の文には**なぜ通らないか**まで書く ── 「使えません」だけだと、打ち直せば通ると読まれて同じところを何度も試すことになる。

**面も入口も増やさない。** 増えたのは行の中の操作2つで、1行に3つ並ぶ形になる（ブランチの行とまったく同じ）── 並びは「変える → 変える → 消す」で、戻せない側が端に来る。色が赤に変わるのは ✕ だけで、**行そのものは3つになっても押せないまま**にあたる（remote を「選ぶ」操作は今も1つも無い）。

Session 3-8-18 で足したのは、**3-8-2 から在った競合のグループに、初めて付く操作**になる ── 解決し終えたことを Git へ伝える1手。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.26。

**アプリは3箇所で「解決してください」と言いながら、その行き先を持っていなかった。** Commit の断り文（3-8-4）・退避が押せない理由（3-8-15）・退避を戻した結末（3-8-15）の3つで、どれも解決を促していた ── そして競合の行にできたのは**エディタで開くこと**だけだった。つまり利用者はアプリの中で直せるのに、**直したと Git へ伝える手段が無かった**ことになる。3-8-15（「Commit するか退避してから」と書いていたのに退避が無かった）・3-8-16（「公開」は在るのに「接続」が無かった）とまったく同じ形で、3-8-18 で埋めたのはそこになる。

**しかも、その状態はアプリ自身が作れる。** Pull は `--ff-only` 固定なので競合せず、切り替えは競合しそうなら Git が断る ── **競合を生む唯一の経路が退避を戻すこと**（3-8-15）にあたる。押した結果として入った状態から出られない、という形をこれで無くした。

**足りなかったのは1手だけで、3方向のマージエディタは要らなかった。** エディタは既にある（競合の行は 3-8-2 から開ける）。そして解決した後の Commit は 3-8-4 のままで通り、マージの途中ならそのまま**マージのコミットを作って途中の印を消す**（実物で確かめた）── 前後がどちらも出来上がっていて、真ん中の1手だけが空いていたことになる。

**「Stage」とは別の操作にした。** 動く Git は同じ `git add` だが、Stage が「作業ツリーの姿を次のコミットへ写す」のに対し、こちらは**競合の3つの記録（元 / こちら / 相手）を1つに畳む**ことになる。同じ名前で出すと、押した人は「後で Stage を解除すれば戻せる」と読む ── **戻せない**（解除しても競合は復元されず、ただの変更として残る。実物で確かめた）。だから絵も言葉も別にしてある。

**取り消しは置いていない。** 戻す手立てが無いからではなく、**戻したことにならない手立てしか無い**ため ── Stage の解除は競合を復元せず、復元できる方（`checkout --merge`）は利用者が書いた解決内容を上書きする。どちらを「取り消し」として出しても、押した人が失うものがある。したがって解決した行に「解除」も出さない。

**マーカーが残っていたら押せない。** Git は `<<<<<<<` が残ったままの記録も、その後のコミットも**通してしまう**（そして履歴に永久に残る。実物で確かめた）── 断っているのはアプリになる。確かめるのは Git 自身（`diff --check`）で、何がマーカーかをこちらで決めない ── 自分で探すと、差分の書き方を説明した文書のような**正当なファイル**まで拾う。

**2つの「競合」の文を、別のことにした。** 「Commit できません」の次の一手は**この操作を押すこと**で、「マーカーが残っています」の次の一手は**エディタで直すこと**になる ── 同じ言葉に潰すと、押した人はどちらを直せばよいか分からないまま同じボタンを押し直す。

Session 3-8-19 で足したのは、**3-8-6 が「別途設計する」と書いて残していた1つ** ── remote の枝から手元にブランチを作ることになる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.27。

**一覧に無いものへは行けなかった。** 3-8-6 の一覧はローカルブランチだけで、`--no-guess` を渡して「名前を打てば remote から生える」git の推測も止めてあった ── その判断は正しかったが、**行き先そのものを持たないまま**だったことになる。clone せずに始めた人・他の人が作った枝で作業を始める人は、そこで Terminal パネルへ出るしかなかった。3-8-15 / 3-8-16 / 3-8-18 と同じ「自分が閉じた道の先を、後から用意する」形になる。

**混ぜるのではなく、別の一覧として足した。** 3-8-6 が remote-tracking branch を載せなかった理由（押したら手元に何かが作られる、という**一覧を見ただけでは分からない副作用**が1行おきに混ざる）は、1文字も撤回していない ── 代わりに面の3段目として別の一覧を置き、そこでは**押しても切り替わらない**（ローカル名を確かめる欄が開くだけ）。押したら何が起きるかが行ごとに変わらない、という性質を両方の一覧で保つ。

**それでも同じ面に置いた。** 上のバーに4つめのボタンを足す形にはしていない ── 一覧を開いて `feature/x` が無かった人の次の問いは「じゃあ remote には在るのか」で、それが別の面にあると、閉じて別のところを押し直すことになる。3-8-6 が一覧と作成を1つの面に置いたのと同じ理由がそのまま3つめにも効く。既定では畳んであり、増えるのは見出しの1行だけになる。

**ローカル名は既定値を出して、変えられるようにした。** `origin/feature/x` を押すと `feature/x` が入った欄が開く ── いちばん多い答えを打たせない。それでも欄にしてあるのは、**同じ名前のローカルブランチが既にある人に、打ち直す以外の道が無い**ためになる。

**同じ名前があったら、Git を動かす前に止まる。** 取りうる道は4つあった ── 付け替える（`--force`）・先に消す・そのブランチへ切り替える・断る。**3つめを選ばなかったのがいちばん迷うところ**になる。利用者が本当に欲しいのはたいてい「その枝で作業を始めること」で、切り替えれば済むように見えるが、その「既にあるブランチ」が**同じ名前の別物**であることは普通に起こる ── 黙って切り替えると、押した人は remote の内容が手元に来たと思ったまま、別の枝の上で作業を始める。1つめと2つめを選ばないのは 3-8-13 / 3-8-14 で引いた線（元の枝を見失わせない・そこにしか無いコミットを巻き添えにしない）のままになる。

**この面はネットワークへ出ない。** 開いても fetch しない ── 一覧を開く操作が外へ出ると、開くたびに認証を求められうるうえ、「見るだけ」のつもりの1回が数十秒かかる（3-8-16 で remote を足した直後に fetch しなかったのと同じ線）。したがって出るのは**最後に取得した時点の写し**で、**そのことを面にそのまま書く** ── 黙ると、消された枝が残って見え、増えた枝は出てこないのを「アプリが壊れている」と読まれる。切れている一覧を黙って切らない（3-8-6）のと同じ判断になる。

**各 remote の `origin/HEAD` は載せない。** あれは「その remote の既定がどれか」を指す**別名**で、選んで手元に作る相手ではない ── 載せると、指した先が別の行（`origin/main`）と同じになる選択肢が並ぶ。

Session 3-8-20 で足したのは、**3-8-18 が「口が無い」と書き残していた2つ** ── マージの開始と中止になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.28。

**競合を解決できるのに、競合を始められなかった。** 3-8-18 で「解決済みにする」を置いたが、そこへ至る唯一の経路は退避を戻すこと（3-8-15）だった ── Pull は `--ff-only` 固定で競合せず、切り替えは競合しそうなら Git が断る。つまりアプリは**競合の出口だけを持っていて、入口を持っていなかった**ことになる。枝を分けて作業する人が最後に必ず行う「取り込む」が、Terminal パネルにしか無かった。3-8-15 / 3-8-16 / 3-8-18 / 3-8-19 と同じ「自分が閉じた道の先を、後から用意する」形になる。

**`--ff` と `--no-autostash` を明示した。** どちらも Git の既定と同じ値だが、**書かないと PC ごとの設定で変わる**もの ── `merge.ff=false` の PC では早送りできる場面でも余計なマージコミットが積まれ、`merge.ff=only` の PC では枝分かれしていると断られる。`merge.autoStash=true` の PC では、Git が作業ツリーを**勝手に退避してから**マージし、途中で失敗すると**利用者が作った覚えのない退避**が 3-8-15 の一覧に並ぶ。同じボタンが PC ごとに違うことをするのは、Pull で `git pull` を使わないと決めた理由（`pull.rebase` 次第で merge にも rebase にもなる）とまったく同じにあたる。実物で3つとも確かめてある。

**早送りできるなら、マージコミットは作らない。** `--no-ff` を選ぶ欄は置いていない ── 履歴の形を決める判断で、リポジトリの流儀によって答えが違う。戦略（`-X ours` / `-X theirs`）も同じ理由で置いていない。あれは**競合を自動で潰す**もので、書いた人の中身が黙って消えうる。

**競合は「失敗」ではなく「途中」として返す。** Git は自動でマージできた分を作業ツリーと index へ**既に書き込んでいる**ので、「何も起きなかった」ではない ── 失敗に丸めると、利用者はもう一度マージを押す。だが途中の印は既に在るので、Git は「未解決のファイルがあります」としか言わず、**先へ進む手立てが画面のどこにも見えなくなる**。だから文言は「マージを開始し、自動でマージできた変更は取り込みました」から始める。3-8-5 の Commit & Push・3-8-10 の公開・3-8-15 の退避に続く4つめだが、**そこから先に利用者がやることがある**のはこれだけになる。

**その先は1手も足していない。** 競合の行は 3-8-2 の「競合」グループにそのまま並び、「解決済みにする」は 3-8-18 のまま効き、Commit は 3-8-4 のままマージを完結させる ── 3-8-18 が「前後がどちらも出来上がっていて、真ん中の1手だけが空いていた」と書いた形の、今度は**入口の側**が埋まったことになる。

**`Already up to date` は普通の成功にした。** それだけを区別するために Git をもう1回動かしたり、結果の型を増やしたりはしない（3-8-5 の Pull と同じ判断）。

**相手はローカルブランチだけにした。** タグもコミットハッシュも remote-tracking の名前も渡らない ── 名前の**形**だけを見て通すと、`git merge <タグ>` が「ブランチのマージ」を名乗ったまま動く（Git はどれも受け取る。実物で確かめた）。動かす前に「その綴りちょうどのローカルブランチが在るか」を確かめてから通す。

**始めるのも、やめるのも、確認を挟んだ。** 開始の確認は**消えるものが無いのに挟む、初めてのもの**になる ── 押す場所が切り替えの行の上（1文字ぶんの距離）にあり、誤って押すと**履歴にマージコミットが積まれる**。その取り消し（`reset`）はアプリが持たない。中止の確認は逆に「消える」側で、**開始前から在った変更は残り、解決中に書いた内容は失われる** ── 2つを同じ文に並べて書く。片方だけだと、どちらかの嘘になる（実物で両方を確かめた）。

**中止はブランチの面ではなく、パネルの帯に置いた。** マージ中に面を開くと、そこに並ぶのは**今は押せない行ばかり**で、出口がその奥にあることになる。専用のマージ画面も作っていない ── マージ中に利用者がすることは既にこのパネルに在るもので、その上に別の画面を被せると、いちばん見たい一覧が隠れる。帯が足すのは「なぜ今この状態なのか」の1行と、そこから出る道1つだけになる。

**「マージの途中か」は Git に聞いた。** 「競合している行があるか」からは導けない ── 解決し終えた後もコミットするまで途中のままで、逆に退避を戻した競合では途中ではない。導ける形にしておくと、いつかどちらかの側で間違える。

Session 3-8-21 で足したのは、**3-8-9 が「当てはまらない」と書いて外していた1つ** ── 競合している1件の ours / theirs の差分になる。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.29。

**競合を直せるのに、何と何が食い違っているのかは見られなかった。** 競合の行にできたのはエディタで開くこと（3-8-2 から）と解決済みにすること（3-8-18 から）の2つで、開いて見えるのは**マーカーが混ざった1つの中身**だけだった ── 「どちらが何を書いたのか」を知るには端末で `git show :2:<path>` を叩くしかなかったことになる。3-8-15 / 3-8-16 / 3-8-18 / 3-8-19 / 3-8-20 と同じ「自分が閉じた道の先を、後から用意する」形になる。

**器を増やすのではなく、組を1つに決めた。** 3-8-9 が競合を外した理由は「『前』と『後』が2組ある」ことで、その診断は正しい ── 答えの側を「3つ並べる面を作る」ではなく「**並べる2つを決める**」にした。出すのは ours（現在の段）と theirs（取り込む側の段）で、共通の元（merge base）は出さない。Diff の部品が受け取るのは2つの中身で、3つ並べるには面も部品も別に要る ── 3方向の Diff は後続のセッションへ回した。

**別のチャンネルにした。** 位置を1つ渡して中身が2つ返る、という形は既存の差分と同じに見えるが、**左右の意味が違う** ── 既存の差分は「前 → 後」で時間の向きがあり、競合の2つは**同じ瞬間の、2つの枝の中身**になる。1本にまとめると、返ってきたものの意味が要求次第で変わる口が1つできる。

**片側にファイルが無い競合でも、ボタンは出す。** 両方で消えた・片方だけが作った、という競合では左右のどちらか（あるいは両方）に中身が無い。それでもボタンを出すのは、**一覧の行が「競合」としか言わない**ため ── どういう競合なのかを知れる場所が、この画面のほかに無い。開いた先で「この側にはファイルが存在しません」と読める方が、押しても何も起きないボタンより手掛かりになる。

**ours / theirs の意味は、マージ中だけ言い切った。** マージ中なら ours は今のブランチ・theirs は取り込む側だが、rebase や cherry-pick では**逆になり**、退避を戻したときの競合では**どちらも自分の変更**で枝の話ですらない。アプリが Git に訊いて真だと言えるのは「マージの途中か」だけなので、そこでだけ「現在のブランチ / 取り込み側」と補い、それ以外では `ours` / `theirs` のまま置いた ── 断定すると、画面が積極的に嘘をつく（3-8-17 の `set-url` と同じ形）。

**段の番号（stage 2 / stage 3）は隠さなかった。** Git の記法を画面へ出さない線に触れて見えるが、ここでは**段そのものが見せているものの正体**にあたる ── 訳語だけを出すと、端末で `git checkout --ours` を打つ人が、画面のどちらを見ていたのか照合できない。

**画面は読むだけのまま。** ours / theirs を作業ツリーへ採る操作も、この画面からの「解決済みにする」も置いていない。前者は**利用者が書きかけた解決内容を上書きする**もので、3-8-18 が取り消しを置かなかった理由と同じ側にあたる。後者を置かないのは、同じ操作の入口が2つになると押せる条件（マーカーが残っていないか）を2箇所で説明することになるため ── 解決は一覧の行のまま、Commit は 3-8-4 のままになる。

Session 3-8-22A では、**新しい Git の機能を広げるのではなく、3-8-1 〜 3-8-21 を1本の流れとして使ったときに引っかかる4つ**を埋めた。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.30。

**取ってくる口が、どこにも無かった。** `git fetch` は 3-8-5 から在ったが、走るのは Pull の中だけで、その Pull は**追跡先が無ければ押せない**。つまり追跡先の無いブランチに居るあいだ、remote-tracking ref を新しくする手立てがアプリの中に1つも無かったことになる ── 3-8-19 の「remote の枝から手元にブランチを作る」一覧は、面に「最後に取得した時点の写し」と書きながら、取得する口だけが無い状態だった。バーではなく Push / Pull と同じ並びに置いたのは、押すと git が動いて上のバーの数字が変わる**同じ性質の操作**だからで、履歴・退避・リモート（面を開く3つ）とは置き場所を分けてある。

**`--prune` は付けた。付けたのはこちらだけ。** この口の用途は一覧を今の remote に合わせることで、そこには「もう相手に無い枝が消えること」まで含まれる ── 付けないと、消えた枝が一覧に残って「同名のローカルブランチを作る」だけの行になる。Pull の中の `fetch` には付けない ── あちらの用途は**取り込むために取ってくる**ことで、そこに消す働きが混ざると「Pull を押したら一覧から枝が消えた」になる。同じ `git fetch` でも押した人の予想が違うので、引数の表を2行に分けた。消えるのは追跡の写しだけで、ローカルブランチも commit も1つも失われない（実物で確かめてある）。

**「マージ中」を「途中の Git 操作」へ広げた。** 3-8-20 が読んでいたのは `MERGE_HEAD` 1つで、rebase / cherry-pick / revert の途中は**存在しないのと同じ扱い**だった ── 端末で rebase を始めて競合させた状態でパネルを開くと、帯も出ず、Stage も Commit も素通りする。アプリは自分が扱えない状態の上で、扱えるふりをしていたことになる。この3つを**始める機能は持たない**（終わらせる口も無い）ので、検出して**危険な操作を止めるためだけ**に使う ── 帯は「Terminal で `git rebase --continue` か `--abort`」と行き先まで出し、中止のボタンはマージにしか置かない（`merge --abort` はその3つを中止しないため、置けば押しても何も終わらないボタンになる）。

**止めるものと通すものを、表で1箇所に決めた。** マージの途中で止めるのは HEAD を動かす操作（切り替え・作成・remote の枝から作る）と、index ごと入れ替える操作（退避する / 戻す）と、必ず断られる Pull だけ。**Stage・解決・破棄・Commit・Commit & Push は通す** ── そこを止めるとマージから出られなくなる（中止しか道が無くなる）。Push と Fetch も通す（HEAD にも index にも触らない）。rebase / cherry-pick / revert では逆に**書き込みを1つも通さない** ── アプリに出口が無いのに Stage や Commit だけを通すと、終わらない道の途中まで案内することになる。表は shared に置いて **Main と Renderer が同じものを読む**（画面では押せないのに IPC は通る、を作らない）。

**「git が断るから任せてよい」は、成り立たなかった。** 3-8-20 が切り替えや退避を止めなかったのは「競合が残っている間は git が断る」からだった。だが競合を全部解決して `git add` まで済ませた一瞬は index がきれいで、そこでは **`git stash push` が通ってしまい、`MERGE_HEAD` が黙って消える**（実物で確かめてある）── 解決に費やした作業ごとマージが無かったことになり、戻す口も無い。一方で切り替えは git が最後まで断った ── **どちらを通すかは git が決めていて、アプリが寄りかかれる規則になっていない**。だから両方とも手前で断つ。

**Commit 欄が白紙から始まらないようにした。** 3-8-20 でマージを始められるようになったのに、それを終わらせる Commit だけは利用者が文章を打たないと押せなかった ── 端末の `git merge` なら `Merge branch 'feature'` が既定で入るところが、アプリでは白紙だった。git が用意した文章を**入力欄の初期値として入れる**形にしてある（欄が空のときだけ）。`--no-edit` で git に任せる形にしなかったのは、**押す前に読めなくなる**ため ── 入力欄には何も出ていないのに履歴には文章が入る、という形は 3-8-4 の「画面に出ているものがそのまま commit になる」を崩す。git が書いたコメント行（`# Conflicts:`）は落とす ── アプリの Commit は `--cleanup=whitespace` 固定で、落とさないとその行が履歴に入る。落とす規則は自分で書かず、git 自身（`stripspace --strip-comments`）に通す。

**押しても開けない行が1つだけ残っていた。** 競合の行は 3-8-2 から「フォルダでも削除でもない」ので、エディタで開くボタンが出ていた。ところが**両方が消した競合（`DD`）だけは作業ツリーにファイルが無い**（rename / rename の競合で普通に起こる）── そこだけ押しても開けなかった。競合の形を `git status` の `XY` から持ち帰る（git を1回も増やさない）ようにして、その1つでだけボタンを出さない。**差分の口は出したまま**にしてある ── 開いた先で「どちらにも無い」と読める方が、押せないボタンより手掛かりになる（3-8-21 の判断のまま）。

Session 3-8-22B では、**実装をほとんど増やさず、3-8-1 〜 3-8-22A で置いたものを production app で通した。** 詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §14.31、測った項目は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §4。

**確かめる場所を「実アプリ」に寄せたのは、ここでしか出ない不具合があるため。** 3-8-15 / 3-8-16 / 3-8-17 は実 git テストを持っていたが、`GitStashOverlay` と `GitRemoteOverlay` を**実アプリで一度も押していなかった**（§4 の記録が 3-8-14 から 3-8-19 へ飛んでいる）。そこを通したことで、自動テストでは出ない種類の抜けが1つ見つかった ── 純粋な関数も Main も正しいのに、**面へ props を渡し忘れていた**という形になる。

**3-8-22A の禁止が、退避の面にだけ届いていなかった。** 3-8-22A は「止めるものと通すものを表で1箇所に決めた」うえで、上のバー・ブランチの面・行の操作・Commit 欄へ被せて回った。ところが `GitStashOverlay` にだけ渡し忘れており、**マージ中でも「戻す」が押せ、競合を解決し終えた後は「作業ツリーを退避」も押せた**（実アプリで確かめた）。Main は両方とも断っていたので**退避が消えることも `MERGE_HEAD` が落ちることも無かった**が、押しても必ず失敗するボタンが残っていたことになる ── 3-8-2 からの「押しても何も起きない操作を置かない」に反する。3-8-22B で、他の箇所とまったく同じ形（`withGitInProgressBlock`）で被せた。

**この抜け方は、被せる形にした代償にあたる。** 3-8-22A は「既存の readiness に引数を足して回るのではなく上から被せる」ことを選び、その理由を「引数を増やす形だと、渡し忘れた1つが静かに素通りする」と書いていた。被せる形でも**被せ忘れは静かに素通りする** ── 違いは、被せ忘れが `GitView.tsx` の1箇所に並んでいるかどうかで、面へ props として渡す先（ブランチ・退避）はその並びから外れる。表そのものは shared に1つで正しく、Main が正本として断つ構えも効いていた ── **効かなかったのは画面の側だけ**で、そこは実アプリで押す以外に測る手段が無い。

**Renderer の判定が許可の根拠ではないことは、実物で確かめた。** マージ中に `window.fluvix.git` の6つ（切り替え・作成・remote の枝から作る・退避する・Pull・もう1度マージ）を直に叩き、すべて `operation-in-progress` で断られること・HEAD も `MERGE_HEAD` も退避も1つも動かないことを測ってある。

**残っている未確認は1点で、環境に起因する。** 「GitHub に公開」の**成功する経路**は、この PC に GitHub CLI が入っていないため実アプリで通せていない（3-8-10 と同じ状況で、当時も gh の無い側だけを確かめている）。gh が無いときの断りと案内・公開の口が remote を足すと消えることは確かめてあり、**アプリの中の経路としては閉じている** ── 残るのは gh と GitHub に触れる1本だけになる。

### Session 3-6-8 の後へ残していること

Files まわりで、Session 3-6-1 〜 3-6-8 の範囲から意図的に外したもの。

| 項目                          | 現状                                                                         |
| ----------------------------- | ---------------------------------------------------------------------------- |
| 切り取り / ペースト           | 器（`mode: 'cut'`）は残すが、**UI へは出さない**と決めた（上記）             |
| 複数選択とその Drag           | 今は単一のファイル / フォルダだけを対象にしている                            |
| ツリー外との Drag & Drop      | エクスプローラから落とす / 落とすと開く、は Workspace の境界を通す別の話     |
| OS クリップボードとの受け渡し | エクスプローラとのコピーには絶対パスが要る。やるなら Main 側の別の経路にする |
| 置換 / Workspace 全体置換     | 検索は読むだけ。書く側は保存の経路（`files:write-file`）に載せる形になる     |
| 正規表現 / あいまい一致       | 語は字義どおりに扱う（大文字 / 小文字だけを区別しない）                      |
| 検索フィルター（拡張子など）  | 要求は検索語だけ。足すと契約と走査の両方に条件が増える                       |
| Git 連携検索 / AI 検索        | 走査の入口が別のもの（index・履歴）になるため、別の経路として設計する        |
| フォルダ名ごとのアイコン      | `src` / `node_modules` / `.git` など。種類の表とは別の3つめの表になる        |
| アイコンテーマの切り替え      | 表と色は固定。差し替えるなら絵の表ごと切り替える形                           |
| 列ごとのカラム幅              | 幅は1つ（160〜480px）。列ごとに持つには列に安定した識別が要る                |
| カラムでの複数選択            | ツリーと同じく単一のまま（複数選択は上の行と同じ範囲）                       |
| Files のその他の設定          | 除外フォルダ・並び順などは持たない。設定画面（§9）と一緒に考える             |

Editor まわりで、Session 3-5 の範囲から意図的に外したもの。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.8。

| 項目                                | 現状                                                                     |
| ----------------------------------- | ------------------------------------------------------------------------ |
| Save As（削除されたファイルの救出） | タブは「削除済み」として残り内容も残る。**別の場所へ書き戻す手段**が無い |
| Shift_JIS / UTF-16                  | 境界は整理済み。増えるのは「バイト列 ↔ 文字列」の2箇所だけ               |
| Settings 画面                       | 設定の**値**は既に永続化されている。並べる場所を作るだけ                 |
| アプリの外での改名の対応付け        | 監視からは削除 + 作成として届く（推測で1つにまとめない）                 |
| 監視の除外設定                      | `.git` / `node_modules` を固定で外している。Settings から読む形にできる  |
| Conflict の3方向マージ              | Compare は読み取り専用。編集できる差分は Git の解決 UI と同時に考える    |

### Session 3-7-5 の後へ残していること

| 項目                                 | 現状                                                                              |
| ------------------------------------ | --------------------------------------------------------------------------------- |
| 何が実行中かを名前で出す             | 「実行中のものがある」までで、実行ファイル名は Renderer へ渡していない            |
| アプリ全体の Settings 画面           | 設定の**値**は Editor / Files / Terminal とも永続化済み。並べる場所を作るだけ     |
| フォントの種類・行の高さ             | 固定値のまま。等幅でないフォントを選べると桁が狂う（上記）                        |
| 「＋」で開くシェルの既定を選ぶ       | 既定は OS の既定シェルのまま。選ぶのは `⌄` から（設定としては持たない）           |
| 端末の配色（テーマ）の設定           | ANSI 16色はシェル側のもの。地・文字・カーソル・選択だけが Theme に追従する（4-4） |
| 動いているターミナルの cwd を移す    | **手段が無い**（ConPTY は cwd を変えられない）。中で `cd` するのは利用者          |
| 別フォルダのタブに、その名前を出す   | 印だけ。名前を出すには Renderer が Workspace の表示名を持つ必要がある             |
| タブの並べ替え・名前の変更           | 並びは開いた順のまま。掴んで動かす経路も、名前を上書きする欄も持たない            |
| タブの復元（起動し直しても残る）     | セッションは OS のプロセス。保存できるのは「何を開いていたか」まで                |
| シェルの行を増やす（WSL / Git Bash） | 表に行を1つ足すだけ。解決の仕方（PATH / レジストリ）が行ごとに違う                |
| 選択のコピー / 貼り付け・端末内検索  | xterm の addon か自前の経路。OS クリップボードは Main 側の別の話                  |
| 1パネルの中での分割                  | 分割はレイアウトの担当（Workspace Shell）。Terminal の中に別の分割を作らない      |

### Session 3-8-22B の後へ残していること

Git まわりで、Session 3-8-1 〜 3-8-22B の範囲から意図的に外したもの（3-8-7 は統合確認と仕上げ、3-8-8 は `.git` の監視、3-8-22A は仕上げ、3-8-22B は production app での統合確認で、どれも Git の操作そのものはほとんど増やしていない）。以降のセッションで足す。

| 項目                                   | 現状                                                                                                                                                                                  |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 複数ファイルの任意選択                 | Stage できるのは「1件」か「グループのすべて」。任意の複数を渡せる欄は作らない（上記）                                                                                                 |
| すべて Unstage                         | 置いていない。次の Commit の中身を丸ごと空にする操作で、押し間違いの代償が釣り合わない                                                                                                |
| 強制 Push（`--force`）                 | 欄そのものを作っていない。他人の commit を消しうる操作で、押し間違えたときに戻せない（上記）                                                                                          |
| 自動 merge / rebase（Pull）            | `--ff-only` 固定。どちらを選ぶかはリポジトリの流儀で決まり、アプリが黙って決めてよいことではない（上記）                                                                              |
| Push / Pull の取り消し                 | 動いている git を途中で止める手段は持たない。Terminal の終了確認と同じ形で、後のセッションで設計する                                                                                  |
| Push / Pull の送り先を選ぶ             | 選べない。決めるのはリポジトリの設定のまま ── 3-8-16 / 3-8-17 で増えたのは**登録簿を編集する口**だけになる                                                                            |
| `git://` / ローカルのパスの remote     | 登録できない。通す形は3つだけで、**知らない形は危なくなくても通さない**。変更のときも同じ（3-8-16 / 3-8-17）                                                                          |
| 認証情報を含む URL の remote           | 断る。通すと、アプリが利用者のトークンを `.git/config` へ平文で書くことになる（3-8-16）                                                                                               |
| remote の URL を画面に出す             | 出さない。出るのは**表示用のラベル**だけ ── だから URL を変える欄も**空から始まる**（3-8-17）                                                                                         |
| 大文字小文字だけの remote の rename    | 通さない。Git は途中まで適用したまま落ちる（`--force` に当たるものが無い）── 動かす前に断る（3-8-17）                                                                                 |
| URL を変えた後の追跡情報の掃除         | しない。前の送り先の分がしばらく残る ── 消す操作を変更に混ぜず、次の Pull で揃うのを待つ（3-8-17）                                                                                    |
| push 側だけ別の URL（`--push`）        | 置いていない。一覧に出るのは fetch 側1つで、2つ並べても読む人にできることが無い（3-8-16 / 3-8-17）                                                                                    |
| URL 変更 / rename の取り消し           | 持たない。打ち直せば戻る ── アプリが前の値を覚えておく形にはしない（3-8-17）                                                                                                          |
| Push / Pull の進捗表示                 | 「動いている」までで、何 % かは出していない。結末は終了コードで決める                                                                                                                 |
| amend / sign / author / date の変更    | 欄そのものを作っていない。履歴に永久に残るものを Renderer から書き換えられる形にしない                                                                                                |
| hook の迂回（`--no-verify`）           | 持たない。リポジトリが置いた決まりごとをアプリが黙って外すことになる                                                                                                                  |
| 空の Commit（`--allow-empty`）         | 持たない。押したのに何も起きない、の別の形にあたる                                                                                                                                    |
| Commit テンプレート / 専用エディタ     | 入力欄は複数行の1つだけ。改行は通るが、書式を用意する側には回らない                                                                                                                   |
| Commit メッセージの AI 生成            | 走査の入口が別のもの（差分の中身）になるため、別の経路として設計する                                                                                                                  |
| Git identity の設定 UI                 | 未設定なら失敗として案内するだけ。**アプリからは設定しない**                                                                                                                          |
| 強制削除（`git branch -D`）            | 欄そのものを作っていない。そこにしか無いコミットを到達不能にする ── 断って Terminal へ案内する（3-8-14）                                                                              |
| 名前を奪う rename（`git branch -M`）   | 既定では渡さない。相手が**自分自身**だと確かめられた1点（大文字小文字だけの改名）でだけ立てる（3-8-14）                                                                               |
| ブランチの一括削除                     | 置いていない。渡せるのは名前1つで、複数を渡せる欄は作らない（Stage の「任意の複数」と同じ線。3-8-14）                                                                                 |
| 一覧に「マージ済みか」を出す           | 載せない。一覧を開くたびに Git へ聞くことが増える ── 基準を持つのは Git で、断られたら理由が返る（3-8-14）                                                                            |
| コミットが無いリポジトリの rename      | 手立てが無い。ref が1つも無く一覧が空になるため、押す行そのものが出ない（3-8-14）                                                                                                     |
| rename に合わせた追跡先の付け替え      | しない。追跡先は改名前の remote 側の名前を指したまま残る ── remote を指せる欄を作らない線のまま（3-8-14）                                                                             |
| remote branch の削除 / rename          | 対象外。一覧に出ていないものは指せず、削除は他人に影響してサーバー側で戻せない（3-8-14）                                                                                              |
| 削除 / rename の取り消し               | 持たない。消えた名前を戻す手立ては reflog の中にあり、それを読む画面は無い（3-8-14）                                                                                                  |
| remote の枝を1つの一覧に混ぜる         | 混ぜない。押したら何が起きるかが行ごとに変わる ── 3-8-19 は**別の一覧**として足した（上記）                                                                                           |
| 一覧を開く前の fetch                   | しない。開くたびにネットワークへ出る形にしない ── 出るのは最後に取得した時点の写しで、そのことを面に書く（3-8-19）。**取得そのものは 3-8-22A の Fetch で押せる**                      |
| remote-tracking の掃除（`prune`）      | **3-8-22A の Fetch に含めた**（`fetch --prune`）── 3-8-19 が否定したのは「一覧を**開く**操作に消す働きを混ぜること」で、押したときだけ消える形とは矛盾しない                          |
| Fetch の相手を選ぶ（`--all`）          | 選べない。相手は今のブランチの remote のまま ── `--all` は**名前を1つも見ていない相手の ref を消す**ことになる（3-8-22A）                                                             |
| rebase / cherry-pick / revert を始める | 置いていない。3-8-22A で足したのは**検出と禁止だけ**で、始める口も終わらせる口（`--continue` / `--abort`）も持たない ── 行き先は Terminal になる                                      |
| 途中の操作からの復帰（`--continue`）   | 持たない。上記と同じ ── 帯が Terminal で打つコマンドまで出すのは、アプリの中に道が無いことの裏返しにあたる（3-8-22A）                                                                 |
| remote の枝へ直接切り替える            | 置いていない。名前を付けずに移る形（detached HEAD）は持たず、指した先に**名前を付けて**移る（3-8-13 と同じ線。3-8-19）                                                                |
| 追跡しない作成（`--no-track`）         | 欄そのものを作らない。追跡しない作成はバーの「＋」が既にその形で、この口の意味は追跡先が付くことそのものになる（3-8-19）                                                              |
| 追跡先の後付けの変更                   | 置いていない。作るときに決まり、後から付け替える口は持たない ── remote を指せる欄を作らない線のまま（3-8-19）                                                                         |
| 同名があるときの上書き / 自動切替      | しない。Git を動かす前に断る ── 同じ名前の**別物**であることは普通に起こる（3-8-19）                                                                                                  |
| `origin/HEAD` を一覧に出す             | 出さない。既定のブランチを指す**別名**で、指した先が別の行と同じになる選択肢を並べない（3-8-19）                                                                                      |
| 競合の3方向（base を含む）Diff         | 置いていない。並べるのは2つに決めた ── 3つ並べるには画面も部品も別に要る（3-8-21）                                                                                                    |
| ours / theirs を作業ツリーへ採る       | 置いていない。**利用者が書きかけた解決内容を上書きする** ── 3-8-18 が取り消しを置かなかったのと同じ側（3-8-21）                                                                       |
| 競合の Diff 画面からの「解決済みに」   | 置いていない。画面は読むだけのまま ── 同じ操作の入口が2つになり、押せる条件を2箇所で説明することになる（3-8-21）                                                                      |
| 改名の競合3行を1つに束ねる             | 置いていない。「どの3行が同じ1つの出来事か」は Git の出力から読めない（Git は3件として返す。3-8-21）                                                                                  |
| rebase / cherry-pick 中の意味づけ      | 読まない。**状態そのものは 3-8-22A で読むようになった**が、ours / theirs の言い換えは増やさない ── どれでもない競合（退避を戻したとき）が残るため、中立の表現が受け皿になる（3-8-21） |
| 競合しているファイルの検索 / 絞り込み  | 置いていない。競合の行は一覧の1グループのままで、そこに絞り込みの欄は作らない（3-8-21）                                                                                               |
| submodule の競合の Diff                | 出さない。submodule が指すのはファイルの中身ではなくコミットで、並べるものが無い（3-8-21）                                                                                            |
| 競合しているファイルの破棄             | 対象外のまま。**何に戻すかが決まらない**（ours / theirs / 共通の元の3つ）── 戻す先を選ばせる画面と一緒に設計する（3-8-9）                                                             |
| 一覧に無いブランチへ名前で切り替え     | 切り替え先は一覧から選んだものだけ。上限（500 件）を超えた分は端末で扱う                                                                                                              |
| 切り替え時の自動 stash / `--merge`     | 渡さない。決めるのは Git 自身で、その判断は上書きしない ── **手で押す退避は 3-8-15 で別に足した**（上記）                                                                             |
| ブランチを作るだけ（切り替えない）     | 置いていない。作れば必ずそこへ移る ── 移っていないブランチは、次の Commit を前の枝へ積ませる（3-8-13）                                                                                |
| 履歴の commit へ切り替える             | 置いていない。commit を指して移る手立ては持たず、指した先に**名前を付けて**移る（3-8-13）                                                                                             |
| 始点を打ち込む欄                       | 作らない。渡せるのは履歴の行が持っていた短い hash だけで、3-8-12 の線をそのまま保つ（3-8-13）                                                                                         |
| ブランチを別の commit へ付け替える     | 置いていない。元の枝がどこにあったかを見失わせる ── 同じ名前があれば断り、別の名前を打ち直してもらう（3-8-13）                                                                        |
| 初回 Commit の自動化                   | しない。最初の commit に何を含めるかは利用者の判断で、履歴の1つめは後から直しにくい（3-8-10）                                                                                         |
| `.gitignore` の生成 / テンプレート     | 置かない。何を無視するかは言語もツールも人も選ぶ話で、アプリの「とりあえずの1つ」が正本のように残る（3-8-10）                                                                         |
| 初期ブランチ名を選ぶ                   | 素の `git init` に任せる。`init.defaultBranch` を設定した人の意図をアプリの中でだけ上書きしない（3-8-10）                                                                             |
| 所有者（Organization）を選んで公開     | 欄そのものを作っていない。ログインしているアカウントの下に作る（3-8-10）                                                                                                              |
| 説明文 / トピック / README の設定      | 渡す欄が無い。GitHub 側でいつでも書ける（README を作らせると初回 Push が必ず断られる。3-8-10）                                                                                        |
| 既にある repository へ公開する         | しない。同じ名前があれば断る ── 別名で作り直すことも上書きもしない（3-8-10）                                                                                                          |
| 公開の取り消し（repository の削除）    | 持たない。作った repository を消す口は、公開の失敗時にも用意していない（3-8-10）                                                                                                      |
| 作った repository を開く / URL を出す  | URL は Main の中だけに留める。Renderer へ渡すと、それを指して何かを頼む欄が欲しくなる（3-8-10）                                                                                       |
| GitHub Enterprise / 別ホスト           | 相手は github.com に固定。選べるようにするなら「どこへ公開するか」の画面と一緒に設計する（3-8-10）                                                                                    |
| GitHub CLI の自動インストール          | しない。出すのは案内と winget の1行だけ（3-8-10）                                                                                                                                     |
| `.git` の監視の細かさ                  | 拾うのは `HEAD` / `index` / `refs/` など状態そのものに限る。設定から変える形は持たない（§14.15）                                                                                      |
| submodule / worktree の追従            | `.git/modules` も `.git/worktrees` も見張らない。「Workspace root ＝ リポジトリ root」の前提から外れる                                                                                |
| サブフォルダのリポジトリを自動で選ぶ   | 選ばない。Workspace root がリポジトリ root のときだけ操作する                                                                                                                         |
| bare リポジトリ / worktree             | 作業ツリーが無い場合は理由を出して止まる                                                                                                                                              |
| 認証情報の保存                         | Fluvix Nexus 自身では持たない。git は credential.helper、GitHub は gh に任せる（3-8-10）                                                                                              |
| ステージ済みの破棄（1回で2段）         | 置いていない。**先に Unstage** してもらう ── 1回で失われるものが2段になる（3-8-9）                                                                                                    |
| 一括の破棄（グループごと / すべて）    | 置いていない。Stage の押し間違いは Unstage で戻せるが、破棄には戻せる先が無い（3-8-9）                                                                                                |
| 未追跡のフォルダ 1 件の破棄            | 断る。1行に見えて中身は数万件になりうる。行にも出さず、Main も届いた対象を確かめて断る（3-8-9）                                                                                       |
| `git clean` / `reset --hard`           | 使わない。前者はごみ箱を経由せず、後者は指した1件より広い範囲を戻す（3-8-9）                                                                                                          |
| 差分の中からの編集 / 行単位の Stage    | Diff Editor は読み取り専用。直すのは元のエディタで行う ── 保存の入口を2つにしない（3-8-9）                                                                                            |
| 履歴の2点を比べる（commit を2つ選ぶ）  | 選べる欄そのものを作っていない。渡せる rev は常に1つで、比べる相手はその親に固定される（3-8-12）                                                                                      |
| 競合しているファイルの差分             | **3-8-21 で実装済み**（並べるのは ours と theirs の2つ）── 3-8-9 は「並べる形が当てはまらない」として外していた（上記）                                                               |
| 改行だけの違いを差分として出す         | 出せない。左右とも LF に均してある ── 均さないと `core.autocrlf` の環境で全行が変更として出る（3-8-9）                                                                                |
| 3方向マージのエディタ                  | 置いていない。解決はエディタで行い、アプリは**解決し終えたことを Git へ伝える**だけを持つ（3-8-18 / 3-8-20 で入口が付いた）                                                           |
| 競合の ours / theirs の差分            | 置いていない。「前」と「後」が2組ある形は既存の差分の器に載るが、3-8-18 / 3-8-20 の範囲外にした（**Session 3-8-21 の候補**）                                                          |
| 解決の取り消し                         | 持たない。`reset` は競合を復元せず、`checkout --merge` は書いた解決内容を上書きする ── どちらも「取り消し」にならない（3-8-18）                                                       |
| マージそのものを始める / やめる        | **3-8-20 で実装済み**（ブランチの行から取り込み、パネルの帯から中止）── 3-8-19 まで口が無く、始めるのは端末だった                                                                     |
| マージの戦略（`-X ours` / `theirs`）   | 置いていない。競合を**自動で潰す**もので、書いた人の中身が黙って消えうる（3-8-20）                                                                                                    |
| `--no-ff` / `--squash` / `--no-commit` | 選ぶ欄を作らない。履歴の形を決める判断で、リポジトリの流儀によって答えが違う ── 早送りできるなら早送りする（3-8-20）                                                                  |
| remote の枝を直接マージする            | 置いていない。相手はローカルブランチだけ ── 取り込むには 3-8-19 で手元に作るか、Pull を使う（3-8-20）                                                                                 |
| 無関係な履歴のマージ                   | 通さない（`--allow-unrelated-histories` を渡さない）。戻すには merge commit を reset することになる（3-8-20）                                                                         |
| マージした後の取り消し                 | 持たない。merge commit を作った後の `reset` は口を作っていない ── 行き先は端末になる（3-8-20）                                                                                        |
| 複数ブランチの一括マージ               | 置いていない。渡せるのは1件で、複数を渡せる欄は作らない（Stage の「任意の複数」と同じ線。3-8-20）                                                                                     |
| 競合の一括解決                         | 置いていない。渡せるのは1件で、複数を渡せる欄は作らない（Stage の「任意の複数」と同じ線。3-8-18）                                                                                     |
| 競合したファイルの破棄                 | 対象外。何に戻すのか（ours / theirs / merge base）が決まらない（3-8-9 のまま）                                                                                                        |
| マージ commit の変更ファイル / 差分    | 出さない。親が2つ以上あると**どちらと比べるか**が決まらず、git 自身も既定で答えない。理由は面に出す（3-8-12）                                                                         |
| 500 件より多い変更ファイル             | 続きを読む欄は置いていない。**どこまで読んだか**を覚える状態が1つ増える。それより先は Terminal の `git show`（3-8-12）                                                                |
| commit の中からファイルを開く / 復元   | 置いていない。開けるのは差分（読み取り専用）まで。過去の中身を作業ツリーへ書き戻す口は無い（3-8-12）                                                                                  |
| 100 件より前の履歴 / 履歴の絞り込み    | 置いていない。件数も pathspec も作者も `git log` へ渡す値になる ── それより前は Terminal の `git log`（3-8-11）                                                                       |
| 履歴からの revert / cherry-pick        | 1つも置いていない。履歴の面から動く書き込みは**失われるものが無い1つ**だけ（ブランチを作る。3-8-13）                                                                                  |
| 退避に名前を付ける                     | 欄そのものを作らない。名乗りは Git が付ける（`WIP on <branch>: …`）── 初期ブランチ名を選ばないのと同じ線（3-8-15）                                                                    |
| 未追跡も退避する（`-u` / `-a`）        | 渡さない。1行に見えて中身が数万件になりうるものを作業ツリーから消すことになる（3-8-9 の破棄と同じ事情。3-8-15）                                                                       |
| 残したまま戻す（`apply`）              | 置いていない。「戻す」の口を2つにすると、押した後に一覧へ残るかが押し方次第で変わる（3-8-15）                                                                                         |
| 段まで戻す（`--index`）                | 渡さない。戻ると unstaged として並ぶ ── Stage は一覧の `＋` で1回で戻せる（3-8-15）                                                                                                   |
| 退避の中身を見る / 差分                | 置いていない。指し方（`stash@{N}`）が hash ではないため、コミットの詳細の経路にそのままは載らない（3-8-15）                                                                           |
| 退避からブランチを作る                 | 置いていない。3-8-13 の「始点」と混ざる ── 履歴の行から作る形とは別の問いになる（3-8-15）                                                                                             |
| 一括で捨てる（`stash clear`）          | 置いていない。渡せるのは1件で、複数を渡せる欄は作らない（ブランチの一括削除と同じ線。3-8-15）                                                                                         |
| 部分的に退避する（`-p`）               | 置いていない。行単位の Stage を置いていないのと同じ線で、対話も始まる（3-8-15）                                                                                                       |
| 捨てた退避を戻す                       | 持たない。`git fsck --unreachable` で拾える間は残るが、それを読む画面は無い（3-8-15）                                                                                                 |
| Graph（枝の線）/ rebase / tag          | 線を引くには全部の親と並べ方が要り、他の2つは履歴を書き換える。別の経路として設計する                                                                                                 |

---

## 11. STEP 4（完了）— 日常使用の土台

Session 4-1 の調査で、Editor は「編集して保存して Git で送る」までは実際に使えるものの、**毎日使うには足りていない**ことが分かった。STEP 4 はその土台を揃える段で、LSP（STEP 5 の予定）を載せる前提にもあたる。

| セッション | 内容                                         | 状態 |
| ---------- | -------------------------------------------- | ---- |
| 4-2        | 別名で保存                                   | 完了 |
| 4-3A       | 設定の保存基盤（`settings.json` と section） | 完了 |
| 4-3B       | Settings 画面                                | 完了 |
| 4-4        | Theme（Dark / Light）                        | 完了 |
| 4-5A       | Localization の土台と Language 設定          | 完了 |
| 4-5B       | Files / Editor / Terminal の文言             | 完了 |
| 4-5C       | Git の文言                                   | 完了 |
| 4-7A       | Command とショートカットの土台               | 完了 |
| 4-7B       | Git / Files の操作を command として名乗る    | 完了 |
| 4-7C       | Keyboard Shortcuts の一覧                    | 完了 |
| 4-8A       | production app での統合確認                  | 完了 |
| 4-8B       | ドキュメントの追従                           | 完了 |
| 4-8C       | STEP 4 Closing                               | 完了 |

**4-6 は欠番。** Localization（4-5）の次にショートカット（4-7）へ進んだときの採番の飛びで、飛ばした内容は無い。

### Session 4-2（完了）— 別名で保存

閉じたのは**アプリの中に1つだけ残っていた「押した先で内容が必ず失われる」経路**。外から消されたファイルのタブは中身を持ったまま残るが、書き戻す先が無いため保存できず、閉じる前の確認は「ディスク上から削除されています」と出したうえで捨てる以外の選択肢を出せなかった。

- ネイティブの保存ダイアログで行き先を選び、**Workspace の中へも外へも書き出せる**。行き先を決めるのはダイアログだけで、Renderer から保存先を渡す経路は作っていない（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.9）
- 削除されたタブ専用にはせず、**どのタブでも押せる**通常の操作にした（Editor の工具列と、削除の帯の2箇所から同じ経路を呼ぶ）
- 保存先が Workspace の中なら、タブがそこへ移って未保存と「削除済み」が解け、以降は通常の Ctrl+S が効く
- 上書きの確認は OS のダイアログに任せ、アプリ側で二重に訊かない
- BOM の有無・CRLF / LF は上書き保存とまったく同じ扱い（**別名で保存しても形が変わらない**）

このセッションで、**位置を付け替えた後のタブに未保存の印が出なくなる不具合**も直した（別名で保存より前からあり、アプリの外での改名でも起きていた）。印が出ないだけで中身は未保存のまま残るため、閉じる前の確認にも並ばず黙って失われる経路だった（§12.9.5）。

**Workspace の外へ保存した場合、タブはそこへ追従しない。** 内容は確かに書き出されるが、Editor が開けるのは Workspace の中だけで、外のファイルを開いたまま保存し続けるには同一性・監視・版管理の作り直しが要る。その要否は §9 の未確定事項として残してある。

### Session 4-2 の後へ残していること

| 項目                               | 現状                                                                                     |
| ---------------------------------- | ---------------------------------------------------------------------------------------- |
| Workspace の外のファイルを開く     | 書き出しだけができる。開いたまま保存し続けるには、タブの同一性を作り直す必要がある（§9） |
| 別名で保存のショートカット         | **Session 4-7A で `Ctrl+Shift+S` が入った**（予告どおり、基盤と同時にした）              |
| 保存先の候補を賢くする             | ダイアログは今開いているファイルの場所で開くだけ。名前を提案し直す形は持たない           |
| 「保存して閉じる」からの別名で保存 | 閉じる前の確認からは救えない。救えないことと、救える場所（Editor の帯）だけを伝える      |
| 新規ファイルをタブから作る         | 別名で保存は既に中身があるタブが対象。何も開かずに始める（untitled）経路は別のもの       |

### Session 4-3A（完了）— 設定の保存基盤

閉じたのは**設定を1つ足すたびに同じ形を写していた状態**。Session 3-5 〜 3-7-5 では設定が増えるたびに「ファイル1つ・チャンネル2つ・検証1つ・Renderer 側の読み書き1組」を丸ごと足していた（`editor-settings.json` / `files-settings.json` / `terminal-settings.json`）。3つ並んだ時点ではっきりしたのは、**増えていたのが設定ではなく同じ形の写しだった**ことにほかならない。写しが3つあるということは間違え方も3通りあるということで、中でも「読み込みが終わるまで保存を許さない」は落としても表に出にくい（起動のたびに設定が既定へ戻るのに、「設定が保存されない」ようにしか見えない）。

- 保存先は `%APPDATA%/Fluvix Nexus/settings.json` の1つにまとめ、その中を **section**（`editor` / `files` / `terminal`）で分ける
- 用途の限定は**ファイル名ではなく section の閉じた集合**が持つ。IPC は `settings:load` / `settings:save-section` の2本だけで、保存要求は section 名で値の型が決まる判別可能なユニオン ──「名前と JSON を渡すと保存される」汎用 API にはしていない
- **壊れていても、壊れたところだけを捨てる。** section の中を平らにしてあるので、`autoSaveMode` が1つ壊れていても待ち時間は残る（旧形式は入れ子で、1つ壊れるとその設定が丸ごと既定へ戻っていた）
- **知らない section・知らない key はそのまま書き戻す。** 新しい版で足した設定が、古い版で一度起動しただけで消えるのを避けるため。逆に Renderer から届いた知らない key は保存しない
- **旧3ファイルは `settings.json` が無いときだけ読み**、取り込めたものをその場で1度書く。1つ壊れていても他は移り、**旧ファイルは消さない**（戻れる道を残す）
- `schemaVersion` の移行の入口を先に作った（現在は版 1 で、まだ1段も無い）── 無いと後から差し込む場所が決まらず、検証と変換が混ざる

**設定を足すときに増えるのはファイルでもチャンネルでもなく、section か key になった。** Theme（`appearance`）や Workspace 設定はこの形でそのまま入る。既存の見た目と操作は何も変わっていない（Auto Save の4方式・Files の表示方式とカラムの幅・Terminal の文字の大きさとさかのぼれる行数）。

### Session 4-3A の後へ残していること

| 項目                      | 現状                                                                             |
| ------------------------- | -------------------------------------------------------------------------------- |
| Settings 画面             | 値の保存はすべて揃った。並べる場所を作るのは Session 4-3B 以降                   |
| `settings:changed`        | 設定を変えた側と読む側が同じ Renderer なので要らない。窓が増えたら要る           |
| Workspace ごとの設定      | 今はアプリの設定だけ。プロジェクトフォルダの中には何も書かない方針は変えていない |
| Theme / Editor の追加設定 | `appearance` は Session 4-4 で入った（中身が決まってから作る、を守った形）       |
| 旧3ファイルの掃除         | 消さずに残している。消してよくなるのは、戻る先が要らなくなってから               |

### Session 4-3B（完了）— Settings 画面

Session 4-3A で設定の**保存**は揃っていた。残っていたのは並べる場所で、この Session で塞いだのは**「保存されているのに、変える口がどこにも無い設定があった」**状態にほかならない（Auto Save の待ち時間は Session 3-5 から保存されていたが、保存ファイルを手で書き換えるしか変える方法が無かった）。

- **ウィンドウ全体に重ねる非モーダルの面**にした（Git の差分・履歴・退避と同じ器）。設定は Editor / Files / Terminal の3つにまたがるのでどのパネルの中にも置けず、Dock 対象にすると開くたびに配置が動く
- 入口は**上部バーの「Settings」1つだけ**。閉じ方は面の中の `×` と Esc の2つ
- カテゴリは **Editor / Files / Terminal の3つだけ**。中身がまだ無い Appearance / General / Git / Workspace / Language / Debug は作らない ── 空のカテゴリは「まだ何も無い場所」を画面に作るだけになる（保存側で空の section を先に作らないのと同じ線）
- 並ぶのは5項目（Auto Save の方式・待ち時間 / Files の表示方式 / Terminal の文字の大きさ・さかのぼれる行数）。**Files のカラムの幅だけは載せない** ── 掴んで動かして決めるもので、数字の欄にすると閉じてから確かめることになる（保存基盤の上では今までどおり持ち続ける。schema も変えていない）
- **設定の値は必ず1つ。** この面は値を1つも持たず（state は「今どのカテゴリを見ているか」だけ）、既存の Context / hook から読んで既存の setter へ返す

**既存 UI を残すか移すかは「見ながら合わせるものか」で分けた。**

| 設定                        | 既存 UI               | 判断                                    |
| --------------------------- | --------------------- | --------------------------------------- |
| Files の表示方式            | Files のツールバー    | **残す**（一覧を見ながら決める）        |
| Terminal の文字の大きさ     | Terminal のタブ列の ⚙ | **残す**（出力を見ながら決める）        |
| Auto Save の方式            | Editor の工具列       | **Settings へ移した**                   |
| Auto Save の待ち時間        | 無かった              | Settings で初めて操作できるようになった |
| Terminal のさかのぼれる行数 | Terminal のタブ列の ⚙ | **Settings へ移した**                   |

残した2つは、どちらから変えても同じ setter を通り、片方で変えればもう片方にもその場で出る（二重に持っていない）。移した3つは**同じ設定を変える口を2つ残さない**ためで、残すと「どちらが効いているか」を利用者に確かめさせることになる。

**保存基盤は1行も作り替えていない。** IPC も preload の口も Main の検証も Session 4-3A のままで、Renderer から任意の JSON や任意のファイルパスを渡せる API は増えていない。Theme / Workspace 設定 / LSP・DAP は今回入れていない（下の表と、STEP 5 / STEP 6）。

### Session 4-3B の後へ残していること

| 項目                 | 現状                                                                                                                               |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Theme / Appearance   | **Session 4-4 で入った**（下記）。増えたのは `appearance` section 1つと key 1つだけ                                                |
| Workspace ごとの設定 | 未着手。プロジェクトフォルダの中には何も書かない方針のまま                                                                         |
| LSP / DAP の設定     | LSP は STEP 5 で入った。保存するのは**使うか / 使わないか**の4つだけで、実行ファイルのパスの欄は作っていない（§12）。DAP は STEP 6 |
| 設定の検索           | 置いていない。5項目しか無い                                                                                                        |
| 「既定へ戻す」       | 置いていない。範囲と既定は項目ごとの説明文に出ている                                                                               |
| Files のカラムの幅   | Settings には載せない。`FileColumns.tsx` の直接操作のまま（値は保存され続ける）                                                    |
| 設定のショートカット | **Session 4-7A で `Ctrl+,` が入った**（予告どおり、基盤と同時にした）                                                              |

### Session 4-4（完了）— Theme（Dark / Light）

塞いだのは**「色は1箇所に集めてある」が、本当は1箇所ではなかった**状態。色そのものは最初から `styles/theme.css` にまとめてあったが、Monaco と xterm は CSS 変数を読まないため、同じ 16進数がそれぞれの設定ファイルにも書き写されていた ── どちらにも「theme.css と同じ値を書き写しているので、片方を変えるときは両方を直すこと」という注意書きが付いていて、**その注意書きが要ること自体が写しの証拠**にほかならない。Theme が2つになれば写しは倍になる。

- **Dark / Light の2つだけ。** System（OS 追従）は入れていない ── あれは Theme の**名前**ではなく**選び方**で、足す場所が違う（将来の追加は名前の集合ではなく「何を選んでいるか」の側に入る）
- **色の実体は `theme.css` の1箇所。** Monaco も xterm も、そこから読んで**生成する**（`theme/themeTokens.ts`）。写しは1つも残っていない
- **Light は色をすべて設計し直した。** Dark を薄めたものではない（明度を反転しただけの色は白い面の上で軒並み読めなくなる）。**Dark の見え方は Session 4-3B までと1つも変えていない**
- **幕（scrim）と影は Theme ごとに別の値。** Dark の 45% の黒をそのまま Light で使うと、確認ダイアログの後ろが「奥が見えない黒い板」になる
- **起動時に Dark が一瞬も出ない。** 経路が3つあり（窓の初期色・CSS が最初に評価される時点・React の最初の描画）、どれも塞いだ
- **保存は Session 4-3A の基盤にそのまま乗った。** 増えたのは `appearance` section 1つと `theme` key 1つで、**IPC も Preload の口も Main の検証も CSP も1行も変わっていない**

**Settings の Appearance は末尾に置いた。** 前の3つ（Editor / Files / Terminal）が「その機能の見え方・振る舞い」であるのに対し、Appearance は**アプリ全体の見た目**にあたる ── 機能の並びの途中に挟むと、どの機能の話をしているのか分からない場所ができる。

**起動時のちらつきを消すのに、新しいチャンネルは1本も要らなかった。**

| 経路                       | 塞ぎ方                                                                 |
| -------------------------- | ---------------------------------------------------------------------- |
| Renderer が動く前の窓      | Main が保存済み Theme から `backgroundColor` を決める                  |
| CSS が最初に評価される時点 | Preload が `<html>` へ属性を当てる（`additionalArguments` で受け取る） |
| React の最初の描画         | 読み込みが返るまでの値を、その属性から読む                             |

Preload でやったのは、**CSP を1文字も緩めないため**にほかならない ── `index.html` にインラインの `<script>` を書けば同じことができるが、それは `script-src 'self'` を捨てることを意味する。IPC を使わなかったのは、IPC が Renderer の動き出しを待つものであり、避けたいのがまさに「動き出すまで」の一瞬だから。

**色だけに意味を持たせていない**という Session 3-6-6 / 3-8-2 からの前提は Light でも変えていない。ファイルの種類はまず絵の形が示し、Git の変更種別は記号（M / A / D …）が示していて、色はその補助にあたる ── そのうえで Light の27色すべてが面に対してコントラスト比 3:1 以上（本文の文字は 7:1 以上）であることを実機で確かめた。

### Session 4-4 の後へ残していること

| 項目                      | 現状                                                                                |
| ------------------------- | ----------------------------------------------------------------------------------- |
| System（OS 追従）         | 未着手。Theme の名前ではなく**選び方**なので、足す場所が違う                        |
| Theme 切り替えの打鍵      | **基盤は Session 4-7A で入ったが、Theme には割り当てていない**（下記 Session 4-7A） |
| Accent Color              | 置いていない。パネル識別色（§3）は設計の一部で、選ばせるものにしていない            |
| フォント / UI 密度 / Zoom | 未着手。Terminal の文字の大きさだけが既にある                                       |
| 構文ハイライトの配色      | 未着手。Monaco の標準テーマ（`vs` / `vs-dark`）を継承したまま                       |
| 高コントラストの Theme    | 未着手。Light は 3:1 以上を満たしているが、専用の Theme は持たない                  |
| Workspace ごとの Theme    | 持たない。Theme はその人の道具の形であって、プロジェクトの持ち物ではない            |

### Session 4-5A（完了）— Localization の土台と Language 設定

閉じたのは**文言がコードの中に散らばっていた状態**。Session 3-4 〜 4-4 の間、画面に出る言葉はそれを出す component の中に直接書かれていた。1言語しか無いあいだは問題として現れないが、**「この文字列は画面に出るのか、ログなのか、内部の識別子なのか」がコードの見た目では区別できない**という形で既に効いていた（`'M'` は画面に出る記号で、`'files'` は内部の id で、`'変更'` は文言にあたる）。

- **日本語（既定）と英語の2つだけ。** 3つめ以降は `LANGUAGE_IDS` に名前を1つと辞書を1つ足すだけで入る形にしてある
- **英語の辞書がキーの正本。** 翻訳キーの型をそこから導いてあるので、綴りを間違えた `t('setting.title')` は型エラーになる。日本語辞書との突き合わせは実数で見張る（片方だけに足すとテストが落ちる）
- **`t` は必ず引数で渡す。** 文言を組み立てる関数が内側に既定の言語を持つと、渡し忘れが「English の画面でも日本語が出る」という**画面にしか現れない不具合**になる ── 必須引数なら型が教えてくれる
- **押した瞬間に切り替わる。** 「適用」も「OK」も無く、`<html>` の属性・上部バー・各パネル・Settings 自身が同時に変わる
- **起動時に日本語が一瞬も出ない。** Theme（Session 4-4）と同じく、Main が保存済みの値を `additionalArguments` で渡し、Preload が最初の描画より前に `<html>` へ当てる ── **CSP を1文字も緩めていない**
- **保存は Session 4-3A の基盤にそのまま乗った。** 増えたのは `general` section 1つと `language` key 1つで、**IPC も Preload の口も Main の検証も1行も変わっていない**（Session 4-4 の `appearance` とまったく同じ入り方）

**Settings の General は先頭に置いた。** Appearance を末尾に置いたのと対になる判断で、機能ごとの設定（Editor / Files / Terminal）の前が「アプリ全体の話」の場所になる。

**言語名はその言語自身で書く**（英語の画面でも「日本語」と出る）。読めない言語で書かれた名前は選べないため ── VS Code や OS の言語設定と同じ作法にあたる。

### Session 4-5B（完了）— Files / Editor / Terminal の文言

Session 4-5A で入れた仕組みへ、**Files・Editor・Terminal と、その周りの確認ダイアログ**の文言を移した。ここが一番量の多い段で、扱ったのは表示される言葉だけではない。

- **失敗の分類と文言を切り分けた。** `filesError.ts` / `terminalError.ts` は「どういう失敗か」を判断する場所であって文言の置き場ではない ── 判断はそのまま残し、返すものを翻訳キーに変えた
- **検索の件数や上限の言い回し**のように、数を埋め込む文言も辞書側へ移した（`{count} 件` の組み立てはコードではなく辞書が持つ）
- **未保存の確認**（`lossMessage.ts`）も同じ形にした。何が失われるかを述べる文で、**失われるものの種類が増えたときに文言だけを直せる**ようにするため

移していないものが2つある。**Monaco の UI と xterm の出力**で、どちらも自前の文言を持つが、あれは**利用者の文書と端末の出力**を出す器であってアプリの文言ではない（言語を切り替えても変わらない）。

### Session 4-5C（完了）— Git の文言

Git は文言の量でも種類でも他と違う。変更ファイルの一覧・ブランチ・履歴・退避・remote・競合・公開と面が多く、しかも**その多くが「今どういう状態か」を説明する文**にあたる（「Commit するには、変更を Stage してください」「まだ Remote がありません。下の欄に名前と URL を入れると…」）。

- **状態 → 文言の関数はすべて `t` を受け取る形にした。** `gitChanges.ts` / `gitBranches.ts` / `gitHistory.ts` / `gitRemotes.ts` / `gitStash.ts` / `gitDiff.ts` / `gitInProgress.ts` などがそれにあたり、**判断のロジックは1つも変えていない**
- **記号は文言にしない。** 変更種別の `M` / `A` / `D` は言語に依らない印で、色だけに意味を持たせない（Session 3-6-6 / 3-8-2）という前提のもとで**記号の側が意味を担っている** ── 訳すと、その前提ごと壊れる
- **生の `stderr` を訳さない。** Git の失敗は分類してから文言にする（Session 3-8-1 からの線）ので、訳す対象は分類の結果であって git の出力ではない

これで STEP 3 で作った Git のすべての面が2言語で出るようになった。Session 4-8A で **History / Stash / Remote / Branch / Diff の5面を実際に開いて**確かめている。

### Session 4-5 の後へ残していること

| 項目                       | 現状                                                                                                                             |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 3つめ以降の言語            | 未着手。足すのは名前1つと辞書1つで、仕組みは変わらない                                                                           |
| System（OS の言語に追従）  | 未着手。Theme の System と同じで、**言語の名前ではなく選び方**なので足す場所が違う                                               |
| 複数形・語順の切り替え     | 持たない。辞書を引いて `{name}` を差し替えるだけ                                                                                 |
| 日付 / 数値の地域化        | 持たない。Git の履歴の日時は今までどおりの組み立て                                                                               |
| Monaco / xterm の UI 言語  | 変えない。利用者の文書と端末の出力を出す器であって、アプリの文言ではない                                                         |
| ネイティブダイアログの言語 | 変えない。OS が決める                                                                                                            |
| Terminal の `＋` の字面    | 全角のまま。`aria-label` / `title` は両言語とも訳されているが、**ボタンの字面だけが記号**として残っている（Session 4-8A で記録） |

### Session 4-7A（完了）— Command とショートカットの土台

閉じたのは**打鍵がコードのあちこちで直接 window に張られていた状態**。Session 3-5 の時点で `Ctrl+S`（保存）が `useEditorSession.ts` の中から `window` に listener を張っており、Session 4-2・4-3B・4-4 では「ショートカットを置きたいが、基盤が無いので置かない」と繰り返し先送りしていた ── **先に1つだけ生やすと、入口が2通りになる**ためにほかならない。

- **「そういう操作がある」と「今それを実行できるか」を分けた。** 前者は静的な表（Command Registry）、後者は所有者が mount している間だけ handler を載せる形。Git の操作は Git パネルが開いている間しか存在しないので、静的な表に書くことがそもそもできない
- **command の名前は閉じた集合。** 任意の文字列を command として実行する経路は存在しない（`execute(id: string)` のような汎用の入口を作らない）── 作った瞬間「アプリが持つ操作」という限定が消える
- **一度決めた id は変えない。** 将来ユーザーの割り当てがディスクに残るため、id を変えると設定した打鍵が静かに効かなくなる
- **打鍵は物理キーの位置で見る。** `event.key`（入力される文字）ではなく `event.code`（キーの位置）を基準にしてあるので、**US 配列でも日本語配列でも同じキーで効く**
- **既定の割り当ては7つだけ。** 既にある打鍵を1つも変えず、Monaco が使っている打鍵を取らず、端末を触っている最中はアプリ側の操作を走らせない、という3つの規則で選んだ
- **確認ダイアログが出ている間は、原則として何も走らない。** これが移設前の `Ctrl+S` からの唯一の振る舞いの変化にあたる

**既定の7つ**（`Ctrl+S` 保存 / `Ctrl+Shift+S` 別名で保存 / `Ctrl+O` フォルダを開く / `Ctrl+,` 設定 / `Ctrl+Shift+E` Files / `Ctrl+Shift+G` Git / `Ctrl+J` Terminal）。

`` Ctrl+` `` は入れていない ── あの位置は日本語配列では半角/全角キーで、IME の切り替えとぶつかる。Terminal の開閉は `Ctrl+J` に置いた。`Ctrl+P` / `Ctrl+Shift+P` は**空けてある**（Command Palette の席）。

**新しい IPC も保存先も1つも作っていない。** 打鍵と command は Renderer の中で完結する。

### Session 4-7B（完了）— Git / Files の操作を command として名乗る

Session 4-7A の表には Workspace / Editor / View / Settings の11件しか無かった。**パネルの中の操作が1つも入っていない**状態で、これは基盤の限界ではなく「所有者が mount している間だけ登録する」形をまだ試していなかっただけにあたる。

- Git の7件（再取得 / Commit / Push / Pull / Fetch / 履歴を開く / 退避の面を開く）と Files の3件（再読み込み / 名前で検索 / 中身で検索）が入り、**21件になった**
- **押せる条件をここで書き直さない。** 渡ってくるのは画面のボタンを押せなくしているのと同じ値で、判断が1つも無い ── 書き直すと「押しても必ず失敗する操作」が command として実行できることになる
- **使えないときは登録しない。** リポジトリでないフォルダでは Git の command そのものが存在せず、パネルを閉じていても同じ。条件を1つ増やす代わりにこれで同じ効果が出る

**この10件には打鍵を割り当てていない。** 名乗るところまでで、押す道は各パネルの UI のままにしてある ── 打鍵を足すと Monaco と端末の既定に踏み込むことになり、それは既定を選び直す作業（Session 4-7A の規則）に戻る。

`terminal` カテゴリは作っていない。端末の `Ctrl + ± / 0` は Session 3-7-3 から `event.key` で判定しており、移すには日本語配列の読み替えごと設計し直すことになる。

### Session 4-7C（完了）— Keyboard Shortcuts の一覧

塞いだのは**「どのキーが何をするのか、どこにも書いていない」**状態。既定を7つ決めても、それを知る方法がソースを読むこと以外に無かった。

- Settings の**6つめのカテゴリ**として入った。**値を持たない最初のカテゴリ**で、対応する保存 section は無い（保存するものが1つも無い）
- 一覧は目録に書き写さず、**Command Registry と効いている割り当てからその場で組み立てる** ── 書き写すと command を足すたびに2箇所を直すことになる
- **21件すべてに画面用の名前を入れた。** 一部だけ埋めると「翻訳されるものとされないものが混ざった一覧」になり、その半端さを画面が引き継ぐ
- カテゴリは列ではなく**見出しに畳んだ**（Git だけで7件あり、列にすると同じ語が7回並ぶ）
- **一覧は所有者の生死に関係ない。** Git パネルを閉じていても Git の7件は並ぶ ── ここが出すのは「アプリが持つ操作の全体」であって、その瞬間に実行できるものではない

**v1 は読むだけ。** 行を押しても command は実行されない。打鍵の変更・User / Workspace の割り当て・`keybindings.json` の永続化はどれも入っておらず、実行の入口は打鍵と各パネルの UI のままにしてある。

### Session 4-7 の後へ残していること

| 項目                             | 現状                                                                            |
| -------------------------------- | ------------------------------------------------------------------------------- |
| 打鍵の編集                       | 未着手。一覧は読むだけ                                                          |
| User / Workspace の割り当て      | 未着手。解決の仕組みは**連結の順番だけで受けられる**形にしてある                |
| `keybindings.json` の永続化      | 未着手。**保存先そのものが存在しない**（作る経路も無い）                        |
| Command Palette                  | 未着手。`Ctrl+P` / `Ctrl+Shift+P` を空けてある                                  |
| Source / When / 競合の表示       | 出していない。値が1種類しか無いか、内部の識別子だから                           |
| `terminal` カテゴリの command    | 未着手。日本語配列の読み替えごと設計し直すことになる                            |
| 和音（`Ctrl+K Ctrl+S` のような） | 持たない。打鍵1つだけを表せる                                                   |
| メニューバーへの打鍵の表示       | 未着手                                                                          |
| Theme 切り替えの打鍵             | 割り当てていない。Dark / Light の2つしか無く、Settings の Appearance から変える |

### Session 4-8A（完了）— production app での統合確認

Session 4-5 と 4-7 は、**実装は済んでいたが production app で通していなかった**。この Session で塞いだのはそこにほかならない ── 打鍵まわりは特に、DOM だけのテストでは確かめられないことが多い（Monaco も xterm も載っていないため）。

**205項目、全項目 PASS**（Localization 94 / Keyboard Shortcuts 111）。

実機でしか確かめられなかったもの:

- **端末に focus があるとき `Ctrl+S` が `window` まで上がってこない**こと（Session 4-7A が前提にしていたことの実測）。`window` に probe を仕込み、端末では届かず・Editor では届くことを対照で確認した
- **`Ctrl+F` / `Ctrl+H` / `Ctrl+/` が Monaco の機能として動く**こと（打鍵を奪っていないこと）
- 端末で `Ctrl+,` / `Ctrl+J` / `Ctrl+O` / `Ctrl+Shift+E` / `Ctrl+Shift+G` が**1つも走らない**こと
- **`Ctrl+P` / `Ctrl+Shift+P` が完全な無反応**であること（席が空いたままであること）
- 確認ダイアログが出ている間、その裏で打鍵が**1つも走らない**こと
- **起動時に日本語が一瞬も出ない**こと（`dom-ready` の時点で既に切り替わっている）
- Git の5面（History / Stash / Remote / Branch / Diff）を実際に開いて、日本語が残っていないこと

**見つかった不具合は1つ。** Editor のタブの説明（tooltip）で、状態の一言は訳されているのに**それを囲む括弧だけがコードに全角で直書き**されていた ── English の画面に `alpha.txt（Unsaved）` と出ていた。Terminal のタブが同じことを翻訳の側でやっているのに、Editor だけがコードで組み立てていた形で、同じ作法に揃えた（**直したのはこの1件だけ**）。

**セキュリティ境界は1つも動いていない。** `window.fluvix` の名前空間は10個のまま（`language` も `keybindings` も増えていない）、設定の口は2つのまま、CSP は1文字も変わっていない。`keybindings.json` は作られておらず、Workspace フォルダにも何も書かれない。

### Session 4-8A の後へ残していること

| 項目                            | 現状                                                                                                                                             |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Terminal の新規ボタンの `＋`    | 全角のまま。`aria-label` / `title` は訳されているが、**英語の案内文が半角の `+` を指している**ので食い違っている ── 字面の判断なので触っていない |
| `PanelPlaceholder.tsx`          | **どこからも参照されていない**（Session 2-2 の名残）。文言も訳されていないが、画面に出ないので触っていない                                       |
| Monaco / xterm 側の打鍵の一覧化 | していない。一覧に出るのはアプリが持つ command だけで、Monaco の既定は出ない                                                                     |

### Session 4-8B（完了）— ドキュメントの追従

Session 4-8B では、STEP 4 の実装結果を [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) と [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) へ追従した。Localization と Keyboard Shortcuts の構造、production app での確認結果、セキュリティ境界、未実装項目を「まだやっていない欠陥」ではなく**この段で入れないと決めた範囲**として記録している。

- Localization は `TranslationKey` / `t` / `LanguageProvider` / `language` 設定の構造として記録済み
- Command Registry と Keyboard Shortcut Registry は Renderer 内の閉じた操作表として記録済み
- Keyboard Shortcuts は読むだけの Settings カテゴリであり、保存 section を持たないことを記録済み
- Session 4-8A の production verification は **205項目、全項目 PASS**（Localization 94 / Keyboard Shortcuts 111）として記録済み
- `window.fluvix` の名前空間、IPC、CSP、`keybindings.json` 非作成、Workspace へ書き込まない方針を記録済み

### Session 4-8C（完了）— STEP 4 Closing

この Session は Closing であり、**LSP 実装・ショートカット編集・Command Palette・`keybindings.json`・terminal command category は入れていない**。STEP 4 の目的は「LSP の前に日常使用の土台を揃える」ことであり、以下の実装と検証が揃った時点で完了とする。

| 範囲                                 | STEP 4 で閉じたこと                                                                                   |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| Save As / deleted file recovery      | 削除されたタブを内容ごと救える別名保存、通常タブからの別名保存、未保存印の消失不具合の修正            |
| unified Settings persistence         | `%APPDATA%/Fluvix Nexus/settings.json` への section 単位保存、旧3ファイルからの移行、壊れた値の切離し |
| Settings UI                          | Editor / Files / Terminal / Appearance / General / Keyboard Shortcuts を同じ面へ整理                  |
| Theme / Appearance                   | Dark / Light、起動時のちらつき防止、Monaco / xterm の theme token 生成                                |
| Localization foundation              | 日本語 / English、初期描画前の language 適用、辞書と翻訳キーの型安全化                                |
| Files / Editor / Terminal            | Files・Editor・Terminal と確認ダイアログの文言を翻訳キーへ移動                                        |
| Git Localization                     | Changes / Branch / History / Stash / Remote / Diff / in-progress 系の文言を翻訳キーへ移動             |
| Command Registry                     | アプリが持つ操作を閉じた id 集合として表し、handler は所有者が mount している間だけ登録               |
| Keyboard Shortcut Registry           | 既定7件の打鍵、物理キー基準、確認ダイアログ中の停止、Monaco / Terminal との境界                       |
| Git / Files command contribution     | Git 7件、Files 3件を command として追加し、押せる条件は既存 UI の状態を再利用                         |
| Keyboard Shortcuts Settings UI       | 21件の command と既定打鍵を Settings から読める一覧にした                                             |
| Session 4-8A production verification | production app で Localization 94 / Keyboard Shortcuts 111、合計205項目 PASS                          |

STEP 4 で意図的に入れていないもの:

| 項目                                | Closing の判断                                                                                                                      |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| ショートカット編集                  | 一覧は読むだけ。編集を入れるには競合検出・保存先・初期値への戻し方を同時に決める必要があるため、STEP 4 の範囲ではない               |
| User / Workspace shortcut override  | Registry は将来の連結順で受けられる形だが、現時点ではアプリ既定だけ。Workspace に設定ファイルを書かない方針を保つ                   |
| `keybindings.json` persistence      | 保存先も IPC も作っていない。任意の keybinding ファイルを読み書きする設計は STEP 5 以降で必要になった時点で閉じてから入れる         |
| Command Palette                     | `Ctrl+P` / `Ctrl+Shift+P` は空けてある。実行対象の検索・表示・disabled 理由・危険操作の扱いを別途設計する                           |
| `terminal` command category         | 端末の既存 `Ctrl + ± / 0` は xterm 入力と日本語配列の読み替えを含むため、Command Registry へ急いで移さない                          |
| Source 列表示                       | v1 は既定しか無いため表示しても同じ値が並ぶ。User / Workspace override を入れる段で Source / When / conflict と一緒に扱う           |
| Monaco / xterm 側の既存打鍵の一覧化 | FluvixNexus Command Registry はアプリ自身の操作だけを扱う。Monaco の検索・置換・コメント切替や xterm の入力規則はそれぞれの器が持つ |

STEP 4 Closing 時点の既知事項:

| 項目                              | 扱い                                                                                                                                        |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| TerminalTabs の新規ボタン `＋`    | `aria-label` / `title` は localized。字面だけが全角記号で残っている。表示ポリッシュ候補であり、STEP 4 の機能欠陥ではない                    |
| `PanelPlaceholder.tsx`            | どこからも参照されていない Session 2-2 の名残。未参照コードの掃除候補であり、画面に出ないため Localization 欠陥ではない                     |
| Monaco / xterm と Registry の境界 | Monaco / xterm の既存打鍵は FluvixNexus command ではない。アプリ横断操作だけを Registry に載せる現在の境界を維持する                        |
| terminal command category         | 未導入。端末固有の入力処理を command 化するなら、日本語配列・xterm 入力・端末 focus 中の横取り禁止を改めて設計する                          |
| Source 列表示                     | 未表示。今は default しか存在しないため、override を持つ段で Source / When / conflict と一緒に導入する                                      |
| 古い Session 4-7 予定記述         | 4-8B と 4-8C で追従し、STEP 4 の表と本文は完了状態へ更新済み                                                                                |
| Session 4-6 相当の欠落            | 4-6 は採番上の欠番。Localization（4-5）の次に Command / Keyboard Shortcuts（4-7）へ進んだだけで、飛ばした実装範囲は無いことを本文に記録済み |

STEP 4 を Close するための必須条件:

| 条件                                     | 状態                                                                                   |
| ---------------------------------------- | -------------------------------------------------------------------------------------- |
| STEP 4 の全実装 Session が main にある   | Session 4-2 から 4-8B まで Commit / Push 済み                                          |
| production app で統合確認済み            | Session 4-8A で Localization 94 / Keyboard Shortcuts 111、合計205項目 PASS             |
| セキュリティ境界が維持されている         | Main / preload / Renderer / IPC / CSP に STEP 4 Closing で変更なし                     |
| 意図的未実装項目が後続項目として明記済み | ショートカット編集、override、`keybindings.json`、Command Palette などを上表で明記済み |
| STEP 5 へ渡す設計境界が明記済み          | 下記の LSP 引き継ぎにまとめる                                                          |

STEP 5（LSP）への引き継ぎ:

| 項目                      | STEP 5 で守ること                                                                                                                                        |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Main / preload / Renderer | LSP server の起動・停止・stdio 管理は Main が持つ。Renderer は任意の executable path / args / cwd を渡さず、閉じた language id や workspace 情報だけ渡す |
| IPC                       | 汎用 execute API ではなく、型付きの LSP 用契約を作る。長命プロセスの通知は Session 3-3 以降の Main → Renderer イベント経路に載せる                       |
| Settings architecture     | LSP 設定を足す場合も、値が決まった section / key だけを `settings.json` に追加する。空のカテゴリや先行する保存口は作らない                               |
| Localization architecture | Renderer に出る文言は翻訳キー化する。Main が返すのは分類可能な失敗種別や machine-readable な状態で、UI 文言を Main に埋め込まない                        |
| Command Registry          | Go to Definition / Rename / Format などを command にする場合も、id は閉じた集合にし、handler は Editor / LSP の準備がある間だけ登録する                  |
| Keybinding Registry       | LSP 由来の既定打鍵を足す場合は Monaco 既定・Terminal focus・Command Palette 予約キーとの衝突を先に確認する。編集や override は別設計のまま               |
| executable path の扱い    | Renderer 任意文字列を Main に実行させない。検出済み候補、固定の adapter、または Main 側で検証済みの設定だけを使う                                        |
| Monaco との境界           | Monaco の基本編集打鍵は引き続き Monaco が持つ。FluvixNexus command はアプリ横断または LSP 連携として明示した操作だけを扱う                               |

以上により、STEP 4 は「日常使用の土台」として正式に完了し、STEP 5 は LSP の process / IPC / settings / command 設計から開始する。

---

## 12. STEP 5（完了）— Language Server（LSP）

STEP 4 が「LSP の前に日常使用の土台を揃える」段だったのに対し、STEP 5 は **Monaco が持っていた「編集する器」に、その言語を本当に理解しているプログラムを繋ぐ**段になる。§4「コードエディタ」の一覧のうち、Session 3-4 の時点で残っていた**コード補完・エラー表示・Go to Definition・Rename・フォーマット**がここで揃った。

構造は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §19 に、確認の手順は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §1・§4 に書いてある。

### Session 5-1 〜 5-12（完了）— 実装

| Session | 入れたもの                                                           |
| ------- | -------------------------------------------------------------------- |
| 5-1     | プロセスの起動 / 停止、stdio 上の JSON-RPC、restart policy           |
| 5-2     | Document Synchronization（didOpen / didChange / didSave / didClose） |
| 5-3     | Diagnostics（push）と Monaco marker                                  |
| 5-4     | Settings（使う / 使わない）と Status Bar                             |
| 5-5     | Completion                                                           |
| 5-6     | Hover                                                                |
| 5-7     | Go to Definition / Find References                                   |
| 5-8     | Formatting                                                           |
| 5-9     | Rename / prepareRename                                               |
| 5-10    | Python（Pyright）と、サーバの capability を読む仕組み                |
| 5-11    | C#（csharp-ls）                                                      |
| 5-12    | Command / Keybinding からの呼び出し                                  |

対応言語と繋ぐ相手:

| 言語                    | Language Server            | 入手経路                  |
| ----------------------- | -------------------------- | ------------------------- |
| TypeScript / JavaScript | typescript-language-server | npm（グローバル）         |
| Python                  | pyright-langserver         | npm（グローバル）         |
| C#                      | csharp-ls                  | dotnet tool（グローバル） |

**どれもアプリに同梱しない**（§5 の方針どおり）。入っていなければその言語だけが使えない状態に留まり、アプリが起動しない理由にはしない。

### Session 5-13（完了）— STEP 5 Closing

この Session は Closing であり、**新しい LSP 機能は1つも入れていない**。production build の実アプリで STEP 5 全体を横断確認し、ドキュメントを追従させ、既知の制約を記録した。

Production Verification: **66項目、全項目 PASS**（5本のスクリプト。docs/DEVELOPMENT.md §4）。

| 範囲                                           | 項目 |
| ---------------------------------------------- | ---: |
| TypeScript / JavaScript / Python の全機能      |   24 |
| C#（csharp-ls）                                |   11 |
| Settings / Status / lifecycle / orphan process |   14 |
| Security boundary                              |   11 |
| 既知の制約の確認                               |    6 |

STEP 5 で閉じたこと:

| 範囲                     | STEP 5 で閉じたこと                                                                                     |
| ------------------------ | ------------------------------------------------------------------------------------------------------- |
| プロセスと JSON-RPC      | Main が起動 / 停止 / stdio を持つ。実行ファイルは PATH から解決した絶対パスで、Workspace の中は探さない |
| Document Synchronization | Monaco の Model が正本。版を添えて要求し、食い違えば `stale` を返す                                     |
| Diagnostics              | push 型の `publishDiagnostics` を Monaco の marker へ。内蔵の検査とは owner を分ける                    |
| 7つの言語機能            | Completion / Hover / Definition / References / Formatting / Rename / prepareRename                      |
| capability negotiation   | `initialize` の応答を読み、名乗っていない機能は要求そのものを出さない                                   |
| 3言語                    | TypeScript / JavaScript・Python・C# が同じ Workspace で同時に動く                                       |
| fallback                 | TS / JS だけが Monaco 内蔵へ落ちる。`.py` / `.cs` は落とさない                                          |
| Settings / Status        | 全体と言語ごとの ON / OFF、6状態の表示、OFF でプロセスが止まり ON で戻る                                |
| lifecycle                | 立て直しは窓の中で3回まで。Workspace 切り替え・終了で orphan process が残らない                         |
| Command / Keybinding     | 6操作を Command Registry へ。F12 / Shift+F12 / F2 / Shift+Alt+F / Ctrl+Space                            |
| Security boundary        | 絶対パス・file URI・実行ファイル・引数・cwd・任意の method・任意のサーバ選択のいずれも渡さない          |

STEP 5 で意図的に入れていないもの:

| 項目                             | Closing の判断                                                                                                     |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| Problems panel                   | 診断は marker として出るだけ。一覧の面は「どこまで出すか」「押したらどう飛ぶか」を別に設計する                     |
| Format on Save                   | 保存の経路（§10.3 の Auto Save）と整形を同時に触る話になる。保存が勝手に中身を変える形は、確認の設計と一緒に入れる |
| Code Action / Quick Fix          | `codeActionProvider` を名乗っていない。編集を当てる経路（`workspace/applyEdit`）の一般対応と対になる               |
| Signature Help                   | 入れていない。Completion の次に来る候補だが、STEP 5 の範囲ではない                                                 |
| Semantic Tokens                  | 入れていない。色は Monaco の文法定義のまま                                                                         |
| Inlay Hints                      | 入れていない                                                                                                       |
| Workspace Symbol                 | 入れていない。探すのは Files の検索                                                                                |
| `workspace/applyEdit` の一般対応 | Rename の応答だけが編集を持ち、その形も `TextEdit` に限ってある。ファイルの作成 / 改名 / 削除を表す欄が無い        |
| executable path の設定           | 設定に欄を作らない。作った時点で「利用者が指定した実行ファイルが起動する場所」になる                               |
| workspace-local server 起動      | Workspace の中は探さない。clone したリポジトリの中の実行ファイルが動く形にしない                                   |
| Workspace 外のファイルを開く     | 定義の行き先が Workspace の外なら開かない                                                                          |
| file watching                    | 入れていない（下記の既知制約）。監視対象・間引き・Workspace の外へ出ない保証を同時に設計する話になる               |
| Python の整形器（Black / Ruff）  | 入れていない。言語サーバを1本足す作業に、別系統の外部ツールを増やす判断を混ぜない                                  |
| DAP                              | STEP 6。LSP とは別のプロセス系統として設計する                                                                     |

STEP 5 Closing 時点の既知事項:

| 項目                        | 扱い                                                                                                                                                                                                                                                                                                        |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pyright-no-file-watching`  | **Pyright は、エディタで開いていないファイルのディスク上の変更に気づかない。** このクライアントが `client/registerCapability` を拒否しており、`workspace/didChangeWatchedFiles` を登録できないため。サーバを立て直すと反映される。tsserver は自前の watcher を持つため出ない（docs/ARCHITECTURE.md §19.10） |
| Python の Formatting        | Pyright が `documentFormattingProvider` を出さないため「利用不可」が正しい挙動。Shift+Alt+F は何も起こさず、内蔵の TypeScript 整形へも落ちない                                                                                                                                                              |
| C# の確認環境               | この PC には .NET SDK も csharp-ls も入っていない。一時ディレクトリへ置き、起動する Electron の `env` にだけ PATH を足して測っている。システム側は変更していない                                                                                                                                            |
| このリポジトリ自身と TS LSP | `typescript` を 7.x で pin しているため `tsserver.js` が無く、自分自身を Workspace にすると TS サーバは立たない。アプリの不具合ではない（docs/DEVELOPMENT.md §1）                                                                                                                                           |
| 内蔵の意味解析              | Monaco 内蔵 TypeScript の semantic validation は STEP 3 から切ったまま。tsconfig も node_modules も見えない状態の型エラーはほぼ本当ではないため                                                                                                                                                             |

STEP 5 を Close するための必須条件:

| 条件                                     | 状態                                                                |
| ---------------------------------------- | ------------------------------------------------------------------- |
| STEP 5 の全実装 Session が main にある   | Session 5-1 から 5-12 まで Commit / Push 済み                       |
| TS / JS の主要 LSP 機能が動く            | Production Verification スクリプト1（24項目）で確認                 |
| Python の主要 LSP 機能が動く             | 同上                                                                |
| C# の主要 LSP 機能が動く                 | スクリプト2（11項目）で確認                                         |
| Python Formatting 非対応が正しく扱われる | 何も起きず、TypeScript として整形もされないことを確認               |
| no-server fallback が維持されている      | LSP OFF で TS は内蔵へ落ち、`.py` は落ちないことを確認              |
| lifecycle と orphan process              | スクリプト3（14項目）で確認                                         |
| Security boundary と CSP                 | スクリプト4（11項目）で確認。CSP は STEP 1 から1文字も変えていない  |
| Settings / Status が実際の状態と一致する | スクリプト3 で、表示とプロセスの有無を突き合わせて確認              |
| Command / Keybinding が動く              | 3言語で F12 / Shift+F12 / F2 / Shift+Alt+F / Ctrl+Space を確認      |
| `npm test` / `npm run build`             | 189 files・3447 passed・1 skipped / build 成功                      |
| ドキュメント更新                         | DESIGN.md §12、docs/ARCHITECTURE.md §19、docs/DEVELOPMENT.md §1・§4 |
| 既知の制約の記録                         | 上表と docs/ARCHITECTURE.md §19.10                                  |

STEP 6 以降への引き継ぎ:

| 項目                       | 次の段で守ること                                                                                                                  |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 長命プロセスの持ち主       | Terminal（STEP 3）と LSP（STEP 5）で同じ形になった。DAP（STEP 6）も Main が持つ ── Renderer に executable / args / cwd を渡さない |
| 編集を当てる経路           | `workspace/applyEdit` の一般対応を入れるなら、ファイルの作成 / 改名 / 削除まで表せる形と、それを断る境界を先に決める              |
| 診断の面（Problems panel） | 出す元は既にある（`diagnosticStore`）。面を作るときは「どこまで出すか」と「押したらどう飛ぶか」を Files の検索結果と揃える        |
| file watching              | 入れるなら Files の監視（§10.5）と1本にする。LSP のためだけにもう1系統の監視を作らない                                            |
| Command Palette            | `Ctrl+P` / `Ctrl+Shift+P` は依然として空いている。LSP の6操作は descriptor を持つので、Palette 側は表を読むだけで済む             |

以上により、STEP 5 は「Language Server 統合」として正式に完了する。

---

## 13. STEP 6（進行中）— Debug（DAP）

STEP 5 が「編集する器に、その言語を理解しているプログラムを繋ぐ」段だったのに対し、STEP 6 は **書いたコードを実際に走らせて、止めて、中を見る**段になる。§5 の11機能がここで揃う。

構造は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20 に書いてある。

### Session 6-0（完了）— 設計確定

**コードは1行も書いていない。** STEP 6 全体の設計判断を確定し、ドキュメントへ反映しただけの Session になる。

LSP には無くて DAP にだけある問題が1つあり、それがこの Session の全体になった ── **「何を起動するか」は利用者にしか決められない**。LSP は開いた文書の拡張子だけで行き先が決まったため、実行ファイル・引数・cwd を Renderer に一切見せずに済んだ（§12）。デバッグで同じことをすると道具にならず、かといってそのまま欄を作れば「Renderer が指定した実行ファイルを Main が起動する場所」になる。

決めた線は1行に収まる。

> **プログラムが何をするかを変えるものは Renderer、何のプログラムが動くかを変えるものは Main。**

確定した10項目:

| #   | 論点                                  | 決定                                                                                               |
| --- | ------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 1   | `.vscode/launch.json`                 | **v1 では読まない。** Workspace の中のファイルが実行対象を決める形にしない                         |
| 2   | 独自 Debug Profile                    | **採用する。** アプリの言葉だけで書かれた7つの欄に限る                                             |
| 3   | 保存の単位                            | **Workspace ごと**                                                                                 |
| 4   | 保存先                                | **Main が持つ `userData` 配下の `debug-profiles.json` 1つ。**Workspace の絶対パスで引く            |
| 5   | Renderer へ返す形                     | **正規化する。**返るのは常に現在の Workspace の profile だけで、key を渡す口が無い                 |
| 6   | 解決済みの launch 構成                | **型ごと分ける。**`ResolvedLaunchConfiguration` は `main/` に置き、`shared/` にも IPC にも出さない |
| 7   | Workspace 外の program                | **断る。**検証は Files と同じ2段（パス文字列 → realpath）                                          |
| 8   | 環境変数                              | **欄は持つが、名前を検査する。**「プログラムより先に何かを読み込ませる」名前は断る                 |
| 9   | プログラムへの引数                    | **持つ。**shell を通さないので、語の分割も展開も起きない                                           |
| 10  | 任意の command / shell / 実行ファイル | **指定できない。**弾くのではなく、欄そのものを作らない                                             |

このうち 8 だけが「欄を作らない」ではなく「形を検査する」を選んだ場所になる。理由は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20.9。

**adapter の逆方向要求 `runInTerminal` は拒否する。** adapter が「このコマンドラインを実行してくれ」と client に頼む経路で、受ければ起動するプロセスの argv を adapter が決められる ── STEP 5 で `client/registerCapability` を拒否したのと同じ性格になる。代償（本物の端末を必要とするプログラムは Debug Console の中では同じように動かない）は制約として記録してある。

### Session 6-1（完了）— DAP Wire Protocol / Adapter Process Foundation

STEP 6 の最下層として、Main 内に `main/debug/` を追加した。入れたのは **Debug Adapter と安全に通信するための最小基盤**だけで、Renderer / preload IPC、Debug Profile UI、`initialize` / `launch` の状態機械、実 adapter 統合はまだ無い。

実装の分割:

| ファイル                       | 役割                                                                  |
| ------------------------------ | --------------------------------------------------------------------- |
| `main/debug/dapMessage.ts`     | DAP message 型、runtime validation、Content-Length framing / parser   |
| `main/debug/dapConnection.ts`  | request seq 管理、response correlation、Event 受信、pending cleanup   |
| `main/debug/adapterProcess.ts` | spawn、stdio 接続、stderr、error / close / dispose、orphan prevention |
| `main/debug/adapterCatalog.ts` | Main 内部の固定 catalog。3言語を閉じた集合として持つ                  |

Catalog は `node` / `python` / `csharp` の行を持つが、Session 6-1 ではすべて `not-integrated` として記録している。`debugpy` / `vscode-js-debug` / `netcoredbg` の実接続は後続 Session の仕事であり、6-1 では任意 adapter 選択や任意 process spawn の口を作っていない。

`dapConnection` は adapter からの逆方向 request（`runInTerminal` を含む）を Main の任意実行にはつなげず、DAP の失敗 response を返す。これにより adapter を待たせず、Session 6-0 の Security boundary を保つ。

### Session 6-2（完了）— Debug Session Lifecycle / State Machine

Session 6-1 の DAP wire / adapter process foundation の上に、Main-owned の `debugSessionManager` と純粋な `debugSessionState` を追加した。Renderer / preload IPC、Debug Profile UI、Breakpoint、Continue / Step、Variables、実 adapter 統合はまだ無い。

入れたもの:

| ファイル                            | 役割                                                                                           |
| ----------------------------------- | ---------------------------------------------------------------------------------------------- |
| `main/debug/debugSessionState.ts`   | `idle` / `starting` / `running` / `stopped` / `terminating` の許可遷移を固定する純粋な状態機械 |
| `main/debug/debugSessionManager.ts` | current session、generation、adapter process、DAP lifecycle、stop / dispose / cleanup を所有   |
| `main/app/lifecycle.ts`             | Workspace 切り替えと app quit で Debug Session を片付ける                                      |
| `main/debug/debugSession*.test.ts`  | 状態遷移、単一セッション制限、stale generation、pending cleanup、orphan prevention の固定      |

Lifecycle は `initialize` の capability を読み、`launch` を送った後、`initialized` event を合図に `configurationDone`（adapter が対応を名乗る場合のみ）を送って `running` へ進む。**`launch` の応答を開始の合図にはしない。**

古い adapter から遅れて届いた event / error / close は、session generation が現在のものと一致するときだけ扱う。stop / dispose / Workspace switch / app quit / start failure / adapter error / adapter exit は同じ冪等 cleanup path に収束し、DAP connection の pending request も閉じる。

### Session 6-3（完了）— Breakpoint

STEP 6 で**初めて Renderer に面が出た** Session になる。6-1 / 6-2 は Main の中だけで閉じていたが、ここで Monaco・preload・IPC・保存の4つが一度に繋がった。

入れたもの:

| ファイル                             | 役割                                                                 |
| ------------------------------------ | -------------------------------------------------------------------- |
| `shared/debug/breakpoint.ts`         | Renderer が扱う4欄（相対位置 / 行 / enabled / verified）と純粋な判断 |
| `shared/debug/breakpointDocument.ts` | 保存形式（Workspace の絶対パスで引く JSON）                          |
| `main/debug/breakpointModel.ts`      | 入れ替え・重複・並び・adapter の答えの当て方（純粋）                 |
| `main/debug/breakpointSource.ts`     | 相対位置 → DAP の `Source`（2段の検証。Workspace の外なら null）     |
| `main/debug/dapBreakpoints.ts`       | `setBreakpoints` 1往復の形と応答の読み取り                           |
| `main/debug/breakpointSync.ts`       | 1ファイルぶんの送信と、控えへの反映                                  |
| `main/debug/breakpoints.ts`          | 正本・保存・通知・セッションとの同期                                 |
| `main/store/debugBreakpoints*.ts`    | `userData/debug-breakpoints.json` と、その検証                       |
| `renderer/src/debug/`                | 今の Workspace の写しを配る器                                        |
| `renderer/src/editor/debug/`         | glyph margin の印（**Monaco を import しない**構造的部分型）         |

**v1 は行 breakpoint だけ。** 条件付き・hit count・logpoint・function・data は STEP 6 の範囲外のまま（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20.11）。

決めたことのうち、設計から動いたものが2つある。

| 論点                | Session 6-0 の予定                     | Session 6-3 の実装                                                                                 |
| ------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------- |
| breakpoint の永続化 | **入れない**（アプリを閉じると消える） | **入れた。**予告どおり §20.5 と同じ形（userData の JSON 1つを Workspace の絶対パスで引く）に載せた |
| 切る口              | `setBreakpoints` 1つ（要求17）         | `listBreakpoints` / `toggleBreakpoint` の2つ（要求18）。**一覧を丸ごと渡す口を作らない**ため       |

Renderer へ返さないものが2つ増えた ── adapter が返した**文言**（絶対パスを含みうる）と、adapter が**動かした行**（印の位置を動かすと、押した行と印がずれて外せなくなる）。どちらも Main の控えにだけ残る。

lifecycle への差し込みは1点だけで、`initialized` の後・`configurationDone` の前に `setBreakpoints` を送る仕込みを `debugSessionManager` に足した。**`launch` の応答を開始の合図にしない**（Session 6-2 の決め）は動いていない。仕込みが登録されていなければ `await` そのものを踏まない形にしてあり、6-3 の有無で 6-2 の lifecycle は1 tick も変わらない。

`MonacoEditor.tsx` への追加は**器を state に載せる行とフックを呼ぶ1行だけ**（Session 6-0 の申し送り）。呼ぶ位置が一番下であることに意味があり、上に置くと「印を当てる → Model を差し替える」の順になって decoration が捨てられる ── production build の確認で実際にそうなった。

### Session 6-4 〜 6-13（予定）— 実装

| Session | 入れるもの                                                                     | 推奨 Agent   |
| ------- | ------------------------------------------------------------------------------ | ------------ |
| 6-4     | 実行制御（Continue / Pause / Step Over / Into / Out / Stop）と停止行           | Claude Code  |
| 6-5     | Call Stack（フレーム選択 → 該当行へジャンプ。§5 の「エラー位置へのジャンプ」） | どちらでも可 |
| 6-6     | Variables                                                                      | Codex        |
| 6-7     | Debug Console（出力と評価）                                                    | Codex        |
| 6-8     | Debug パネルの器・Panel Registry 登録・Debug Toolbar                           | Claude Code  |
| 6-9     | Debug Profile の編集 UI と Settings / Status                                   | Claude Code  |
| 6-10    | Python（debugpy）                                                              | どちらでも可 |
| 6-11    | C#（netcoredbg）                                                               | どちらでも可 |
| 6-12    | Command / Keybinding（F5 / F9 / F10 / F11 / Shift+F11 / Shift+F5）             | Codex        |
| 6-13    | STEP 6 Closing（production build での横断確認とドキュメント追従）              | Claude Code  |

最初に繋ぐのは **Node**（この PC で実機確認できる唯一の言語。Python / .NET SDK は STEP 5 Closing 時点と同じく未導入）。ただし Node の adapter だけは**入手経路と stdio 対応が未確定**で、6-1 の最初の仕事がその確認になる（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20.7）。

### 実装を Claude Code と Codex で分担する

STEP 5 は両方を使って進めたが、**同じ working tree で並行実装はしない**と決めた。STEP 5 の実測で、機能追加の5 Session（5-5 〜 5-9）が**9つの同じファイルを全員触っていた**ため ── 契約・チャンネル・preload の口・ハンドラ・provider の登録は、機能を1つ足すたびに必ず同じ場所へ1行ずつ増える。DAP でも同型になり、さらに `MonacoEditor.tsx`・Panel Registry・i18n の3面が加わる。

運用は次で固定する。

> **1 Session を1つの Agent が担当し、`npm run verify` を通して Commit / Push してから次の Agent へ渡す。**

| 項目               | 決めたこと                                                                        |
| ------------------ | --------------------------------------------------------------------------------- |
| 引き継ぎの単位     | Session（= 1 commit）。**push していない commit を残さない**                      |
| 切り替えてよい境界 | 6-0 → 6-1、6-2 → 6-3、6-9 → 6-10、6-10 → 6-11                                     |
| 切り替えない区間   | 6-3 / 6-4 / 6-5 の内部（`MonacoEditor.tsx` を3連続で触る）、6-9 の内部            |
| ドキュメント       | **Closing まで溜めない。**各 Session の commit に決めたことを最小限で残す         |
| 役割               | Codex が先行調査 → Claude Code が設計確定（Session 6-0 がその最初の適用にあたる） |

### STEP 6 で意図的に入れないもの

一覧は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20.11。主なものは `launch.json` の読み込み・attach・統合端末での実行・条件付き breakpoint・変数の書き換え・複数セッションの同時実行・`cwd` の指定・`${...}` の変数展開。

### Session 6-1 へ渡す条件

| 条件                                                  | 状態                                                                                      |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Debug Profile の欄が確定している                      | 7欄（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §20.3）                                 |
| Renderer へ渡さないものが確定している                 | §20.9 の表                                                                                |
| program args と adapter args の区別が明文化されている | §20.4                                                                                     |
| launch lifecycle の順序が確定している                 | §20.8。**`launch` の応答を開始の合図にしない**まで含む                                    |
| 切る口の数が決まっている                              | 要求17・購読4（§20.9）                                                                    |
| 言語の増やし方が決まっている                          | `language` を判別子とする union。v1 は枝ごとの固有欄ゼロ（§20.10）                        |
| 6-1 の分割が決まっている                              | `dapMessage.ts` / `dapConnection.ts` / `adapterProcess.ts` / `adapterCatalog.ts`（§20.7） |
