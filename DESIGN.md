# Fluvix Nexus 設計ドキュメント

> ステータス: STEP 1（基盤構築）完了 / STEP 2（Dockable Workspace 基盤）完了 / STEP 3（各パネルの本機能）進行中 — Session 3-6-8 完了
> 最終更新: 2026-08-19

このドキュメントは**製品としての設計方針**を扱う。実装の構造と開発手順は以下を参照。

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — 現在の実装構造、IPC の作法、セキュリティの状態
- [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) — コマンド、品質基盤の方針、動作確認の手順、exe 化の残作業

## 1. 目的

軽量・シンプルでありながら、実際のアプリ開発に日常的に使える Windows 向けコードエディタを作る。

VS Code や Cursor のように何でも詰め込むのではなく、**必要な機能を高品質に絞り込む**方向性を取る。まずは開発者本人が日常的に使えるレベルの品質を目指し、最終的には `.exe` 化して他のユーザーにも無料で配布できるデスクトップアプリにする。

核となるコンセプトは以下の一言に集約される。

> 必要な開発機能だけを高品質に持ち、Files・Terminal・GitHub・Workspace を自由に組み替えて、自分専用の開発環境を作れる軽量コードエディタ。

---

## 2. 基本構成（技術スタック）

- **Electron + React + TypeScript** を第一候補とする。
- プロセス構成は以下を明確に分離する。
  - **Main Process**：アプリのライフサイクル管理、ウィンドウ管理、OS 連携処理の実体
  - **Renderer**：UI 描画（React）。OS に直接触れる処理は行わない
  - **Preload**：Renderer に安全な API のみを橋渡しする層
  - **IPC**：Main と Renderer 間の通信経路
- Terminal・Git 操作・ファイル操作など OS に触れる処理は Renderer から直接行わず、Main Process 経由の安全な構成にする。
- Node.js / Python / .NET SDK 本体はアプリに同梱せず、PC にインストール済みの環境を検出して利用する（軽量さを優先）。

---

## 3. パネル構成

基本パネルは4つ。すべて独立した Dockable Panel として扱う。

| パネル    | 役割                                          |
| --------- | --------------------------------------------- |
| Workspace | コード編集                                    |
| Files     | ファイル管理                                  |
| Terminal  | PowerShell、Node、Claude Code などの CLI 実行 |
| GitHub    | Commit / Push / Pull などの Git 操作          |

### Dockable UI

- 上下左右への移動、リサイズ、タブ化、独立ウィンドウ化に対応する。
- パネルサイズに応じて UI が自動的に形を変える（レスポンシブ・パネル）。
- パネル識別のため、ダークテーマを基調としつつ非常に薄い識別色を使う。
  - Files：薄い青
  - Terminal：薄い緑
  - GitHub：薄いオレンジ or 紫
  - Workspace：ほぼ無彩色
- 自由に配置を変えても、どのパネルかが直感的に分かることを目指す。

### パネルの表示切り替え

- 使わないパネルは閉じられ、必要になったら View メニューからいつでも戻せる。
- 閉じることはパネルの定義を消すことではない。閉じたパネルも一覧には残り続ける。
- 戻したときは「本来の居場所」＝適用中のプリセットでの位置に近い場所へ戻る。既に組んだ配置は崩さない。

### レイアウトプリセット

- 自分好みの配置を保存・復元できるようにする。
- 例：Coding / Claude Code / Debug などのプリセット、および自作レイアウトの保存。
- 現在は Default のみ実装。プリセットは表に追加するだけで増える構造にしてある。
- レイアウトロック機能も候補として検討する。

### 前回の配置で起動する

- 組み替えた配置（分割・タブ・大きさ・閉じたパネル）は自動で保存し、次回起動時にそのまま復元する。保存操作は要求しない。
- 保存先はアプリの設定データ（`%APPDATA%` 配下）で、プロジェクトフォルダには書かない。配置は「その人の道具の形」であって、プロジェクトの持ち物ではないため。
- 保存データが壊れていても起動できなくならないこと。読めなければ Default の配置で立ち上げる。
- 「レイアウトを初期化」は画面だけでなく保存内容も初期化する（次回起動で古い配置が戻らない）。

### Files パネルの表示切り替え

- 縦長時：一般的なファイルツリー表示。
- 横長時：Unity の Project ウィンドウ + Mac Finder のカラム表示のように、フォルダ階層を横方向に展開する表示。
- **どちらも同じファイルを見ている。** 表示方式が変わるのは並べ方だけで、開いているフォルダ・読み込み済みの中身・操作（作成 / 改名 / 削除 / 移動 / コピー / ドラッグ&ドロップ / 検索）は共通。切り替えても読み直しは起きない。
- **形に合わせるが、選んだら勝手に変えない。** まだ選んでいない間はパネルの形に従う（横長ならカラム、縦長ならツリー）。ツールバーで選んだ後は、リサイズしても置き場所を変えても選んだ方のままにする。境界の付近でパネルを掴んで動かしても表示方式が往復しないよう、切り替わる幅と戻る幅には遊びを持たせる。
- 選び直しはいつでもでき、選択中のボタンをもう一度押せば「形に合わせる」状態へ戻る。
- **選んだ見え方はアプリを閉じても残る**（表示方式・カラムの幅）。カラムの幅は境目を掴んで変えられ、全部の列が同じ幅になる。

### GitHub パネル

- Git 初心者でもターミナルを使わず操作できることを目標にする。
- 初回のみ「GitHub に公開」ボタンから、Git 初期化 → 初回 Commit → GitHub リポジトリ作成 → remote 設定 → Push までを案内・自動化する。
- 2回目以降は `Commit & Push` / `Commit` / `Push` / `Pull` の操作ボタンを表示する。
- 初心者向けに「①変更確認 → ②コミットメッセージ → ③Commit & Push」の順番が分かるガイド UI を用意する（※詳細な UI 実装は後日）。
- 単体ウィンドウとして切り離して表示可能にし、Unity や DTM ソフトなど別の制作環境の横に並べて使えるようにする。

### Terminal パネル

- 自由ドッキングに対応。
- 複数タブを持ち、PowerShell / Claude Code / Node / その他 CLI を切り替えて使える。
- Claude Code を長時間使う場合、Terminal を右側に大きく配置するような使い方も想定する。

### Workspace（開いているプロジェクトフォルダ）

4つのパネルはいずれも「今開いているフォルダ」を共通の対象として動く。Files はそのフォルダを辿り、Terminal はそこを作業ディレクトリとして起動し、Git はそこをリポジトリとして扱い、Editor はその中のファイルを開く。したがって**フォルダを開くことがアプリの入口**になる。

- フォルダを開く / 閉じるができる。開く手段は OS 標準のフォルダ選択ダイアログ。
- 次回起動時は前回のフォルダで開く。保存操作は要求しない（レイアウトと同じ考え方）。
- 保存先はアプリの設定データ（`%APPDATA%` 配下）。**プロジェクトフォルダの中には何も書かない。**
- 前回のフォルダが消えている / 繋がっていない場合は、落ちずに未選択の状態で起動し、理由を伝える。
- 未選択のときは Editor に Welcome を出す。「まず何をすればよいか」が分かる状態にしておく。
- 「最近開いたフォルダ」は将来対応（開いた記録は id / 名前 / 開いた時刻を持つ形にしてある）。

#### 用語について

「Workspace」はこのドキュメントの中で3つの意味に使われている。実装では次のように呼び分ける。

| ここでの呼び方                 | 実装での名前     | 実体                         |
| ------------------------------ | ---------------- | ---------------------------- |
| Workspace（§3 の表のパネル）   | Editor パネル    | コード編集の画面             |
| 画面全体の器（Dockable UI）    | Workspace Shell  | パネルを並べる枠と、その配置 |
| 開いているプロジェクトフォルダ | Workspace Folder | このセクションが扱うもの     |

---

## 4. コードエディタ（Workspace）

**Monaco Editor + LSP** を有力候補とする。

最低限、以下の機能を実装し、「VS Code ほど多機能ではなくても、1日普通に開発してストレスが出ない品質」を目標とする。

- シンタックスハイライト
- 行番号
- コード補完
- エラー・警告表示
- オートインデント
- 複数タブ
- 検索・置換
- プロジェクト全体検索
- Go to Definition
- Rename Symbol
- フォーマット
- コメント切替
- 基本ショートカット

「1日普通に開発してストレスが出ない」ためには、上の一覧に無いものも要る。**アプリの外でファイルが変わる**（ビルドツール・Git の checkout・別のエディタ）ことへの備えと、**未保存の内容を取り違えて失わない**ことがそれで、Session 3-5 で入れた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12）。機能の数には出ないが、無いと「開いたまま作業を続けられない」。

### 対応言語（v1）

v1 から正式対応する言語は以下の3つとする。

- JavaScript / TypeScript
- Python
- C#

Node.js、Python、.NET SDK 本体はアプリに同梱せず、PC にインストールされている環境を検出して LSP 経由で利用する。

---

## 5. デバッグ機能

v1 から対応する。**DAP（Debug Adapter Protocol）** の利用を前提に検討する。

最低限実装する機能：

- Breakpoint
- Continue
- Pause
- Step Over
- Step Into
- Step Out
- Stop
- Variables
- Call Stack
- Debug Console
- エラー位置へのジャンプ

---

## 6. Claude Code 連携

- 独自 AI を新規開発するのではなく、**Claude Code を操作しやすくする UI** として設計する。
- **第一段階**：Terminal パネルから Claude Code を CLI としてそのまま利用する。
- **将来段階**：専用パネルから自然言語で指示を書き、以下の流れを直接つなげる。

  ```
  Fluvix Nexus（専用パネル） → Claude Code → 現在開いているプロジェクトを編集
  ```

- Claude Code などの外部 AI からの指示は、本ソフト上の Terminal または Workspace 上で実行される構成とし、AI エージェント自体の実装は行わない（あくまで実行環境・操作 UI を提供する立場）。

---

## 7. 配布・更新まわり（方針レベル）

以下の3点は、現時点では詳細実装ではなく**方針**として整理する。詳細は STEP 1 以降で詰める。

### 7.1 Windows 向け exe 化

- 最終的に Windows の `.exe` として配布する。
- Installer / アプリアイコン / バージョン管理 / GitHub Releases 対応を見据えた構成にしておく。
- **electron-builder** を前提とする。STEP 1 では導入せず、それを妨げない構成（`productName` の設定、`electron` の devDependencies 化、`out/` への成果物分離、ユーザーデータを `userData` 配下へ統一）までを済ませている。残作業は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §6 を参照。

### 7.2 自動更新（方針）

- **electron-updater** を前提候補とする。
- 配布形式・署名方式などの詳細は将来対応として整理し、現時点では確定しない。

### 7.3 更新通知 UI（方針）

- 自動更新で取得した更新内容を、既存の Dockable UI に馴染む形で通知する。
- **専用パネル方式**または**モーダル方式**のどちらか（あるいは両方の使い分け）を想定するが、具体的な UI 設計は後日詰める。

### 7.4 無料配布（方針）

- ユーザーには無料で提供する方針。
- 配布は **GitHub Releases** を基本とする。
- Windows SmartScreen 対策やコード署名（証明書の取得・運用）は、**将来対応**として切り分ける（v1 の必須要件にはしない）。

---

## 8. 将来的な Mac 対応を妨げない設計方針

- 開発対象自体は当面 Windows 向けだが、以下の点でクロスプラットフォーム化の余地を残す。
  - Electron を採用することで OS 依存部分を Main Process に閉じ込め、Renderer / UI ロジックは OS 非依存に保つ。
  - OS 固有処理（ファイルパス、シェル起動コマンド、通知 API など）は抽象化レイヤーを介して呼び出し、Windows 固有実装を差し替え可能な形にしておく。
  - パネルレイアウト・エディタ・LSP/DAP 連携などのコア機能は OS に依存しない設計とする。
  - 自動更新・署名まわりは OS ごとに実装が異なることを前提とし、将来的に macOS 対応を追加する際に electron-updater 側の macOS 対応（notarization 等）を後から組み込める構成にしておく。
- v1 では Mac 対応の実装は行わない。あくまで「後から対応を追加しやすい構造にしておく」ことが目的。

---

## 9. 未確定・後日詰める事項

- 更新通知 UI の具体的なビジュアル・配置
- コード署名 / Windows SmartScreen 対策の具体的な進め方
- テレメトリ・クラッシュレポートの要否
- 初心者向けガイド UI の詳細（Git ガイド以外にも展開するか）
- レイアウトロック機能の要否と仕様
- Mac 対応を実際に開始するタイミング
- 対応する文字コードの範囲（UTF-8 以外を開けるようにするか。境界は整理済み ── [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.5）
- Editor の内容を別の場所へ書き戻す手段（Save As）の仕様

---

## 10. 実装ステップと現状

### STEP 1（完了）— 基盤構築

Electron + React + TypeScript のベース構築と、Main / Renderer / Preload / IPC の分離。

| セッション | 内容                                     |
| ---------- | ---------------------------------------- |
| 1-1        | プロジェクト初期セットアップ、3層の分離  |
| 1-2        | IPC 基盤（登録基盤・エラーの正規化）     |
| 1-3        | IPC 契約レイヤー（shared）と結果型       |
| 1-4        | セキュリティ設定と起動確認               |
| 1-5        | 全体整理・品質基盤・アプリシェルの仕上げ |

STEP 1 の成果物は「機能を載せるための土台」であり、パネル UI・エディタ・Terminal などの機能は含まない。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。

### Session 1-4 からの持ち越し項目の判断

| 項目                           | 判断                    | 理由                                                                                                                                  |
| ------------------------------ | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| ウィンドウ状態の保存 / 復元    | STEP 1 で対応           | 常時起動して使う道具の体感品質に直結する。レイアウトプリセットが再利用する永続化層をここで用意できる                                  |
| 単一インスタンス制御           | STEP 1 で対応           | 保存ファイルの同時書き込みを防ぐ。Terminal / LSP / DAP がプロセスを持つ前に前提を固定する必要がある                                   |
| 権限要求の既定拒否             | STEP 1 で対応           | セキュリティの既定値であり、後から足すと「いつから安全か」が曖昧になる                                                                |
| アプリメニュー整理             | STEP 1 で対応（最小限） | 配布ビルドから開発者向け機能を外す。本格的な操作面はアプリ内 UI に置く方針のため、メニュー自体は育てない                              |
| ログ基盤                       | STEP 1 で対応（最小限） | 出力先を一本化しておかないと、Terminal / LSP / DAP が増えた時点で追えなくなる                                                         |
| CSP の運用整理                 | STEP 1 で対応           | ポリシーの明文化と、dev / 配布ビルドでの適用範囲の違いの確認                                                                          |
| Format / Test 基盤             | STEP 1 で対応           | コードが増える前に入れるほど安い。Prettier + Vitest                                                                                   |
| Lint（ESLint）                 | 見送り                  | TypeScript 7 に `typescript-eslint` が未対応で、`.ts` を解析できない。対応後に再検討（[docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §3） |
| electron-builder による exe 化 | 準備のみ                | 実際のパッケージングはアイコン・appId・署名方針など決めるべきことが多く、独立した STEP として扱う（§7.1）                             |

### STEP 2（完了）— Dockable Workspace 基盤

Dockable UI の土台。**パネルの中身（Files / Editor / Terminal / Git の本機能）は STEP 3 の担当**で、STEP 2 の対象は「どのパネルでも同じように置ける器」を作りきることだった。

| セッション | 内容                                                   |
| ---------- | ------------------------------------------------------ |
| 2-1        | Workspace Shell（レイアウトの器・Panel Registry）※完了 |
| 2-2        | Dock / Split のデータモデルと基本操作 ※完了            |
| 2-3        | パネルのドラッグ&ドロップ ※完了                        |
| 2-4        | Split 境界のリサイズ ※完了                             |
| 2-5        | パネルの表示管理・レイアウトプリセットの基盤 ※完了     |
| 2-6        | レイアウトの保存 / 復元（永続化）※完了                 |
| 2-7        | STEP 2 全体の統合テスト・品質整理 ※完了                |

Session 2-1 で画面の骨格を、Session 2-2 で Dock / Split をレイアウトデータの操作として実装し、Session 2-3 でそれをタブのドラッグ&ドロップにつないだ。Session 2-4 で領域の境界をドラッグして大きさを変えられるようにし、Session 2-5 でパネルを閉じる / 戻す操作とレイアウトプリセットの器を入れた。パネルを掴んで別の領域へ移す・上下左右へ落として分割する・境界を掴んで好みの大きさにする・要らないパネルを閉じて必要なときに戻す、という §3 の「自由な組み替え」がマウスだけで行えるようになっている。Session 2-6 でその結果を保存し、次回起動時に同じ配置で立ち上がるようにした。

Session 2-7 では新しい機能を足さず、**Dock → Drag → Resize → Close → Restore → Save → Restart → Restore** という実際の利用フローを1本の流れとして検証し、保存データが壊れている場合の振る舞いを一通り確かめたうえで、レイアウト状態を保持する層の置き場所を整理した（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.2）。構造とデータモデルは同 §7、確認の内訳は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) §4。

配置は操作のたびに自動保存される（明示的な保存操作は無い）。保存先は `%APPDATA%/Fluvix Nexus/workspace-layout.json` で、Renderer はファイルに触れず IPC 経由で Main に預ける。保存データにはバージョン番号を持たせ、読めない・古すぎる・壊れている場合は Default の配置で起動する（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.8）。

閉じたパネルは定義としては残り、View メニューからいつでも戻せる。戻す位置は適用中のプリセットを設計図にして決めるため、Default 以外のプリセットを足せば「そのプリセットでの定位置」へ戻るようになる（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.7）。

領域の最小サイズは値をコンポーネントに書かず `layout/constraints.ts` 1箇所で決めている。Editor のように「置けるだけでは足りず、実際に操作できる広さが要る」パネルはそこで個別に底上げする（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §7.6）。

STEP 2 の時点で足りていなかった **Main → Renderer のイベント経路**は、Session 3-3 で既存の IPC 契約と対になる形で追加した（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §3.3）。Terminal の出力・LSP / DAP の通知も同じ経路に載る。

### STEP 3（進行中）— 各パネルの本機能

STEP 2 の Workspace 基盤に載せる形で、パネルの中身を実装していく。

| セッション | 内容                                                            |
| ---------- | --------------------------------------------------------------- |
| 3-1        | Workspace / プロジェクトフォルダの基盤 ※完了                    |
| 3-2        | Files のファイルツリー基盤 ※完了                                |
| 3-3        | Files 基本操作・Editor 連携基盤 ※完了                           |
| 3-4        | Monaco Editor 本実装 ※完了                                      |
| 3-5        | Editor 実用機能・外部変更監視 ※完了                             |
| 3-5.1      | Files 既知不具合の修正 ※完了                                    |
| 3-5.2      | Delete のエラー分類 ※完了                                       |
| 3-6-1      | Files のファイル移動 ※完了                                      |
| 3-6-2      | Files のコピー / ペースト ※完了                                 |
| 3-6-3      | Files のドラッグ&ドロップ ※完了                                 |
| 3-6-4      | プロジェクト全体検索① 走査 + ファイル名 ※完了                   |
| 3-6-5      | プロジェクト全体検索② 全文検索 ※完了                            |
| 3-6-6      | Files のファイル種類別アイコン ※完了                            |
| 3-6-7      | Files の横長時のカラム表示 ※完了                                |
| 3-6-8      | Files の仕上げ（見え方の保存・カラムの幅・自動スクロール）※完了 |

Session 3-1 では、パネルの中身に入る前に**全パネルが共通の対象にするもの**を用意した。フォルダを開く・閉じる・前回のフォルダで起動する、という §3「Workspace」の振る舞いがこれにあたる。Files / Editor / Terminal / Git は、どのフォルダを対象に動くかをここから読む（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §8）。

開いているフォルダの正本は Main 側にある。ファイルを読む・シェルを起動する・Git を叩くといった処理はすべて Main で起きるため、そこが「今どのフォルダか」を持っていないと、Renderer から届いたパスを信じて OS を触ることになる。Renderer が持つのは表示用の写しで、切り替えは必ず IPC を通る。開く対象のパスを Renderer から渡せないことが、この API の限定の実体になっている（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §8.4）。

保存先は `%APPDATA%/Fluvix Nexus/workspace-folder.json` で、レイアウトとは別ファイル・別 API にしてある（用途ごとに API を切る方針。[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §5）。保存されたフォルダが消えていても落ちず、未選択の状態で起動して理由を Welcome に出す。

Session 3-2 では、その Workspace を基点に **Files パネルの中身**を実装した。フォルダを開くとルートが出て、フォルダを展開すると1階層ずつ中身が読まれる、という §3 の Files の土台にあたる（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §9）。

Renderer は fs にも絶対パスにも触れない。Renderer が言えるのは「今の Workspace の中の、この相対位置」までで、root を指定する手段はどこにも無い。`..`・絶対パス・symlink による Workspace 外への脱出は Main が2段階（パス文字列 → realpath による実体）で拒否する（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §9.3）。汎用のファイル API を公開しない方針（§5）は、root を渡せる API が1つあればそこで迂回されるため、Files 側でも同じ線を引いている。

**開いた瞬間に全体を読まない。** 読むのは常にフォルダ1階層で、展開したときに初めてそのフォルダを読む。`node_modules` を持つプロジェクトでも起動が重くならないのはこのため（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §9.4）。フォルダが読めない場合（消えた・権限が無い）もアプリは落とさず、Files パネルの中の1行として扱って、その場で再試行できるようにしてある。

Session 3-3 では、その Files を**閲覧できるツリーから、開発作業に使える入口**へ進めた。ファイルを選ぶと Editor に開き、Files から作る・名前を変える・消せるようになっている（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10）。

**Workspace の外に手が届かないという性質は、書き換える側にも同じだけ効かせている。** 読む側だけを守っても、作成 / 改名 / 削除から外へ出られれば意味が無い。作成・改名・削除はどれも「あるフォルダの中で起きる」操作なので、確かめるのは**親フォルダ**で、触るのはその実体（realpath）の直下だけに限る（§10.2）。名前に区切り文字を含められないことが、その「直下だけ」の担保になっている。

**削除は OS のごみ箱へ送るだけ**で、完全削除の経路は公開していない。UI にも確認を入れているが、UI は迂回されうる以上、戻せない削除が Renderer からの要求で起きる状態そのものを作らない。ごみ箱へ送れない場所（ネットワークドライブなど）では失敗として返し、完全削除に切り替えることはしない。

Editor 側は**タブの状態を独立したモデルとして**用意した（§10.3）。同じファイルを2枚開かないこと、リネームされてもタブが同じタブであり続けること、フォルダごと消えたら閉じることまでを、この層が決める。Monaco Editor 本体は後続で、差し替わるのは中身を描く1ファイルだけになる。

Session 3-4 では、その1ファイルを実際に **Monaco Editor** へ差し替えた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §11）。ここで §4「コードエディタ」のうち、シンタックスハイライト・行番号・オートインデント・複数タブ・検索と置換・コメント切替・基本ショートカット・そして**編集と保存**が動くようになっている。残るコード補完・エラー表示・Go to Definition・Rename・フォーマットは LSP と一緒に入る。

**Monaco のために Files や Workspace を作り直していない。** Files でファイルを選ぶ → 中身を読む → タブが持つ → Editor が出す、という Session 3-3 の経路はそのままで、最後の「出す」だけが変わった。保存はその経路に同じ作法で1本足したもので、Renderer が言えるのは相変わらず「Workspace の中の、この相対位置」までに留まる。

**STEP 1 で決めた CSP を1文字も緩めていない。** Monaco は既定では Worker の起動用スクリプトを `blob:` として組み立てるため、そのまま入れると `worker-src 'self'` では動かない。ここは CSP を広げるのではなく、Monaco 側にその経路を通らせない形（`MonacoEnvironment.getWorker`）を選び、**アプリにバンドルされた Worker だけ**を返すようにした（§11.2）。contextIsolation・sandbox・Renderer への Node 非公開も含め、前提はすべて Session 3-3 のまま。

編集の中身を持つのは **Monaco の Model** で、タブとは別の層にある（§11.5）。タブの同一性は発番した id、中身の同一性は Workspace 内の位置、と基準が違うため。エディタは1つだけ作り、タブを切り替えたら Model を差し替える。この形にすると編集内容と Undo 履歴が Model 側に付くので、**タブを切り替えても、Editor パネルを閉じて開き直しても、編集が消えない**。

保存では**無条件の上書きをしない**（§11.6）。読んだ時点の mtime とサイズを覚えておき、保存の直前に食い違っていれば書かずに返す。Reload / Compare を並べた Conflict UI は後続だが、「気づけないまま上書きする」構造にしてしまうと後から選択肢を差し込む場所が無くなるため、検出の入口だけを先に置いた。

Auto Save は **必ず OFF で始まる**（§11.7）。自動保存は利用者が明示的に選ぶものにする ── 既定で入れると、開いて眺めていたつもりの操作がディスクに残る。設定の形は将来 Settings から `onFocusChange` / `onWindowChange` を足せるようにしてあり、Session 3-4 で動くのは `off` と `afterDelay` の2つ。

Session 3-5 では、その Editor を**実際の開発で1日使える**ところまで進めた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12）。§4 の「1日普通に開発してストレスが出ない品質」に向けて足りていなかったのは機能の数ではなく、**アプリの外で起きることへの備え**だった。

ビルドツールがファイルを書き換える。Git で checkout する。別のエディタで直す。どれも開発中に普通に起きることで、それに気づけないエディタは「開いたまま作業を続けられない」。そこで Workspace の中を Main 側で見張り、変化を Session 3-3 から使っている `files:changed` に載せた（§12.1）。**Renderer に filesystem の監視 API は渡していない** ── 監視するのは Main で、届くのは相変わらず「Workspace の中の、この相対位置」だけ。

**未保存でなければ黙って取り込み、未保存なら止まる**（§12.3）。失うものが無いなら選択肢を出す意味が無く、失うものがあるなら**アプリが片方を選んではいけない**。どちらが要るかを知っているのは利用者だけで、しかも間違えると取り返せない。止まったときに出すのが Reload / Compare / 上書き の3つで、Compare は Monaco の Diff Editor をそのまま使っている。**押す前に何を失うかが分かる**ようにしてあるのが要点で、そうしないと Compare を挟む意味が薄れる。

**未保存の内容を失う操作は4つあり、確認は1本にまとめた**（§12.6）。タブを閉じる・Workspace を閉じる・Workspace を切り替える・アプリを終了する。それぞれの入口に確認を書くと、入口が増えるたびに保護が抜ける。「失われるものを持っている側（Editor）」と「失わせる操作をする側」を分け、間を1つの器でつないである。後で Terminal に「実行中のプロセスがある」確認が要るようになっても、申告を1つ足すだけで増える。

**終了時も同じ経路を通る**（§12.7）。「閉じるか」を決めるのは Main、「未保存があるか」を知っているのは Renderer という分担を崩さず、閉じる操作を Main が握ったまま判断だけを尋ねる形にした。ウィンドウの × も Alt+F4 も `app.quit()` も同じ 'close' を通るため、終了処理と保存確認が別々に走って競合することがない。ただし **OS のシャットダウンや強制終了では保証できない** ── 原理的にできないことなので、そこは Auto Save を選べるようにすることで補う。

Auto Save は4方式すべてが動くようになり、設定はアプリ再起動後も保たれる（§12.4）。保存先は `%APPDATA%/Fluvix Nexus/editor-settings.json` で、レイアウト・Workspace と同じくプロジェクトフォルダには何も書かない。Settings 画面（§9）はまだ無いが、**設定の値そのものは既にアプリの設定として保存されている**ため、画面が入っても保存形式は変わらない。

文字コードは **UTF-8 前提であることを明確にした上で、BOM の有無だけは保つ**（§12.5）。Windows のツールが付ける BOM を落としたまま保存すると、開いて保存しただけで差分が出る。Shift_JIS / UTF-16 へ広げるときに増えるのは「バイト列 ↔ 文字列」の2箇所だけで、Editor の経路は変わらない形にしてある。変換表を持つ大きなライブラリは、実際に必要になるまで入れない。

作成・改名・削除の後に**ツリー全体を読み直さない**（§10.5）。読み直すのは変わった親フォルダ1つだけで、Session 3-2 の Lazy Load はそのまま保たれる。その伝達に使っているのが、STEP 1 から持ち越していた **Main → Renderer のイベント経路**（§3.3）。操作した側（Files）と、知る必要がある側（Files のツリーと Editor のタブ）が違うため、応答ではなくイベントで配る形にした。Session 3-5 のファイル変更監視は**この経路にそのまま載った**（受け手は1つも変えていない）。Terminal の出力・Git の状態変化・LSP / DAP も同じ経路に載る。

**STEP 2 の時点では意図的に手を付けていない項目**は以下のとおり（Files は Session 3-2 / 3-3、Editor は Session 3-3 / 3-4 で着手済み）。

| 項目                                             | 補足                                                                                                                                                          |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Files 本機能                                     | ツリー・作成 / 削除 / リネーム / 移動 / コピー・ドラッグ&ドロップ・外部変更の追従・ファイル名検索・全文検索・種類別アイコン・**横長時のカラム表示**は実装済み |
| Monaco Editor 本機能                             | **Session 3-4 で実装済み**（バンドル済み Worker で `worker-src 'self'` を維持）                                                                               |
| ファイルの編集・保存                             | **Session 3-4 で実装済み**（`files:write-file`・未保存の印・Auto Save）                                                                                       |
| 外部変更の監視                                   | **Session 3-5 で実装済み**（Main で監視し、`files:changed` に載せる）                                                                                         |
| 外部変更との競合（Reload / Compare / Overwrite） | **Session 3-5 で実装済み**（Compare は Monaco の Diff Editor）                                                                                                |
| 未保存の保護（タブ / Workspace / 終了）          | **Session 3-5 で実装済み**（確認は1本にまとめてある）                                                                                                         |
| Settings（設定の永続化）                         | **Session 3-5 で実装済み**（`settings:*`）。設定を並べる**画面**は §9 として後続                                                                              |
| Terminal 本機能                                  | §3。node-pty は Main 側。出力は Session 3-3 のイベント経路に載せる                                                                                            |
| Git / GitHub 本機能                              | §3。Commit / Push / Pull と、初回の「GitHub に公開」                                                                                                          |
| ブランチ作成 UI                                  | Git パネルの一部。初心者向けガイドの方針は §3                                                                                                                 |
| LSP                                              | §4。JavaScript / TypeScript・Python・C#。SDK は同梱せず PC の環境を検出する                                                                                   |
| DAP                                              | §5。Breakpoint / Step / Variables / Call Stack                                                                                                                |
| パネルの独立ウィンドウ化                         | §3。Main 側で別ウィンドウを開き、Shell のルートを分岐する                                                                                                     |
| タブのドラッグによる並べ替え                     | レイアウト操作としては実装済みで、UI の入口だけが無い（ARCHITECTURE.md §7.9）                                                                                 |
| レイアウトプリセットの追加・自作レイアウトの保存 | Coding / Debug などの表への追加と、名前を付けた保存（§3）                                                                                                     |

STEP 2 の基盤に対しては、これらのために先回りした実装を入れない。パネルは Registry に登録された中身を描くだけで、配置・大きさ・可視状態は Workspace 側が持つ、という分担を保つ。

Session 3-5.1 では、Session 3-5 の作業中に見つかっていた **Files の不具合3件**だけを直した（新しい機能は足していない）。3件とも「規則そのものは正しいのに、当てる相手を間違えていた」という同じ形をしている。

**利用者が指した名前と、検証のための文字列を混ぜない。** 相対位置を丸ごと trim していたため、`notes.txt ` を指した要求が `notes.txt` を指す要求に化けていた。trim は判定にだけ使い、fs へ渡す名前は生のまま通す（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.2）。

**「これから作れる名前か」と「既に在るものを指せる名前か」は別の問い。** 新規作成向けの規則を削除にも当てていたため、他の OS で作られた `aux.ts` が**ツリーには並ぶのに消せない**状態になっていた。守るものが何も無いのに手が出せないものが残るのは、名前の規則が守ろうとしていたものとは無関係な不便でしかない。境界の検証（Workspace の外へ出ないこと・symlink の扱い・触る場所を親の realpath 直下に限ること）は緩めていない。

**「確定した」と「送信している」も別。** リネームの同名衝突で失敗した後、入力欄が確定済みとして固まり、打ち直すことも取り消すこともできなくなっていた。二重送信を抑えるのは応答を待っている区間だけで、失敗したら必ず打ち直せる状態へ戻す（§10.4）。

修正の過程で、**Windows のシェル削除 API が末尾の空白 / ドットを落とす**ことも分かった（`notes.txt ` を渡すと隣の `notes.txt` がごみ箱へ入る）。指したものと違うものが消える方が、消せないことより悪いので、宛先を伝えられない名前は渡さずに断る。改名は `fs.rename` を使うので字義どおり動くため、**改名してからなら消せる**（§10.2）。

Session 3-6-1 では **Files のファイル移動**を足した（`files:move`）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.7。

改名（`files:rename`）に相乗りさせず別のチャンネルにしたのは、**移動が2つの場所を指す操作**だからにほかならない。改名は「同じフォルダの中で名前を変える」に閉じており（新しい名前に区切り文字を含められない）、境界を確かめる相手は1つしかない。そこへ移動を混ぜると、片方だけを通した経路から Workspace の外へ手が届く。分けたことで、Session 3-2 / 3-3 で決めた検証の手順（相対位置の検証 → 親の realpath → 境界 → その直下だけを触る）を**元と先の両方に**そのまま通せている。

**移動にしか無い失敗が1つだけある。** 移動先が動かすもの自身か、その中（`src` を `src/lib` へ）。パスの形も名前も正しいので、fs に任せると OS の「引数が変」で返り、利用者に何を直せばよいかを伝えられない。判断できる側で判断し、理由を添えて断る。

変化の伝え方は変えていない。移動は改名と同じ `renamed` として `files:changed` に載り、**受け手（Files のツリー・Editor のタブ）は1行も変えていない** ── 「位置が変わった」から読み直す相手を導く形にしてあったため、改名では1つのフォルダに、移動では2つのフォルダに、同じ規則のまま落ちる。Session 3-3 のイベント経路が、また1つ機能を受け止めた形になる。

UI は「動かすものを決める → 行き先のフォルダを右クリックする」の2手。行き先を選ぶダイアログを別に作っていないのは、**行き先を選ぶのに一番向いた道具が目の前のツリーそのもの**であるため。

Session 3-6-2 では **Files のコピー / ペースト**を足した（`files:copy`）。対象は単一のファイル / フォルダで、詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.8。

境界の確かめ方は移動と**まったく同じものを使い回している** ── 元とコピー先の両方に同じ手順を通す。移動でその形にしておいたので、コピーのために境界の検証は1行も増えていない。

**同名でも断らず、上書きもしない。** `example.txt` → `example copy.txt` → `example copy 2.txt` と名前を変えて作る。作成・改名・移動では「同じ名前があります」と断っているのに、コピーだけ訊き直さずに済ませているのは、複製が「同じものをもう1つ」という操作で**名前そのものに意味が無い**ため。名前の規則は Main と Renderer の両方が同じ答えを見る必要があるので、他の名前の規則と同じく shared に置いた。

**リンク（symlink / ジャンクション）は辿らない。** 辿れば Workspace の外にある実体の中身を中へ持ち込めてしまい、「外は読まない」という線がここで抜ける。フォルダの中にあるリンクはとばして残りを複製し、**とばした件数を画面に出す**（複製したつもりで中身が欠けている方が、断られるより悪い）。リンクそのものを複製しようとした場合は断る ── 削除・移動がリンクに対して通る（動かすのは入口だけ）のと矛盾しない。違うのは、コピーだけが中身を持ってくる操作である点。

フォルダの再帰コピーは1回の操作では終わらないため、途中で失敗しうる。そのとき**作りかけを消しに行かない** ── 消す手段（完全削除）を持たない方針を、アプリ自身が作ったものに対しても曲げない。残ったものはツリーに現れ、「途中で止まって作りかけが残っている」ことを文言で伝える。

変化の伝え方は今回も変えていない。コピーは `created` 1件として `files:changed` に載り、フォルダの中身までは配らない（展開されていなければ読まれておらず、展開されていれば既存の読み込み経路が拾う）。**コピー専用の更新経路を1つも作っていない。**

UI は移動と同じ2手（右クリック →「コピー」→ 貼り付け先を右クリック →「ここに貼り付け」）。OS のクリップボードは使わない。控えは Renderer の中だけに持ち、`{ mode: 'copy' | 'cut', entry }` という形にしてある ── `cut` は器だけ用意して**まだ UI へ出していない。** cut は結局のところ移動であり、移動には既に入口がある。同じ操作への入口を2つ並べる前に、どちらを主にするかを Session 3-6-3 のドラッグ&ドロップと合わせて決めた（下記）。

Session 3-6-3 では **Files パネルの中のドラッグ&ドロップ**を足した。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.9。

**新しいファイル操作は1つも作っていない。** 足したのは、Session 3-6-1 の移動と Session 3-6-2 のコピーに対する**2つめの入口**だけ ── 行を掴んでフォルダへ落とせば移動、Ctrl を押しながらならコピーになる（Windows のエクスプローラと同じ約束）。落とせるかの判定も、右クリックの2手が使っている関数をそのまま呼んでいる。同じ規則をドラッグ側に書き直すと、右クリックからは断られる操作がドラッグからは通る、という食い違いが生まれる。

**禁止の最終的な正本は Main のまま。** 自分自身・自分の子孫・同じフォルダへの移動は画面に出ている位置だけで判断できるのでその場で断るが、ジャンクションを挟んだ行き先が実は Workspace の外だった、という形は Renderer からは見抜けない（realpath を持たない）。そこは Main が断る。実際、ツリー上はフォルダに見えるジャンクションへのドロップは、案内が出たうえで Main に拒否される ── Renderer の判定は「できない操作を見せない」ためのものであって、許可の根拠ではない。

**案内のために要素を1つも重ねていない。** 落とせるフォルダ行と、Workspace root にあたるツリーの余白に色を付けるだけで、上に別の要素を敷かない。敷くとそれがカーソルの下に入り、どの行を指しているかを判定できなくなる（カーソルに付く小さな表示だけは重なるので、ポインタイベントを受けないようにしてある）。落とせない場所では、落とせるように見える表示を出さない。

**cut は UI へ出さないことに決めた。** 移動の主な入口はドラッグ&ドロップになり、右クリックの「移動…」は行き先が画面に出ていないときの道として残る。ここへ「切り取り → 貼り付け」を足すと、同じ操作への入口が3つになり、3つめは既にある2手と手数も結果も同じものになる。器（`mode: 'cut'`）は残す ── エクスプローラとの受け渡しを入れるときに要る形で、それは Main 側の別の経路の話になる。

Session 3-6-4 では **プロジェクト全体検索の基盤とファイル名検索**を足した（`files:search`）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.10。

§4 の一覧にある「プロジェクト全体検索」のうち、**名前で探す**ところまで。ファイルの中身に対する全文検索は Session 3-6-5 で、この走査・上限・取り消しの上に載せた（下記）。

**走査は Main が持つ。** Renderer から列挙（`files:read-directory`）を繰り返して自前で再帰する形にしなかったのは、そうすると Lazy Load のために1階層ずつにした経路が、検索という別の目的で全階層を舐める経路に化けるため。Renderer が言えるのは検索語までで、返るのも相対位置を持つ `FileEntry` だけになる（絶対パスは相変わらず持たない）。

**Workspace の外へは出ない。** 起点は realpath まで解決した root ひとつで、そこから先は readdir が返した名前を継ぎ足して降りるだけ ── 外から相対位置を受け取らないため、検証すべき入力そのものが無い形にしてある。リンク（symlink / ジャンクション）の中へは潜らない（コピーが辿らないのと同じ線）。リンクそのものは、そこに在るものとして1件出す。

**無制限に列挙しない。** 件数（500）・深さ（12 段）・走査数（50,000 件）・時間（4 秒）の4つで打ち切る。件数だけでは足りない ── 一致が0件でも数十万件を舐めうるため、走査数と時間は「見つからないときのため」の上限にあたる。打ち切りは失敗ではなく「ここまでしか見ていない」という事実として、理由ごとに違う文言で伝える。`.git` / `node_modules` を外す規則は**外部変更の監視と共有した**（2箇所に書くと、`.git` の中の変更は届かないのに検索結果には出る、という食い違いが生まれる）。

**走るのは常に1本。** 新しい検索を始めると古い検索は Main 側で必ず止まり、利用者からも止められる。Workspace の切り替え / Close でも進行中のものを捨てる。取り消しは失敗ではなく、**そこまでに見つけたものと一緒に返る**。

**パネルは増やしていない。** 検索は Files パネルの中のもう1つの見せ方で、ツールバーの虫めがねで切り替え、左上の `<` でツリーへ戻る。切り替えでどちらの状態も捨てないため、ツリーの展開状態も検索結果も行き来で失われない。結果を押すと**ツリーの行を押したときと同じ経路**で Editor が開き（検索専用の「開く」を作っていない）、その場所はツリー側でも祖先が開いて選択される。

検索中・0 件・取り消し・打ち切り・失敗は必ず言い分ける。利用者の次の一手（待つ / 語を変える / 絞り込む / もう一度試す）がそれぞれ違うため、1つの「結果がありません」にまとめない。

Session 3-6-5 では **ファイル本文の全文検索**を足した（`files:search-content`）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.11。

これで §4 の一覧にある「プロジェクト全体検索」が、名前と中身の両方で行えるようになった。**Session 3-6-4 の経路は1つも変えていない** ── 全文検索は別のチャンネルとして足し、共有したのは値と純粋な関数（除外の規則・深さと走査数の上限・検索語の規則）と、走っている検索の管理だけになる。

**中身を探すのは Main。** Renderer へ渡るのは相対位置・行・桁と、一致の周りを切り出したテキストだけで、ファイルの中身そのものは運ばれない（絶対パスも相変わらず持たない）。読むのはテキストだけで、バイナリ・大きすぎるファイル・`.git` / `node_modules`・**Workspace の外を指すリンク**は読まずにとばす。とくにリンクを読まないのは、読めば外にある実体の中身が結果に載るため。

**「Editor で開けるものを探す」に揃えた。** 1ファイルの大きさの上限を Editor と同じ 2 MB にしてあるので、見つかったものは必ず開けるし、開けるものは必ず探される。別の値にすると、そのどちらかが必ず食い違う。

**上限は6つ**（一致の総数 1,000 / 1ファイル 50 件 / 読むファイル 2,000 件 / 1ファイル 2 MB / 時間 6 秒 / 深さ・走査数は 3-6-4 と共有）。すべて失敗ではなく「ここまでしか見ていない」として、理由ごとに違う文言で伝える。1ファイルの中で打ち切った場合は、そのファイルの行に `50 件以上` と出る。

**走るのは合わせて常に1本。** 名前の検索と全文検索は同じ管理を通るため、モードを切り替えて検索し直した瞬間に前の検索が止まる。Workspace の切り替え / Close で捨てるのも同じ。

**入口は増やしていない。** Files パネルの検索モードの中が「ファイル名 | 全文」の切り替えになっただけで、パネルも View メニューの項目も増えない。行き来してもどちらの結果も消えない。結果は **フォルダ → ファイル → 一致** の3段で並び、`3:7 const example = 1` のように行・桁と周辺のテキストが読める。

**押すと Editor が該当位置へ飛ぶ。** 開く経路はツリー・名前検索と同じで、**既に開いているファイルならタブは増えず**、そのタブが手前に出て位置だけが動く。一致した範囲はそのまま選択されるので、飛んだ先で探し直さずに済む。

置換・正規表現・検索フィルター・Git 連携検索・AI 検索は**今回は実装していない**（構造としては足せる形にしてある）。

Session 3-6-6 では **Files のファイル種類別アイコン**を足した。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.12。

**機能は1つも増えていない。** 変わったのは一覧の見え方だけで、IPC も契約もデータ構造も1つも動いていない ── 種類は名前から Renderer 側で分かるものなので、`FileEntry` にアイコンの欄を足さなかった。足せば「今ディスクにあるものの写し」だったはずの型が表示の都合を持ち始め、アイコンを1つ増やすたびに IPC の応答が変わる。

**判定は1箇所、絵はその隣。** 名前から種類を決めるのは `files/fileIcon.ts`（React にも DOM にも依存しない・テスト対象）で、SVG は `FileTreeIcons.tsx`、色は CSS が持つ。ツリー・ファイル名検索・全文検索の3つが同じ関数を呼ぶため、**同じファイルがツリーと検索結果で違う絵になることが構造的に起きない**。全文検索の結果は `FileEntry` を持たず名前しか無いので、入口は名前1つに揃えてある。

**表は2つに分け、名前の表が先に効く。** 拡張子で決まるもの（`.ts` / `.tsx` / `.js` / `.jsx` / JSON / HTML / CSS / Markdown / Python / C# / 画像 / テキスト）と、名前そのものに意味があるもの（`package.json` / `.gitignore` / `Dockerfile` / `README.md`）を混ぜると、`README.md` を「md の例外」として拡張子の表に書くことになる。`package.json` が JSON ではなくパッケージの定義として出るのは、この順番の結果にほかならない。種類を増やすときに足す場所も、どちらの表かで決まる。

**外部アセットを1つも増やしていない。** アイコンフォントも画像も使わず、その場で描く SVG のままで、CSP は1文字も緩めていない（Monaco のときと同じ線。§4）。大きさは種類によらず 14px に固定してあるので、行の高さもインデントも展開の三角の位置も変わらず、Rename / 移動 / コピー / ドラッグ&ドロップ / 検索の見た目と当たり判定はそのまま。

**色は補助であって、唯一の手掛かりにしない。** 種類はまず絵の形が示していて、色が主になるのは形が同じ組（`.tsx` / `.jsx`）だけ。テキストと種類が分からないファイルには色を付けない ── 全部に色が付くと色そのものが情報を持たなくなり、一覧が色の並びになる。フォルダは閉じている / 開いているで形が変わるが、フォルダ名ごとの専用アイコン（`src`・`node_modules`）は今回持たない。

Session 3-6-7 では **Files の横長時のカラム表示**を足した（§3「Files パネルの表示切り替え」）。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.13。

**Files のデータは1つのまま。** カラム表示はツリーと**同じ `FileEntry` / `relativePath` / 読み込み済みの中身**を見る、もう1つの描き方でしかない。Main 側の API も、Files の状態も、1つも増やしていない ── 増えたのは「一番右のカラムが見せているフォルダ」1つだけで、列の並びはそこから導く（左のフォルダを選び直すと右の列が消えるのは、切り詰める処理ではなく導出の結果）。

**Lazy Load もそのまま。** カラムを開くことは*そのフォルダを展開すること*として表してあるので、読み込みは Session 3-2 からの1本の経路（展開されたのに中身を知らないフォルダを読む）に乗る。Workspace 全体の先読みは起きず、**ツリーとカラムで読み込み済みの中身が共有される**（切り替えても読み直さない）。

**「勝手に変わる」と「置いた場所に合う」を両立させる。** パネルの形は利用者がいつでも変えられるため、**形から決まる提案**と**利用者が選んだ方**を別の値として持つ。まだ選んでいない間は横長でカラム・縦長でツリーになり、一度選んだら以降リサイズでも置き場所を変えても変わらない。しきい値には遊びを持たせてあり、境界の上でパネルを掴んで動かしても表示方式が往復しない。

**選んだことはパネルより長く生きる。** パネルを Dock / Split で動かすと React の木の中で作り直されるため、選択をパネルの中に持つと*運んだだけで消える*。選択だけを Shell の外側に置いてある（パネルの形の観測はパネルの中のまま ── 場所が変われば測り直すのが正しい）。

**操作の入口は増やしていない。** Rename / Delete / 移動 / コピー / ドラッグ&ドロップ / ファイルを開く / 検索は、ツリーとまったく同じ関数を通る。カラムの余白へのドロップだけは行き先が変わり、**そのカラムのフォルダ**になる（一律 root にすると、指した場所と違うところへ動く）。

Session 3-6-8 では、Files に**新しい機能を1つも足さず**、Session 3-6-1 〜 3-6-7 で作ったものを使い続けたときに引っかかる3つを埋めた（[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §10.14）。この回で Files パネルはひと区切りになる。

**選んだ見え方が、次に開いたときにも残る。** 表示方式（ツリー / カラム）とカラムの幅がアプリを閉じると忘れられていた ── パネルの配置は Session 2-6 から保たれているのに、その中の見え方だけが戻る状態だった。保存先は `%APPDATA%/Fluvix Nexus/files-settings.json` で、**Editor の設定に相乗りさせず別のファイル・別のチャンネル**にしてある（用途ごとに API を切る方針。§5）。「パネルの形に任せる」を選び直したことも保存する ── 保存しないことでそれを表すと、一度カラムを選んだ人は二度と自動に戻れなくなる。

**カラムの幅を掴んで変えられる。** 変わるのは全部の列で、列ごとには持たない。これは好みの話ではなく、**列が導出だから**にほかならない（列は「今どこを開いているか」から毎回導かれ、選び直すたびに顔ぶれが変わる）。列ごとの幅を持たせても、その幅が「どの列のものだったか」を保てる場所がどこにも無い。中身に合わせて自動で伸ばさないのは Session 3-6-7 のままで、幅を変えるのは利用者だけ。

**ドラッグを縁で止めると、中身が流れる。** Session 3-6-3 の時点では、行き先が画面の外にあるといったん離して掴み直すしかなかった。ドラッグは「離すまでが1つの操作」なので、途中で離す必要があること自体が操作の形と食い違っている。**掴む・判定する・落とす経路は1つも変えていない** ── 足したのは「カーソルが縁にある間、その下の器を少しずつ動かす」ことだけで、落とせるかの判断も、落とした後に走る `files:move` / `files:copy` も Session 3-6-1 / 3-6-3 のまま。器の外では動かさない（そこはそもそも落とせない場所で、流すと隣のパネルへ運んだだけでツリーが動く）。

### Session 3-6-8 の後へ残していること

Files まわりで、Session 3-6-1 〜 3-6-8 の範囲から意図的に外したもの。

| 項目                          | 現状                                                                         |
| ----------------------------- | ---------------------------------------------------------------------------- |
| 切り取り / ペースト           | 器（`mode: 'cut'`）は残すが、**UI へは出さない**と決めた（上記）             |
| 複数選択とその Drag           | 今は単一のファイル / フォルダだけを対象にしている                            |
| ツリー外との Drag & Drop      | エクスプローラから落とす / 落とすと開く、は Workspace の境界を通す別の話     |
| OS クリップボードとの受け渡し | エクスプローラとのコピーには絶対パスが要る。やるなら Main 側の別の経路にする |
| 置換 / Workspace 全体置換     | 検索は読むだけ。書く側は保存の経路（`files:write-file`）に載せる形になる     |
| 正規表現 / あいまい一致       | 語は字義どおりに扱う（大文字 / 小文字だけを区別しない）                      |
| 検索フィルター（拡張子など）  | 要求は検索語だけ。足すと契約と走査の両方に条件が増える                       |
| Git 連携検索 / AI 検索        | 走査の入口が別のもの（index・履歴）になるため、別の経路として設計する        |
| フォルダ名ごとのアイコン      | `src` / `node_modules` / `.git` など。種類の表とは別の3つめの表になる        |
| アイコンテーマの切り替え      | 表と色は固定。差し替えるなら絵の表ごと切り替える形                           |
| 列ごとのカラム幅              | 幅は1つ（160〜480px）。列ごとに持つには列に安定した識別が要る                |
| カラムでの複数選択            | ツリーと同じく単一のまま（複数選択は上の行と同じ範囲）                       |
| Files のその他の設定          | 除外フォルダ・並び順などは持たない。設定画面（§9）と一緒に考える             |

Editor まわりで、Session 3-5 の範囲から意図的に外したもの。詳細は [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) §12.8。

| 項目                                | 現状                                                                     |
| ----------------------------------- | ------------------------------------------------------------------------ |
| Save As（削除されたファイルの救出） | タブは「削除済み」として残り内容も残る。**別の場所へ書き戻す手段**が無い |
| Shift_JIS / UTF-16                  | 境界は整理済み。増えるのは「バイト列 ↔ 文字列」の2箇所だけ               |
| Settings 画面                       | 設定の**値**は既に永続化されている。並べる場所を作るだけ                 |
| アプリの外での改名の対応付け        | 監視からは削除 + 作成として届く（推測で1つにまとめない）                 |
| 監視の除外設定                      | `.git` / `node_modules` を固定で外している。Settings から読む形にできる  |
| Conflict の3方向マージ              | Compare は読み取り専用。編集できる差分は Git の解決 UI と同時に考える    |
